<div id=toc></div>

# Table of Contents

- [cs.CL](#cs.CL) [Total: 2]
- [cs.LG](#cs.LG) [Total: 9]
- [cs.DC](#cs.DC) [Total: 1]
- [cs.SE](#cs.SE) [Total: 6]


<div id='cs.CL'></div>

# cs.CL [[Back]](#toc)

### [1] [CAVEWOMAN: How Large Language Models Behave Under Linguistic Input and Output Compression](https://arxiv.org/abs/2606.24083)
*Morayo Danielle Adeyemi,Ryan A. Rossi,Franck Dernoncourt*

Main category: cs.CL

TL;DR: Cavewoman是一个双通道评估协议，分别评测用户提示压缩（输入压缩）和模型响应压缩（输出压缩）对推理成本、任务准确率和文本一致性的影响。研究发现输出压缩能降低API和开源模型的推理成本（最高3倍），而输入压缩反而增加成本（最高2.7倍）且导致准确率下降和文本偏离。


<details>
  <summary>Details</summary>
Motivation: 业界广泛提倡"缩短提示、省略语法、节省token"的原始风格来降低推理成本，但输入压缩和输出压缩到底哪一方真正省钱缺乏系统验证。该研究旨在通过双通道对比评估，厘清压缩方向对成本和准确率的实际影响。

Method: 提出Cavewoman双通道评估协议，在8个模型（包括API模型和开源模型）、5个数据集、5种压缩级别下，分别对输入（用户提示）压缩和输出（模型响应）压缩进行评测，衡量指标包括任务准确率、实际单样本成本和参考文本一致性。

Result: 输出压缩在多数API模型上实现1.4-2.4倍成本降低（最高3倍），所有开源模型在公开定价下也获益。输入压缩则严格双输：成本反而上升约1.15倍（均值），最差数据集达1.8倍，强压缩下达2.7倍，同时准确率骤降。约半数非推理模型的正确生成在表层文本上偏离了未压缩基准的生成。

Conclusion: 输出压缩是有效的降本手段，而输入压缩得不偿失——不仅增加成本还损害准确性。提示压缩带来的token节省被模型更长的补偿性响应所抵消。

Abstract: "Talk short. Drop grammar. Save token." This caveman style is widely promoted as a way to cut inference cost, but whether it actually saves anything depends on which channel (the user's prompt or the model's response) is being compressed. We present Cavewoman, a two-channel evaluation protocol that scores every generation on task accuracy, realized per-item cost, and reference-text agreement against the model's unconstrained reference. We evaluate eight models on five datasets at five reduction levels, with both channels measured on the same items. Output compression cuts realized cost on most API models (1.4-2.4x per model, up to 3x in the best case) and on all four open-weight models under public-tier pricing. Input compression has the opposite effect, a strict lose-lose: it raises net cost rather than lowering it (~1.15x on the five-benchmark mean, up to 1.8x on the worst dataset and 2.7x under stronger compression), because models compensate with longer responses even as accuracy collapses. Under the same setting, surface text diverges from the unconstrained reference: on the non-reasoning models, roughly half of all generations are correct yet their surface text no longer entails the model's own unconstrained baseline generation. The divergence survives length-controlled re-scoring, multiple-comparisons correction, and replication under complementary semantic measures. Code and data are available at https://github.com/danielle34/cavewoman.

</details>


### [2] [Harmonic: Hierarchical State Space Models for Efficient Long-Context Language Modeling](https://arxiv.org/abs/2606.24650)
*Petr Nyoma*

Main category: cs.CL

TL;DR: Harmonic是一种层级式状态空间模型，通过三个不同时间尺度的递归层级堆叠实现语言建模，每层接收下层预测误差而非原始隐状态。在多个基准上超越Transformer和Mamba，支持更长序列训练，计算复杂度为O(L)。


<details>
  <summary>Details</summary>
Motivation: Transformers的二次复杂度限制了长序列建模能力，而现有SSM（如Mamba）仍有改进空间。作者希望通过层级化架构和误差传递机制实现更高效的长序列语言建模。

Method: 堆叠三个不同时间尺度的递归层级（slow/fast），每级接收下级预测误差作为输入；采用层级化SSM设计，避免位置编码（RoPE）限制；计算复杂度O(L)。

Result: enwiki8上32K tokens比Transformer好11.4%、比Mamba好0.7-1.8%；64K tokens时Mamba和Transformer OOM，Harmonic成功训练；1B参数量级替换TinyLlama注意力层后消除RoPE限制，长序列保持稳定。

Conclusion: Harmonic在长序列建模上显著优于Transformer和Mamba，消除了位置编码长度限制，且计算效率更高，是高效语言建模的有前景方向。

Abstract: We present Harmonic, a hierarchical state space model (SSM) for language modeling. The architecture stacks three recurrent levels at progressively slower timescales; each level receives the prediction error of the level below as input, rather than its raw hidden state. On enwiki8 with equal token budgets, Harmonic outperforms a comparable Transformer (28M params) by +1.4% at 1K tokens, +6.7% at 8K tokens, and +11.4% at 32K tokens (bpt, lower is better). It also outperforms Mamba at every tested length by 0.7--1.8%. At 64K tokens, both Mamba and Transformer run out of memory on an 80GB H100; Harmonic trains successfully, reaching 6.169 bpt. Results replicate on WikiText-103 (H-TF gap +1.7% to +7.2% across 1K--32K). At 1B parameter scale, replacing all attention layers in TinyLlama 1.1B with HarmonicBlock eliminates the RoPE positional encoding limit: the resulting Hallamonic model maintains stable loss across sequence lengths 1K--8K on two independent clean benchmarks (Lambada and fineweb-edu held-out), while TinyLlama degrades catastrophically past its 2K-token RoPE limit (gap: +9.4 bpt at seq=8K on Lambada). Compute is O(L) per forward pass vs. O(L^2) for attention.
  Logs: https://github.com/Omibranch/harmonic-logs.

</details>


<div id='cs.LG'></div>

# cs.LG [[Back]](#toc)

### [3] [Low-power analogue neural networks with trainable nonlinear connections for continuous control](https://arxiv.org/abs/2606.23742)
*Ian T. Vidamour,Fernando Aguirre,Thomas J. Hayward,Matthew O. A. Ellis,Charles Swindells,Alexander McDonnell,Martin Trefzer,Finley Robins,Luca Manneschi,Susan Stepney,Tony Kenyon,Oliver J. Sutton,Jack C. Gartside,Ivan Y. Tyukin,Adnan Mehonic,Eleni Vasilaki*

Main category: cs.LG

TL;DR: 受Kolmogorov-Arnold网络启发，将可训练非线性函数置于物理神经网络的连接上（而非节点上），利用现场可编程模拟阵列上的带通滤波器实现，在平滑连续值目标任务上显著减少节点和连接数，但在分类边界任务上无参数效率优势。约35,000个连接可保真迁移至硬件，专用CMOS实现功耗约30微瓦。


<details>
  <summary>Details</summary>
Motivation: 现有物理神经网络强制非线性器件响应作为标量权重，限制了计算效率。作者希望将可训练非线性置于连接上，使每个物理连接成为可学习的计算单元，从而提升物理神经网络的表达能力。

Method: 借鉴Kolmogorov-Arnold网络架构，在连接上放置可训练非线性函数，使用现场可编程模拟阵列上的模拟带通滤波器实现这些函数。通过memristive器件和CMOS实现验证，并在多种任务上评估（机器人运动学、连续控制、光伏最大功率点跟踪、分类决策边界）。

Result: 在平滑连续值目标任务上，以更少的节点和连接达到或超越多层感知机性能；在分类决策边界任务上无参数效率优势。约35,000个连接可保真迁移至硬件，专用CMOS实现预计功耗约30微瓦。

Conclusion: 将可训练非线性置于连接上而非节点上能为物理神经网络带来任务相关的优势，优势来源于连接上的可训练非线性本身，而非特定器件。平滑物理基函数是性能提升的关键。

Abstract: Physical neural networks promise low-power machine learning by computing directly with analogue device physics, but most architectures force nonlinear device responses to act as scalar weights. Inspired by Kolmogorov-Arnold networks, we place trainable nonlinear functions on the connections, making each physical connection a learnable computational element. Realising these functions as analogue band-pass filters on field-programmable analogue arrays, we find that the benefit is task-dependent and follows from the smoothness of the physical basis: the networks represent smooth, continuously valued targets, including robotic kinematics, continuous control, and photovoltaic maximum-power-point tracking, with far fewer nodes and connections than multilayer perceptrons, but offer no parameter-efficiency advantage on classification-like decision boundaries. Trained networks transfer to hardware across approximately 35,000 connections with quantified fidelity, and a dedicated CMOS implementation is projected to operate at approximately 30 microwatts. A memristive realisation reproduces the same behaviour in simulation, indicating that the advantage comes from placing trainable nonlinearity on connections, rather than from a particular device.

</details>


### [4] [Forget Without Compromise: Nexus Sampling for Streaming KV-Cache Eviction Under Fixed Budgets](https://arxiv.org/abs/2606.23961)
*Duc Duong,Hoang Anh Duy Le,Jianwen Xie,Anshumali Shrivastava,Zhaozhuo Xu*

Main category: cs.LG

TL;DR: 提出Nexus Sampling，一种无需训练的KV缓存驱逐方法，通过Nexus评分（迭代式直接注意力行走，发现桥接token）结合加权蓄水池采样（概率性保留而非确定性top-K选择），在80%驱逐率下性能接近密集注意力，在检索密集型任务上优于top-K基线，缓存内存节省高达10倍。


<details>
  <summary>Details</summary>
Motivation: 现有KV缓存驱逐方法使用每步直接注意力分数+确定性top-K选择，这会导致一旦某个token低于阈值就被永久驱逐，而那些直接注意力无法从噪声中区分出的微妙重要token会被错误地永久删除。

Method: Nexus评分（迭代式直接注意力行走发现桥接token）+ 加权蓄水池采样（以包含概率替代确定性top-K选择）。

Result: 在80% KV缓存驱逐率下，LongBench上匹配密集注意力（1%以内），在检索密集型任务上优于top-K基线，每序列缓存内存减少高达10倍。

Conclusion: Nexus Sampling在理论上证明了在微妙重要token的长期存活率上优于确定性top-K，是一种有效的训练免费KV缓存驱逐方案。

Abstract: Long-context and agentic LLM workloads push the KV cache past any fixed memory budget, forcing the inference stack to permanently evict tokens at every step of a continuous-inference stream. Existing methods all share the same template, a per-step direct-attention score followed by deterministic top-$K$ selection, which converts a single below-cutoff step into an irreversible verdict and permanently erases any subtly important token that direct attention cannot single out from noise. To address this challenge, we propose Nexus Sampling, a training-free eviction method that pairs Nexus scoring, an iterative walk over direct attention that surfaces bridge tokens, with weighted reservoir sampling, which retains tokens with inclusion probability in place of deterministic top-$K$. Theoretically, we show that Nexus Sampling dominates deterministic top-$K$ in long-run survival of subtly important tokens. Empirically, at 80% KV cache eviction, Nexus Sampling matches dense attention within 1% on LongBench while outperforming top-$K$ baselines on retrieval-heavy tasks, with up to 10x smaller per-sequence cache memory.

</details>


### [5] [Learning to Trigger: Reinforcement Learning at the Large Hadron Collider](https://arxiv.org/abs/2606.23993)
*Zixin Ding,Shaghayegh Emami,Giovanna Salvi,Cecilia Tosciri,Abhijith Gandrakota,Jennifer Ngadiuba,Nhan Tran,Christian Herwig,David W. Miller,Yuxin Chen*

Main category: cs.LG

TL;DR: 本文提出使用强化学习（GFPO变体）对大型强子对撞机的高通量事件触发阈值进行在线自适应调优，在模拟和真实CMS数据上实现了更高的信号效率和带宽合规时间比例。


<details>
  <summary>Details</summary>
Motivation: 传统触发菜单是静态且手工调优的，随着探测器条件、堆积和背景构成漂移，性能会变得次优，需要一种自适应调优机制。

Method: 将阈值调优建模为序列决策问题，使用Group-Filtered Policy Optimization (GFPO)的流式变体（GFPO-F, GFPO-FR），智能体摄入实时速率和信号敏感特征流，动态调整触发阈值以最大化信号效率同时将背景率控制在容差带内。

Result: 在模拟流上，H_T触发器和异常检测触发器的合规时间比例分别提升48%和28%，信号效率提升高达2%；在真实CMS数据（Run 283408）上零微调分别获得56%和28%的合规时间改进。

Conclusion: 首次在真实LHC碰撞数据上证明基于RL的触发控制可行，能显著提升触发系统对动态运行条件的自适应能力。

Abstract: High-throughput scientific facilities such as the Large Hadron Collider depend on real-time event filtering (\textit{triggering}) under tight constraints on bandwidth, latency, and storage. In practice, trigger menus are largely static and hand-tuned and can become suboptimal as detector conditions, pileup, and background composition drift over time. We cast online threshold tuning as a sequential decision-making problem: a reinforcement learning agent ingests streaming summaries of recent rates and signal-sensitive features and updates trigger thresholds to maximize signal efficiency while tracking a target background rate within a tolerance band. We adapt Group-Filtered Policy Optimization (GFPO) to streaming control and introduce two variants (GFPO-F, GFPO-FR) that enforce background rate feasibility during training. On a benchmark that emulates realistic collider operation, we study two representative triggers: a total transverse energy ($H_{T}$) trigger sensitive to pileup variation, and an anomaly-detection (AD) trigger based on reconstruction loss for rare or non-standard signatures. On Monte Carlo streams, our agent increases the fraction of in-tolerance time intervals by 48\% ($H_T$) and 28\% (AD), with a cumulative gain of up to 2\% in signal efficiency on those in-tolerance intervals. Transferring from simulation to \emph{real} collision data (CMS Run 283408), the same agent, without fine-tuning, achieves a 56\% ($H_T$) and 28\% (AD) in-tolerance improvement over baselines, with further signal-efficiency gain on both triggers. To our knowledge, this is the \emph{first} demonstration of RL-based trigger control on real Large Hadron Collider collision data. Code is available at https://github.com/Zixind/GFPO\_LHC.

</details>


### [6] [RoPE-Aware Bit Allocation for KV-Cache Quantization](https://arxiv.org/abs/2606.24033)
*Fengfeng Liang,Yuechen Zhang,Jiaya Jia*

Main category: cs.LG

TL;DR: Block-GTQ是一种基于RoPE感知的KV缓存量化位分配器，通过将键缓存量化为块级别的位分配问题，利用能量分数贪婪分配整数位宽，在低比特量化下显著提升模型的长上下文检索、理解和推理能力，并实现了3.24x压缩比和1.34x加速。


<details>
  <summary>Details</summary>
Motivation: 现有低比特KV缓存量化方法通常将每个缓存键视为平坦向量处理，忽略了RoPE下键对未来注意力logit的贡献实际上分解为二维频率块的依赖关系，导致量化误差不均匀。

Method: 提出Block-GTQ，基于TurboQuant-MSE构建，为每层和每个KV头计算每个RoPE块的无标签能量分数，通过边际增益贪心分配整数位宽，实现块级位分配。

Result: 在10个模型的诊断面板上，每层MAE降低32-80%；Llama-3.1-8B-Instruct下K2V2的NIAH平均从70.6提升至97.4，LongBench-EN从36.87提升至53.31；DeepSeek-R1-Distill-Qwen-7B下K3V2得分51.7/37.5接近fp16；Qwen2.5-3B-Instruct上K3V3实现3.24x压缩、1.34x加速，峰值内存从56.31GB降至19.85GB。

Conclusion: Block-GTQ在低比特KV缓存量化上显著优于均匀量化方法，有效保留了RoPE查询-键logits，转化为实际的长上下文性能提升，且具备实际部署可行性。

Abstract: Existing low-bit KV-cache quantizers often treat each cached key as a flat vector. Under RoPE, however, a key's contribution to a future attention logit decomposes into a position-dependent sum over two-dimensional frequency blocks. This makes key-cache quantization a block-wise bit-allocation problem: high-energy RoPE blocks are more sensitive to quantization error and should receive more bits. We introduce Block-GTQ, a RoPE-aware bit allocator for key-cache quantization built on TurboQuant-MSE(TQ-MSE). For each layer and KV head, Block-GTQ computes a label-free energy score for each RoPE block and greedily allocates integer bit widths by marginal gain. Under matched K/V bit budgets, Block-GTQ better preserves RoPE query-key logits on a ten-model diagnostic panel, cutting per-layer MAE by 32-80% at 2 and 3 b/dim K-only quantization and winning all 367/367 layer comparisons against uniform TQ-MSE. These fidelity gains translate to stronger downstream long-context retrieval, understanding, and reasoning. At K2V2 on Llama-3.1-8B-Instruct, Block-GTQ raises the six-task NIAH average from 70.6 to 97.4, and the LongBench-EN average from 36.87 to 53.31. On AIME 2024/2025 with DeepSeek-R1-Distill-Qwen-7B, without an fp16 recent-key buffer, Block-GTQ at K3V2 scores 51.7/37.5, close to fp16's 54.2/37.9, whereas uniform TQ-MSE collapses to 0.0/0.0. We further implement a packed-cache serving path. On a single H800 GPU with Qwen2.5-3B-Instruct, packed K3V3 achieves 3.24x KV-cache compression with fp16-comparable quality, runs 1.34x faster than fp16 FlashAttention2 at 128K context, reduces peak memory from 56.31 GB to 19.85 GB, and remains feasible at 256K and 512K where fp16 OOMs. Code is available at https://github.com/JIA-Lab-research/blockgtq.

</details>


### [7] [FedUP: One-Shot Federated Unlearning via Centroid-Guided Plug-in Filters](https://arxiv.org/abs/2606.24113)
*Feihong Nan,Zhengyi Zhong,Pan Wang,Weidong Bao,Xiongtao Zhang,Quan Wen,Ji Wang*

Main category: cs.LG

TL;DR: FedUP是一个一次性联邦遗忘框架，利用轻量级可插拔过滤器作为"知识漏斗"筛选目标数据，通过冻结原始模型参数并在服务器端使用差分隐私保护类质心样本训练过滤器，无需多轮通信和复杂重训练，实现从分钟级到秒级的遗忘延迟，并具备内在可逆性。


<details>
  <summary>Details</summary>
Motivation: 现有联邦遗忘方法面临非目标知识丢失与高请求延迟之间的两难困境，亟需一种既能高效遗忘又能保持模型性能的解决方案。

Method: 提出FedUP框架：冻结原始模型参数，在服务器端利用差分隐私保护的类质心样本训练轻量级可插拔过滤器作为"知识漏斗"，实现一次性联邦遗忘，无需多轮客户端-服务器通信。

Result: 在多种图像和文本任务上的实验表明，FedUP有效减少了非目标知识丢失，实现了优越的遗忘精度和效率，将延迟从分钟级降至秒级。

Conclusion: FedUP通过可插拔过滤器架构解决了联邦遗忘中知识保留与效率的矛盾，同时具备天然的可逆性（移除过滤器即可恢复遗忘知识）。

Abstract: Federated unlearning (FU) is critical for complying with legal mandates like the right to be forgotten in decentralized systems, yet current methods face a persistent dilemma between non-target knowledge loss and high request latency. To resolve these issues, we propose FedUP, a one-shot federated unlearning framework utilizing lightweight pluggable filters that act as a "knowledge funnel" to screen out target data while preserving original model performance. By freezing original model parameters and training filters at the server side using differentially private (DP)-protected class centroid samples, FedUP bypasses the need for multi-round client-server communication and complex retraining, reducing unlearning latency from minutes to mere seconds. Additionally, the framework's pluggable architecture ensures inherent reversibility, enabling the seamless restoration of forgotten knowledge by simply removing the filters. Extensive experiments on diverse image and text tasks demonstrate that FedUP effectively reduces non-target knowledge loss and achieves superior unlearning precision and efficiency across various scenarios. Code is available at: https://github.com/suows/FedUP-code.

</details>


### [8] [Holistic Data Scheduler for LLM Pre-training via Multi-Objective Reinforcement Learning](https://arxiv.org/abs/2606.24133)
*Chenhao Dang,Jing Ma,Mingjie Liao*

Main category: cs.LG

TL;DR: 本文提出Holistic Data Scheduler (HDS)，一种基于强化学习（SAC算法）的在线数据混合框架，通过多目标奖励函数（数据质量、域间影响、权重范数）动态调整LLM预训练数据配比，在The Pile上以44%更少训练步数达到同等困惑度，MMLU 0-shot提升7.2%。


<details>
  <summary>Details</summary>
Motivation: 现有在线数据混合方法仅依赖单一优化视角，忽略了LLM预训练需要从多个维度动态调整数据配比的复杂性。

Method: 将数据调度建模为连续控制空间上的强化学习问题，使用SAC算法探索高维策略空间；设计包含数据驱动奖励、损失驱动奖励（捕获跨领域影响）和模型驱动奖励（基于权重范数）的多目标综合奖励函数。

Result: 在The Pile上，HDS以44%更少训练迭代达到最优方法最终的验证困惑度；MMLU 0-shot提升7.2%，其他基准测试上也取得一致改进。

Conclusion: 多维度综合奖励函数的在线数据混合方法能显著提升LLM预训练效率和最终模型能力。

Abstract: The composition of training data, governed by the diversity of sources and their mixing strategy, is a cornerstone of Large Language Model (LLM) pre-training. Online Data Mixing (ODM), the technique of adaptively adjusting data mixtures during training, has emerged as a promising direction to improve efficiency. However, existing methods are constrained by their reliance on a singular optimization perspective, which fundamentally overlooks the need for complex LLM pre-training to consider the dynamic data composition from multiple dimensions. To overcome this limitation, we introduce the Holistic Data Scheduler (HDS), a novel online data mixing framework. HDS formulates the data scheduling challenge as a reinforcement learning problem in a continuous control space and leverages the Soft Actor-Critic (SAC) algorithm for its stability and sample efficiency in exploring the high-dimensional policy space. At the core of HDS lies a novel multi-objective, holistic reward function that integrates three critical perspectives: a data-driven reward for quality, a loss-driven reward capturing inter-domain influence, and a model-driven reward based on weight norms. To validate our design and determine its optimal configuration, we conducted systematic experiments on LLMs of various sizes. On The Pile benchmark, HDS reaches the final validation perplexity of the next best method with 44% fewer training iterations. Furthermore, it achieves a 7.2% improvement on the MMLU 0-shot task along with consistent gains on other benchmarks, showcasing its ability to enhance both training efficiency and final model capability.

</details>


### [9] [AsyncOPD: How Stale Can On-Policy Distillation Be?](https://arxiv.org/abs/2606.24143)
*Wonjun Kang,Kevin Galim,Seunghyuk Oh,Minjun Kang,Sanghyun Park,Donghoon Kim,Minjae Lee,Minseo Kim,Rishabh Tiwari,Yuchen Zeng,Hyung Il Koo,Kangwook Lee*

Main category: cs.LG

TL;DR: 本文系统研究了异步在线策略蒸馏（OPD）中的陈旧性问题，分析了KL散度方向对陈旧数据鲁棒性的影响，并提出了一种全异步OPD训练流水线AsyncOPD，在保持可比精度的前提下将训练吞吐量提升了1.6倍至3.8倍。


<details>
  <summary>Details</summary>
Motivation: 在线策略蒸馏（OPD）在大语言模型后训练中日益重要，但其面临与强化学习类似的在线策略系统瓶颈——推理负载下rollout生成可能主导训练时间。异步训练可以缓解这一瓶颈，但会引入陈旧策略数据。现有工作研究了异步RL中的陈旧数据问题，但OPD中的影响尚未被充分探索。

Method: 1) 系统研究了KL方向对陈旧数据的鲁棒性差异（teacher-weighted forward KL更鲁棒，student-weighted reverse KL更脆弱）；2) 评估了异步RL的稳定方法在OPD中的有效性，发现不如简单的OPD专用替代方案（在learner端重新计算reverse-KL信号）；3) 分析了有限教师分数缓存对稀疏和采样reverse-KL OPD估计器的偏差-方差权衡，提出了多样本蒙特卡洛方法；4) 构建并开源了全异步OPD训练流水线AsyncOPD。

Result: AsyncOPD相比严格同步训练，在保持可比精度的前提下，训练吞吐量提升了1.6倍至3.8倍。

Conclusion: 异步OPD训练是可行的，通过合理的估计器设计（如选择恰当的KL方向、使用多样本MC）可以有效缓解陈旧数据带来的问题，显著提升训练效率。

Abstract: On-policy distillation (OPD) trains a student on its own rollouts guided by teacher feedback and is becoming increasingly important for large language model (LLM) post-training. Like reinforcement learning (RL), however, OPD faces an on-policy systems bottleneck, as rollouts can dominate training time for reasoning workloads. Asynchronous training pipelines can alleviate this bottleneck by decoupling rollout generation from learner updates, but doing so introduces stale-policy data. While prior work has studied stale data in asynchronous RL, its effects in OPD remain underexplored. We present the first systematic study of staleness in asynchronous OPD, focusing on a practical setting where teacher feedback is implemented through local KL losses and full-vocabulary teacher logits are too expensive to store or transfer, necessitating finite teacher-score caches. We first show that KL direction changes the stale-data problem: teacher-weighted forward KL is more robust to stale rollouts, whereas student-weighted reverse KL is vulnerable. Second, for this vulnerable reverse-KL case, we study whether methods designed to stabilize asynchronous RL can mitigate OPD staleness. In our experiments, they do not improve over a simpler OPD-specific surrogate: recomputing the reverse-KL signal under the current student at learner time. Third, we analyze how finite teacher-score caches create a bias-variance tradeoff for sparse and sampled reverse-KL OPD estimators. This motivates multi-sample Monte Carlo (MC), which preserves MC correctability while reducing one-sample variance. Finally, we present and open-source AsyncOPD, a fully asynchronous OPD training pipeline built from these estimator choices. Experiments show that AsyncOPD improves training throughput by $1.6\times$ to $3.8\times$ over strict synchronous training while reaching comparable accuracy.

</details>


### [10] [Lightweight Transformer Models for On-Device Fault Detection: A Benchmark Study on Resource-Constrained Deployment](https://arxiv.org/abs/2606.24173)
*Disha Patel*

Main category: cs.LG

TL;DR: 本文对比了传统机器学习方法（随机森林、XGBoost、SVM、逻辑回归）与轻量级Transformer架构（DistilBERT、TinyBERT、MobileBERT）在设备端故障检测任务上的性能、模型大小和延迟，并在三个公共数据集上进行了评估。结果显示，在数据分离良好的场景下，轻量级Transformer可媲美传统ML但代价是体积和延迟大幅增加；INT8量化可减小体积25%；提出的自适应推理管线在保持高F1的同时显著降低延迟。


<details>
  <summary>Details</summary>
Motivation: 设备端故障检测需要在不依赖云端的情况下实时诊断，但资源受限的硬件上部署机器学习模型需要在准确率、延迟和模型大小之间精心权衡。现有研究缺乏对传统ML与轻量级Transformer在此场景下的系统性对比。

Method: 使用三个公共数据集（NASA C-MAPSS、SECOM、UCI AI4I 2020），对比传统ML方法（随机森林、XGBoost、SVM、逻辑回归）与轻量级Transformer（DistilBERT、TinyBERT-6L、TinyBERT-4L、MobileBERT）的F1分数、AUC、模型大小和CPU推理延迟；并评估INT8动态量化和两阶段自适应推理管线。

Result: 在C-MAPSS上，轻量级Transformer达到87.8% F1但模型大小是传统ML的100倍、延迟是9000倍；TinyBERT-4L是最适合部署的Transformer（55MB, 18ms）；INT8量化减少25%体积并保持86.9% F1；自适应管线达到87.6% F1和19.5ms平均延迟；在严重不平衡数据集上所有方法表现均不佳。

Conclusion: 传统ML方法在资源受限的设备端故障检测中仍具优势；轻量级Transformer在数据分离良好时可匹配传统ML性能但代价高昂；极端类别不均衡是当前方法尚未解决的根本性挑战。

Abstract: On-device fault detection enables real-time diagnostics without cloud dependency, but deploying machine learning models on resource-constrained hardware demands careful tradeoffs between accuracy, latency, and model size. We present a benchmark comparing traditional ML methods (Random Forest, XGBoost, SVM, Logistic Regression) against lightweight transformer architectures (DistilBERT, TinyBERT-6L, TinyBERT-4L, MobileBERT) for binary fault detection across three public datasets: NASA C-MAPSS turbofan degradation, SECOM semiconductor manufacturing, and UCI AI4I 2020 predictive maintenance. We evaluate classification performance (F1-score, AUC), model size, and CPU inference latency, and further assess INT8 dynamic quantization and a two-stage adaptive inference pipeline. Our results reveal that on well-separated sensor data (C-MAPSS), lightweight transformers match traditional ML at 87.8% F1 but at 100x the model size and 9000x the latency. TinyBERT-4L emerges as the most deployment-friendly transformer at 55 MB and 18 ms CPU latency. INT8 quantization reduces size by 25% while preserving 86.9% F1. Our adaptive pipeline, routing 97.9% of predictions through a quantized triage model and only 2.1% to a larger expert, achieves 87.6% F1 at 19.5 ms average latency. On severely imbalanced datasets (SECOM, UCI-PM), both traditional and transformer methods struggle significantly, highlighting fundamental limitations of current approaches for extreme class imbalance in fault detection. All code is publicly available.

</details>


### [11] [FlowPipe: LLM-Enhanced Conditional Generative Flow Networks for Data Preparation Pipeline Construction](https://arxiv.org/abs/2606.24679)
*Kunyu Ni,Lei Cao,Jie He,Xiaotong Zhang,Jianfeng Jin,Junyu Dong,Yanwei Yu*

Main category: cs.LG

TL;DR: FlowPipe 提出了一种基于条件生成流网络（C-GFlowNets）的统一框架，将数据准备管道构建公式化为有向无环图上的条件概率流生成。它通过轨迹平衡目标、深度语义调制（FiLM）和失败感知机制，解决了现有方法在长期信用分配、数据集上下文注入和无效状态探索方面的局限性，在74个真实数据集上平均提升准确率11.96%，训练收敛速度提升12.5倍。


<details>
  <summary>Details</summary>
Motivation: 现有SOTA的Multi-DQN方法存在三个关键限制：解耦的值估计器削弱了长期信用分配、数据集上下文仅被弱注入策略、在稀疏搜索空间中探索效率低且存在大量无效状态。因此需要一种新方法来解决这些问题。

Method: FlowPipe 使用条件生成流网络（C-GFlowNets）结合轨迹平衡目标，将终端验证奖励与早期管道决策连接；引入深度语义调制（通过FiLM层）让LLM推导的逻辑先验根据数据集语义调节策略的内部激活；将失败感知机制融入流目标，避免无效状态并集中搜索高潜力区域。

Result: 在两个基准套件74个真实数据集上，FlowPipe相比SOTA基线平均准确率提升11.96%，训练收敛速度提升12.5倍。

Conclusion: FlowPipe通过统一的条件概率流生成框架有效解决了数据准备管道自动构建中的关键挑战，显著优于现有方法。

Abstract: Data preparation pipelines improve data quality in machine learning by transforming raw tables into learning-ready data through sequential cleaning and feature transformation operators. However, automatically constructing such pipelines is computationally difficult because operator sequences are combinatorial and end-to-end evaluation is expensive. Existing state-of-the-art (SOTA) Multi-DQN methods still face three key limitations: decoupled value estimators weaken long-horizon credit assignment, dataset context is only weakly injected into the policy, and exploration is inefficient in a sparse search space with many invalid states. To address these issues, we propose FlowPipe, a unified framework that formulates pipeline synthesis as conditional probabilistic flow generation over a directed acyclic graph. FlowPipe uses Conditional Generative Flow Networks (C-GFlowNets) with a Trajectory Balance objective to connect terminal validation rewards with early pipeline decisions. It further introduces Deep Semantic Modulation through Feature-wise Linear Modulation (FiLM), allowing LLM-derived logical priors to condition the policy's internal activations according to dataset semantics. In addition, FlowPipe incorporates failure awareness into the flow objective to avoid invalid states and concentrate search on high-potential regions. Experiments on two benchmark suites with 74 real-world datasets show that FlowPipe outperforms SOTA baselines, improving accuracy by 11.96% on average and achieving 12.5x faster training convergence. Source code is available at https://github.com/KunyuNi/FlowPipe.

</details>


<div id='cs.DC'></div>

# cs.DC [[Back]](#toc)

### [12] [CrossPool: Efficient Multi-LLM Serving for Cold MoE Models through KV-Cache and Weight Disaggregation](https://arxiv.org/abs/2606.24506)
*Zhuoren Ye,Tianyu Wo,Dinghao Xue,Mingming Zhang,Yuchen Teng,Chunming Hu,Renyu Yang*

Main category: cs.DC

TL;DR: CrossPool 是面向冷 MoE 模型的推理引擎，将 FFN 权重与 KV-cache 分离到两个 GPU 内存池，通过共享 KV-cache 池、层间流水线调度和持久化内核等技术，提升 GPU 内存利用率并支持突发长上下文请求，相比最先进的基于 kvcached 的多 LLM 服务系统，P99 TBT 降低高达 10.4 倍。


<details>
  <summary>Details</summary>
Motivation: 现有 LLM 服务系统中稀疏 MoE 模型多为冷模型（请求稀疏），但 GPU 内存中权重稳定而 KV-cache 瞬态变化，预留最坏情况 KV 容量浪费内存；且权重与 KV-cache 共存于同一 GPU 内存池时，静态权重与动态 KV-cache 竞争资源，冷模型低并发场景下 KV 容量利用率低，长上下文支持弱。

Method: 提出 CrossPool，将 FFN 权重和 KV-cache 分离为两个独立 GPU 内存池：权重池合并多个冷模型的 FFN 权重，KV-cache 池动态服务活跃请求并保持注意力局部性；结合 KV-cache 规划器与虚拟化、层间流水线调度器（隐藏隐状态传输）以及持久化内核与控制降级（减少 CPU-GPU 控制开销）。

Result: 相比最先进的基于 kvcached 的多 LLM 服务系统，P99 TBT（time-between-tokens）降低高达 10.4 倍。

Conclusion: 通过高效的 GPU 内存池化，CrossPool 能够支撑突发的长上下文请求，显著提升冷 MoE 模型的服务性能。

Abstract: Emerging LLM services increasingly host many sparse MoE models, yet most models receive sparse requests and remain cold. This creates a GPU memory problem: model weights are stable and model-determined, while KV-cache is transient and demand-determined. Because cold models rarely reach peak KV-cache demand at the same time, reserving worst-case KV capacity per model wastes memory; a shared KV-cache pool can instead provision aggregate active demand. However, KV-cache sharing is not sufficient when weights and KV-cache remain in a monolithic GPU memory pool. Static weights compete with dynamic KV-cache, and KV-head-limited attention under cold, low-concurrency traffic exposes only a fraction of replicated KV capacity, leading to low GPU memory utilization and weak long-context support. We present CrossPool, a serving engine for cold MoE models that separates FFN weights and KV-cache into two GPU memory pools: a weights pool that consolidates FFN weights across cold models, and a KV-cache pool that dynamically serves active requests while keeping attention local to KV-cache. CrossPool combines a KV-cache planner and virtualizer, a layer-wise pipeline scheduler that hides hidden-state transfers, and persistent kernels with control lowering to reduce CPU-GPU control overhead. With efficient GPU memory pooling, CrossPool underpins bursty long-context requests and outperforms the state-of-the-art kvcached-based multi-LLM serving system, reducing P99 TBT by up to $10.4\times$.

</details>


<div id='cs.SE'></div>

# cs.SE [[Back]](#toc)

### [13] [Fifty Years of Specification Completeness: What Aviation Certification Tells AI Governance About Epoch Limits, Proof Surfaces, and the Structural Gap](https://arxiv.org/abs/2606.25120)
*Christo Zietsman*

Main category: cs.SE

TL;DR: 本文借鉴航空软件认证（DO-178C/DO-330）的结构性要求（治理-证据追溯、上下文边界有效性、客观证据架构），指出当前AI治理文档（系统提示、AGENTS.md、治理政策等）缺乏这些结构性属性，并提出了PromptQ七原则框架以在文档层面操作化这些要求。


<details>
  <summary>Details</summary>
Motivation: 现有AI治理框架未将航空领域已验证三十年的结构性要求（结构化治理链接、上下文约束有效性、客观证据架构）作为单个治理文档的内在属性，导致AI治理文档质量缺乏保障。

Method: 将DO-178C的追溯架构、DO-330的重新认证触发机制和DO-178C的客观证据要求映射为三个结构性发现：治理文档有效性的时期限制、证明表面作为重新验证反馈机制、以及AI治理工具中结构性完备要求的缺失。通过PromptQ七原则框架在治理文档层操作化这些要求。

Result: 指出航空的结构性要求在系统层因AI非确定性而失效，但在文档层仍然可迁移；实证研究（arXiv:2604.21090）发现37%的AI治理文档低于结构性质量阈值。

Conclusion: 航空认证的结构性要求可迁移至AI治理文档层面，PromptQ框架为操作化这些要求提供了可行方案。

Abstract: Aviation software certification has operationalised three structural requirements for governed software systems since 1992: structured governance linkage between governing specifications and operational evidence, context-bounded validity that triggers revalidation when operational context changes, and an objective evidence architecture that defines what proof means and what makes it sufficient. These requirements appear in DO-178C and DO-330 and are enforced through FAA and EASA certification.
  No existing framework requires these structural properties as intrinsic properties of individual AI governance documents. A system prompt, an AGENTS.md file, a governance policy, or a task envelope can be deployed without satisfying any of the three requirements aviation has enforced for three decades. Aviation is the most technically rigorous instance: its standard-setting bodies have acknowledged that their frameworks break down for AI systems, yet none requires these properties of individual governance documents.
  Aviation's structural requirements break down at the system level because AI systems are non-deterministic, but remain transferable at the document level: the governance artifact is a static artifact whose structural properties can be evaluated independently of the stochastic system it governs.
  The paper maps DO-178C's traceability architecture, DO-330's requalification triggers, and DO-178C's objective evidence requirements onto three structural findings: epoch limits on governance document validity, proof surfaces as the revalidation feedback mechanism, and the absence of structural completeness requirements in AI governance instruments. An empirical companion (arXiv:2604.21090) found that 37% of AI governance documents fall below the structural quality threshold. PromptQ's seven-principle framework operationalises these requirements at the governance document layer.

</details>


### [14] [LibEvoBench: Probing Temporal Knowledge Stratification in Code Generation Models](https://arxiv.org/abs/2606.25402)
*Daniele Cipollone,Sergey Titov,Maliheh Izadi,Egor Bogomolov,Arie van Deursen*

Main category: cs.SE

TL;DR: 本文提出LibEvoBench基准测试和SEUS指标，评估LLM在多版本Python库API上的版本感知能力，发现当前模型存在"版本无感"问题：对演变中的API性能下降，而指定版本号无帮助，相关文档能显著提升准确率。


<details>
  <summary>Details</summary>
Motivation: 大型软件项目常依赖旧版本库，API不断演变，LLM需要掌握多个版本的API知识，但当前训练语料时间混杂且缺乏版本感知机制，导致模型产生时代错乱的API调用错误。

Method: 构建LibEvoBench多任务基准测试（覆盖多个版本的流行Python库），提出SEUS（Software Evolution Understanding Score）指标衡量模型对API演变的推理一致性，系统评估多种SOTA LLM在不同实验条件下的表现。

Result: SOTA模型对演变中的API性能显著下降，对稳定API则保持一致；单纯指定目标版本号无帮助，而提供相关文档可大幅提升准确率。

Conclusion: 当前LLM存在系统性的版本无感缺陷，需要新的训练范式来支持时间锚定的代码知识，为代码生成的时序推理研究指明方向。

Abstract: Large software projects often depend on older versions of libraries, even as APIs continue to evolve across releases. This creates a challenge for LLMs: they must maintain knowledge of multiple API versions, not merely the latest or most common one. However, current LLMs are trained on temporally mixed corpora and lack explicit mechanisms for such version-specific reasoning, leading to anachronistic errors - calling APIs as they exist in a different library version. To systematically evaluate this phenomenon, we introduce LibEvoBench, a multi-task benchmark spanning multiple versions of widely used Python libraries, along with a new metric, the Software Evolution Understanding Score (SEUS), to measure models' consistency when working with evolving APIs. Our results show that state-of-the-art models are largely version-oblivious: performance degrades for evolving APIs, while for stable APIs it remains the same across versions. Moreover, simply specifying the target version provides no benefit, while relevant documentation significantly boosts models' accuracy. These findings highlight a systematic limitation of current training paradigms and motivate new approaches for temporally grounded knowledge in code generation.

</details>


### [15] [The Web4 Agent Economy: A Large-Scale Empirical Study of the Landscape, Challenges, and Opportunities](https://arxiv.org/abs/2606.25876)
*Yuhan Jin,Shuohan Wu,Chong Chen,Lingfeng Bao,Xiaohu Yang,Jiachi Chen*

Main category: cs.SE

TL;DR: 本文对Web4（自主AI代理作为经济主体）生态系统进行了首次大规模实证研究，分析了99,448个跨链身份注册、317,596,323笔交易日志、341个MCP项目源码和349个GitHub issues，揭示了机器对机器支付经济高度活跃但基础设施（身份认证、跨环境操作、支付互操作性）不成熟的核心矛盾。


<details>
  <summary>Details</summary>
Motivation: Web4正从概念走向现实，自主代理可持有加密钱包、执行链上交易、支付外部API调用，相关协议（MCP、x402、EIP-8004）受到业界关注，但实际部署状况、工程挑战和社区应对策略缺乏系统性的实证研究。

Method: 大规模多源数据实证分析：跨链身份注册数据（99,448条）、交易日志（3.17亿+条）、341个MCP项目源码、349个经过筛选的GitHub issues，覆盖代理部署使用、工程挑战、社区响应三个维度。

Result: 自主代理已形成高度活跃的机器对机器支付经济，每日处理数百万笔交易；但增长建立在脆弱的基础设施之上，身份认证/授权实践、跨环境操作和支付互操作性是核心瓶颈；社区响应分布不均，支付互操作性是最持久未解决的瓶颈。

Conclusion: Web4代理经济的快速增长与其底层基础设施的脆弱性之间存在严重差距，需要建设更安全、更成熟的Web4代理生态系统。

Abstract: The Internet is transitioning from Web3 toward Web4, where autonomous agents serve as independent economic actors. These agents can now hold crypto wallets, execute on-chain trades, and pay for external API calls. This transition calls for a new infrastructure stack capable of supporting key agent operations, including agent-to-tool interaction, agent-to-agent payments, and verifiable agent identity, represented by emerging protocols such as the Model Context Protocol, x402, and EIP-8004. Despite growing industrial interest in these protocols, the real-world Web4 agent ecosystem remains largely underexplored. To bridge this gap, we conduct the first large-scale empirical study of the Web4 ecosystem. Specifically, our study targets three interconnected questions: how Web4 agents are deployed and used in practice; what engineering challenges developers face when building Web4 agents; how current project communities respond to these challenges. To answer these questions, we analyze 99,448 multi-chain identity registrations, 317,596,323 transaction logs, the source code of 341 MCP projects, and 349 filtered GitHub issues. Our findings reveal that autonomous agents have established a highly active machine-to-machine payment economy, processing millions of daily transactions. However, this growth is built on immature infrastructure, including identity/authorization practice, cross-environment operation, and payment interoperability. Our follow-up analysis shows that community responses are visible but unevenly distributed across repositories, and payment interoperability remains the most persistent unresolved bottleneck. Overall, this study reveals a critical gap between the rapid growth of the Web4 agent economy and its fragile underlying infrastructure, highlighting future directions for building a more secure Web4 agent ecosystem.

</details>


### [16] [The Dependency Black Hole](https://arxiv.org/abs/2606.25949)
*Ehsan Zabardast,Bhuwan Paudel,Javier Gonzalez-Huerta*

Main category: cs.SE

TL;DR: 对267个微服务的工业案例研究发现依赖网络存在类似黑洞的宏观结构（密集核心、过渡区域、外围区域），提出了依赖黑洞理论来解释依赖集中如何产生和演化。


<details>
  <summary>Details</summary>
Motivation: 大规模微服务系统中存在严重的依赖集中问题，但缺乏系统的理论框架来解释这种现象的形成和演化机制。

Method: 对包含267个微服务的产品进行工业案例研究，三角化分析编译时、运行时和任务依赖三种依赖信号，并与从业人员迭代验证解释。

Result: 发现了依赖网络中的黑洞状宏观结构：密集核心（依赖磁铁）、过渡区域（逐渐缠绕的服务）、外围区域（轻连接服务）。

Conclusion: 提出了依赖黑洞理论（奇点、事件视界、吸积盘映射），为理解依赖增长、识别风险服务和治理干预提供了解释性框架。

Abstract: Microservice architectures promise independent evolution through loose coupling, yet large systems often exhibit strong dependency concentration around a small set of services. In an exploratory industrial case study of a product composed of 267 microservices, we triangulated multiple dependency signals -- compile-time, run-time, and task dependencies -- and iteratively validated our interpretations with practitioners. We observed a recurring macro-structure in the dependency network that resembles a black hole: a dense core of dependency magnets, a transitional region of services increasingly entangled with the core, and an outer region of lightly connected services. Based on these observations, we propose the dependency black hole theory, mapping the network to the black hole anatomy of a singularity, an event horizon, and an accretion disk, and formulating three hypotheses about how dependency concentration emerges and evolves at scale. The theory provides an explanatory lens for reasoning about dependency growth, identifying services at risk of becoming dependency magnets, and motivating governance interventions. We outline practical implications and directions for longitudinal and multi-case validation.

</details>


### [17] [EnerInfer: Energy-Aware On-Device LLM Inference](https://arxiv.org/abs/2606.23001)
*Bohua Zou,Nian Liu,Binqi Sun,Matteo Mascherin,Debayan Roy,Yutao Liu,Yu Peng,Ning Jia,Haibo Chen*

Main category: cs.SE

TL;DR: EnerInfer是一个面向端侧LLM推理的框架，通过利用NPU/内存频率的配置松弛，在不牺牲用户体验的前提下实现能效优化和热管理，采用无需逐模型 profiling 的跨模型预测与在线反馈机制。


<details>
  <summary>Details</summary>
Motivation: 现有端侧LLM推理系统主要优化解码速度，默认认为越快越好，但忽略了能效和散热瓶颈。实际上存在可被利用的配置松弛空间——适度降低NPU和内存频率可显著提升能效并减少发热，同时保持用户体验质量。

Method: EnerInfer采用去聚合的、模型结构感知的预测与排序驱动的在线反馈机制。它跨NPU/DDR频率设置预测未见过LLM的吞吐和功耗，在运行时干扰下选择满足QoE的高效配置，并使用轻量有限时域热预测在能效优化与热约束推理间动态切换。

Result: 在真实LLM上，EnerInfer在手机上提升能效高达65%，在笔记本上提升12%，在开发板上提升24%，且未违反QoE约束。

Conclusion: 端侧LLM推理存在可被利用的配置松弛，EnerInfer证明了通过模型结构感知的跨模型预测与在线反馈，可以在无需逐模型profiling的情况下实现显著的能效提升和热管理。

Abstract: On-device LLM inference is increasingly attractive for privacy-preserving, reliable, and cost-effective deployment, yet its energy and thermal costs remain a critical bottleneck. Existing systems primarily optimize for decoding speed, implicitly assuming that faster execution is always preferable. We show instead that on-device LLM inference often has exploitable configuration slack: modestly lowering NPU and memory frequencies preserves quality of experience (QoE) while substantially improving energy efficiency and reducing heat.
  Realizing this opportunity in production is challenging. The most energy-efficient NPU/DDR setting varies with the model, inference engine, platform, and runtime conditions, with no stable ranking across configurations. Commercial devices further lack component-level power sensing, and shell temperature evolves with request arrivals, response lengths, and thermal history. To address these challenges, we propose EnerInfer, the first on-device LLM inference framework that jointly manages energy efficiency, throughput, and thermal comfort for LLM workloads. EnerInfer replaces per-model profiling and sensor-heavy control with disaggregated, model-structure-aware prediction and ranking-driven online feedback. It predicts throughput and power for unseen LLMs across NPU/DDR frequency settings, selects QoE-satisfying efficient configurations under runtime interference, and uses lightweight limited-horizon thermal prediction to dynamically switch between energy-optimized and thermally constrained inference. Evaluations on real-world LLMs show that EnerInfer improves energy efficiency by up to 65%, 12%, and 24% on phones, a laptop, and a development board, respectively, without QoE violation.

</details>


### [18] [Toward Self-Evolution-Ready Workflow Harnesses: A Reversible Migration Path and Convertibility Taxonomy for Expert LLM Pipelines](https://arxiv.org/abs/2606.24598)
*Yimo Lin,Zhen Zhang,Yibin Li*

Main category: cs.SE

TL;DR: 本文提出了一种基于Strangler-Fig模式的逆向可逆迁移路径，将遗留的"LLM+脚本"工作流重构为可组合、类型化、可审计的阶段，并通过三层可转换性分类法（A/B/C）诊断工作流就绪度并路由执行。


<details>
  <summary>Details</summary>
Motivation: 现有的专家验证的"LLM+脚本"工作流虽然有价值，但它们是静态的，无法根据反馈调整执行。现有的Agent研究主要针对全新构建的Agent和合成基准，忽略了遗留工作流的迁移问题。

Method: 提出了一种可逆的Strangler-Fig迁移路径，将遗留工作流重构为可组合、类型化、可审计的阶段。核心是一个三层可转换性分类法（A/B/C），作为系统harness中的路由阶段实现，用于诊断工作流的就绪度并进行路由。

Result: 该框架能够诊断工作流的就绪度并根据分类结果进行路由，实现了从静态工作流向自适应Agent的渐进式迁移。

Conclusion: 本文填补了遗留工作流向Agent系统迁移的空白，提供了一条实用的渐进式迁移路径。

Abstract: While expert-validated "LLM + script" workflows deliver significant value, they remain static: they encode hard-won domain knowledge yet fail to adapt execution based on feedback. Existing agent research predominantly targets greenfield agents and synthetic benchmarks, leaving the migration of active legacy workflows unresolved. To bridge this gap, we present a reversible, Strangler-Fig migration path that refactors legacy workflows into composable, typed, and auditable stages. Central to this framework is a three-tier convertibility taxonomy (A/B/C), implemented as a routing stage within the system harness, which diagnoses a workflow's readiness and routes it accordingly.

</details>
