<div id=toc></div>

# Table of Contents

- [cs.CL](#cs.CL) [Total: 10]
- [cs.LG](#cs.LG) [Total: 15]
- [cs.AI](#cs.AI) [Total: 10]
- [cs.DC](#cs.DC) [Total: 10]
- [cs.SE](#cs.SE) [Total: 3]


<div id='cs.CL'></div>

# cs.CL [[Back]](#toc)

### [1] [Scaling Enterprise Agent Routing: Degradation, Diagnosis, and Recovery](https://arxiv.org/abs/2606.17519)
*Kellen Gillespie,Robyn Perry*

Main category: cs.CL

TL;DR: 研究生产级LLM助手在工具目录规模扩大时的路由准确率退化问题，发现路由F1随代理数量增加下降16-23个百分点，通过基于嵌入的候选筛选可恢复10-17个百分点的性能。


<details>
  <summary>Details</summary>
Motivation: LLM助手需将用户请求路由到不断增长的专用工具库，但路由准确率随工具目录规模扩大而下降的问题尚未被系统研究。

Method: 在包含110个代理、584个工具的企业生产助手上，评估三种前沿模型（10-110个代理规模）的单步路由性能；进行oracle分析分解退化原因；使用基于嵌入的候选筛选方法恢复性能；并通过1435条人工标注的生产数据进行验证。

Result: 路由F1在需求不明确的请求上下降16-23个百分点；退化分解为检索差距和混淆差距；嵌入候选筛选可恢复10-11pp F1；生产实验验证恢复效果为10-17pp，但绝对性能低10-15pp。

Conclusion: 工具目录规模扩大会导致LLM路由性能显著退化，而基于嵌入的候选筛选是一种有效的缓解方法。

Abstract: Production LLM assistants route user requests to growing libraries of specialized tools, but how does routing accuracy degrade as the catalog scales? We study single-step routing on a 110-agent, 584-tool catalog from a deployed enterprise productivity assistant, evaluating three frontier models from 10 to 110 agents. Routing F1 on under-specified requests drops 16--23 percentage points across models. An oracle analysis decomposes the degradation into a \emph{retrieval} gap (the model cannot surface the right tool) and a \emph{confusion} gap (even with perfect retrieval, the oracle ceiling drops 10pp). Embedding-based shortlisting recovers +10--11pp F1 at full scale across all three models and two providers. A production annotation study (1,435 human-labeled utterances, three annotators) confirms the recovery on real traffic at +10--17pp despite 10--15pp lower absolute performance.

</details>


### [2] [The Benchmark Illusion: Pruned LLMs Can Pass Multiple Choice but Fail to Answer](https://arxiv.org/abs/2606.17609)
*Rui Wen,Lu Sun,Jiayang Liu,Zesheng Xu,Tianshuo Cong,Zheng Li*

Main category: cs.CL

TL;DR: 剪枝后的大语言模型在多项选择评估中表现良好，但在开放生成任务中经常失败。研究发现这是"基准测试幻觉"——正确答案并未被擦除，而是被降级了，通过束搜索、采样或少样本示例可以恢复。多项选择基准可能高估了压缩模型的实际可用性。


<details>
  <summary>Details</summary>
Motivation: 压缩大语言模型（剪枝）可以降低内存和推理成本，但标准基准测试可能无法捕捉到压缩引入的失败模式。作者想探究剪枝后模型在多项选择和开放生成任务之间的表现差异及其根本原因。

Method: 使用多语言问答任务，在剪枝前后追踪相同问题。比较贪婪开放生成与多项选择评分下的表现差异，并分析错误类型（答案被擦除 vs 被降级）。

Result: 在高稀疏度剪枝下（尤其是Wanda方法），模型在贪婪开放生成中经常失败，但仍能在多项选择评分中选择正确答案。这种"仅识别性错误"中，答案通常未被擦除，只是被降级——通过束搜索、采样或一个上下文示例即可恢复。

Conclusion: 多项选择基准测试可能高估了压缩LLM的可用性，造成评估盲区。压缩模型应该通过实际生成能力来测试，而不仅仅是识别能力。

Abstract: Compressing large language models reduces memory use and inference cost, but it can also create failures that standard benchmarks miss. A pruned model may still perform well on multiple-choice evaluations, yet fail to answer the same question in open generation. We ask what pruning changes: does it erase the correct answer, or does it make the answer harder to produce as the top output?
  We study this question with multilingual question answering, tracking the same questions before and after pruning. We find a benchmark illusion. Under high-sparsity pruning, especially Wanda, models often fail in greedy open generation while still selecting the correct answer under multiple-choice scoring. In these recognition-only errors, the answer is usually not gone, but demoted: it often reappears with beam search, sampling, or one in-context example. Overall, multiple-choice benchmarks can overstate the usability of compressed LLMs, creating an evaluation blind spot. Compressed models should be tested on what they can produce, not only on what they can recognize.

</details>


### [3] [From Trainee to Trainer: LLM-Designed Training Environment for RL with Multi-Agent Reasoning](https://arxiv.org/abs/2606.17682)
*Chao Chen,Chengzu Li,Zhiwei Li,Yinhong Liu,Zhijiang Guo*

Main category: cs.CL

TL;DR: 提出LLM-as-Environment-Engineer框架，让策略模型自动分析失败轨迹并修改下一阶段训练环境配置；在MAPF-FrozenLake测试平台上，使用Qwen3-4B作为骨干网络达到最优性能，并发现当前RL检查点比原始基础模型更适合担任环境工程师角色。


<details>
  <summary>Details</summary>
Motivation: RL训练LLM的流程中，不同阶段间的环境配置依赖人工重新设计，从业者需要启发式地推断哪种配置能最好地改进当前策略，缺乏自动化手段。

Method: 提出LLM-as-Environment-Engineer框架，让当前策略模型分析失败轨迹和上下文信息，生成下一阶段训练环境配置的修改建议；构建MAPF-FrozenLake可控测试平台，环境生成器暴露多维环境配置。

Result: Qwen3-4B作为骨干网络在基准测试中取得最强聚合性能，超越更大规模专有LLM（如GPT、Gemini）和固定环境训练基线；成功的环境更新依赖于失败证据并保留已工作的配置。

Conclusion: 当前RL检查点比原始基础模型更适合作为环境工程师，说明策略学习提升了模型诊断自身弱点的能力。

Abstract: Reinforcement learning pipelines for Large Language Model (LLM) training often rely on manually redesigned environments between stages, requiring practitioners to heuristically infer which configuration will best improve the current policy. To automate this process, we propose the LLM-as-Environment-Engineer framework in which the current policy model analyzes failure trajectories together with contextual information and proposes modifications to the next-stage training environment configuration. We also introduce MAPF-FrozenLake, a controllable testbed whose generator exposes multi-dimensional environment configurations, making it suitable for studying and benchmarking environment redesign. On this testbed, we condition the environment engineer on structured summaries of policy behavior, failure cases, and environment statistics, from which it produces the configuration for the next training stage. With Qwen3-4B as the backbone, our framework achieves the strongest aggregate performance on our benchmarks, outperforming larger proprietary LLMs (e.g., GPT, Gemini) and fixed-environment training baselines. We further analyze which forms of context are most effective, finding that successful environment updates rely on failure evidence and preserve configurations that already work. Interestingly, the current RL checkpoint serves as a better environment engineer than the original base model, suggesting that policy learning improves the model's ability to diagnose its remaining weaknesses.

</details>


### [4] [SuCo: Sufficiency-guided Continuous Adaptive Reasoning](https://arxiv.org/abs/2606.17687)
*Jiahao Wang,Bingyu Liang,Chenhao Hu,Longhui Zhang,Xuebo Liu,Min zhang,Jing Li,Xuelong Li*

Main category: cs.CL

TL;DR: 本文提出最小充分思维链（MSC）概念及其训练框架SuCo，通过自适应推理控制减少大推理模型过度冗长的思维链，在提升准确率的同时显著降低推理计算成本。


<details>
  <summary>Details</summary>
Motivation: 大推理模型（LRMs）在处理简单问题时也会生成极其冗长的思维链（CoT），导致计算成本膨胀。现有方法依赖离散推理模式或固定预算层级，缺乏判断推理何时充分的通用标准。

Method: 提出SuCo两阶段训练框架：阶段1（MFT）基于问题自适应充分性阈值构建MSC数据，微调模型内化简洁充分的推理模式；阶段2（SAPO）通过强化学习结合动态复杂度追踪和充分性感知奖励（惩罚过度思考与思考不足），优化推理策略。

Result: 在数学、代码和科学基准上的大量实验表明，SuCo在准确率和推理效率上均取得一致性提升。

Conclusion: MSC定义和SuCo框架能够有效实现自适应推理长度控制，在不过度牺牲准确率的前提下大幅降低推理开销。

Abstract: Despite remarkable performance on complex tasks, Large Reasoning Models (LRMs) often generate excessively long Chain-of-Thoughts (CoT), inflating computational costs even for simple queries. Existing efforts to mitigate this inefficiency typically rely on discrete reasoning modes or fixed budget tiers, lacking a principled criterion of when reasoning is sufficient. In this work, we introduce Minimal Sufficient CoT (MSC), defined as the shortest prefix of a CoT trajectory which is adequate for producing the correct answer. We empirically show that MSC not only reduces reasoning tokens, but also improves accuracy across difficulty levels. Building on MSC, we propose Sufficiency-guided Continuous Adaptive Reasoning (SuCo), a two-stage training framework for autonomous reasoning control along a continuous spectrum. In stage 1, MSC-Aligned Fine-Tuning (MFT) constructs MSC data using problem-adaptive sufficiency thresholds that naturally scale with question difficulty, then fine-tunes the model to internalize concise yet sufficient reasoning patterns. In stage 2, Sufficiency-Aware Policy Optimization (SAPO) further optimizes the model through reinforcement learning with dynamic complexity tracking and sufficiency-aware rewards that penalize both over- and under-thinking. Extensive experiments across mathematics, code, and science benchmarks show that SuCo consistently achieves improvements in both accuracy and reasoning efficiency.

</details>


### [5] [VoidPadding: Let [VOID] Handle Padding in Masked Diffusion Language Models so that [EOS] Can Focus on Semantic Termination](https://arxiv.org/abs/2606.17999)
*Chunyu Liu,Zhengyang Fan,Kaisen Yang,Alex Lamb*

Main category: cs.CL

TL;DR: 本文提出VoidPadding方法，将掩码扩散语言模型（MDLM）中[EOS]的双重角色（语义终止符与填充符）解耦，引入[VOID]作为填充符，让[EOS]仅负责终止信号。该方法在推理时支持提前终止和自适应响应画布扩展，显著提升数学推理和代码生成性能，同时大幅减少解码步数。


<details>
  <summary>Details</summary>
Motivation: 现有MDLM在指令微调时沿用了自回归模型的惯例，使用重复的[EOS]标记进行填充，导致[EOS]同时承担语义终止符和填充符的双重角色，这种角色混杂是大块解码下[EOS]溢出问题的根本原因。

Method: 提出VoidPadding方法，引入新的[VOID]标记专门用于填充，将[EOS]的角色严格限定为语义终止符。推理时，模型学习到的[EOS]信号用于提前停止，[VOID]信号用于指导自适应响应画布扩展。

Result: 在Dream-7B-Instruct上，VoidPadding在数学推理和代码生成基准的块大小平均四项任务均值上，比原始模型提升+17.84分，比RainbowPadding提升+6.95分，同时解码NFE平均减少55.7%。

Conclusion: 解耦[EOS]的双重角色能有效解决MDLM中的溢出问题，提升指令微调效果和推理效率。

Abstract: MDLMs generate text by denoising a preallocated masked response canvas, making response-length modeling central to instruction tuning. Existing MDLMs often inherit the autoregressive convention of using repeated \texttt{[EOS]} tokens for padding during instruction tuning, giving \texttt{[EOS]} a dual role as both a semantic terminator and a padding token. We show that this dual role is a root cause of \texttt{[EOS]} overflow under large-block decoding. To decouple these roles, we propose VoidPadding, which introduces \texttt{[VOID]} for padding and reserves \texttt{[EOS]} for termination. During inference, the learned \texttt{[EOS]} signal enables early stopping, while the learned \texttt{[VOID]} signal guides adaptive response canvas expansion. On Dream-7B-Instruct, VoidPadding improves the block-size-averaged four-task mean across mathematical reasoning and code generation benchmarks by \(+17.84\) points over the original model and \(+6.95\) points over RainbowPadding, while reducing decoding NFE by 55.7\% on average. Code is available at https://github.com/Haru-LCY/VoidPadding.

</details>


### [6] [Compositional Skill Routing for LLM Agents: Decompose, Retrieve, and Compose](https://arxiv.org/abs/2606.18051)
*Xueping Gao*

Main category: cs.CL

TL;DR: 提出SkillWeaver框架，通过分解-检索-组合的方式解决组合型技能路由问题；引入CompSkillBench基准（300个组合查询，2209个MCP技能）；提出迭代技能感知分解（SAD）将分解准确率从51.0%提升至67.7%，上下文窗口消耗降低99%以上。


<details>
  <summary>Details</summary>
Motivation: LLM智能体日益依赖外部技能（工具），但实际任务常需组合多种技能而非单一选择，现有方法缺乏系统化的组合技能路由方案。

Method: SkillWeaver框架：LLM任务分解器 + 双编码器技能检索器（FAISS索引） + 依赖感知DAG规划器；SAD方法：检索增强的反馈循环迭代对齐分解与可用技能。

Result: SAD将分解准确率从51.0%提升至67.7%（+32.7%）；上下文窗口消耗减少99%以上；迁移实验显示即使目标类别不在检索池中仍获+35.6%的相对DA增益。

Conclusion: 任务分解质量是组合技能路由的主要瓶颈，迭代对齐分解与可用技能可显著改善；正确粒度是有效检索的前提条件。

Abstract: LLM agents increasingly rely on external skills -- reusable tool specifications -- but real-world tasks often require composing multiple skills, not just selecting one. We formalize this as the Compositional Skill Routing problem: given a complex user query and a large skill library, decompose the query into atomic sub-tasks, retrieve the appropriate skill for each sub-task, and compose an executable plan. We present SkillWeaver, a decompose-retrieve-compose framework combining an LLM task decomposer, a bi-encoder skill retriever with FAISS indexing, and a dependency-aware DAG planner. To support evaluation, we introduce CompSkillBench, a benchmark of 300 compositional queries over 2,209 real MCP server skills spanning 24 functional categories, sourced from the public MCP ecosystem. Our experiments reveal that task decomposition quality is the primary bottleneck: standard LLM decomposition reaches only 34.2% category recall at the step level. To address this, we propose Iterative Skill-Aware Decomposition (SAD), a retrieval-augmented feedback loop that iteratively aligns decomposition with available skills. SAD improves decomposition accuracy from 51.0% to 67.7% (+32.7%, Wilcoxon p < 10^-6) in a single iteration; DA-conditioned analysis confirms that correct granularity is the prerequisite for effective retrieval (CatR@1 rises from 34% to 41% when DA=1). SkillWeaver reduces context window consumption by over 99%, and transfer experiments confirm generalization (+35.6% relative DA gain even when target categories are absent from the retrieval pool).

</details>


### [7] [ConSA: Controllable Sparsity in Hybrid Attention via Learnable Allocation](https://arxiv.org/abs/2606.18056)
*Yao Chen,Yinqi Yang,Junyuan Shang,Xiangzhao Hao,Simeng Zhang,Yilong Chen,Tingwen Liu,Shuohuan Wang,Dianhai Yu*

Main category: cs.CL

TL;DR: 提出ConSA框架，通过L0正则化和增广拉格朗日约束，在用户指定的稀疏度目标下学习混合注意力（全注意力与滑动窗口注意力）的最优分配，在0.6B和1.7B规模LLM上优于基于规则的基线方法。


<details>
  <summary>Details</summary>
Motivation: 现有混合注意力架构依赖手工规则或后验启发式进行FA/SWA分配，缺乏对注意力行为的深入分析，需要一种可学习的、可控稀疏度的分配方法。

Method: 采用L0正则化学习二值掩码以选择每个注意力单元使用FA还是SWA，并通过增广拉格朗日约束在层粒度或KV头粒度上强制执行目标稀疏度。

Result: 在0.6B和1.7B两个LLM上，学习到的分配一致优于基于规则的基线；KV头粒度分配优于层粒度分配；学习模式将SWA置于底层，FA集中在中间层连续块，不同于规则方法的均匀交错模式。

Conclusion: 学习到的混合注意力分配结构在不同模型规模、稀疏度和分配粒度下保持稳定，揭示了内在注意力行为的细粒度频谱。

Abstract: Hybrid architectures combining full attention (FA) and sliding-window attention (SWA) are a promising paradigm for efficient LLM inference. However, existing methods typically rely on hand-crafted rules or simple post-hoc heuristics for FA/SWA allocation and offer limited analysis of the attention behaviors underlying these designs. We propose Controllable Sparsity in Hybrid Attention (ConSA), a framework that learns optimal FA/SWA assignment under a user-specified sparsity target. ConSA employs L0 regularization to learn binary masks selecting between FA and SWA for each attention unit, while an augmented Lagrangian constraint enforces the target sparsity at either layer or KV-head granularity. We evaluate ConSA on two LLMs at the 0.6B and 1.7B scales. Learned allocations consistently outperform rule-based baselines, with KV-head-wise allocation yielding clear gains over layer-wise allocation. The learned patterns place SWA in the bottom layers and concentrate FA into contiguous middle-layer blocks, diverging from evenly interleaved patterns in rule-based methods. This structure persists across model scales, sparsity levels, and allocation granularities, revealing a fine-grained spectrum of intrinsic attention behaviors that underlies the learned allocation.

</details>


### [8] [RubricsTree: Scalable and Evolving Open-Ended Evaluation of Personal Health Agents across Health Memory and Medical Skills](https://arxiv.org/abs/2606.18203)
*Weizhi Zhang,Zechen Li,Hamid Palangi,Ben Graef,A. Ali Heydari,Simon A. Lee,Salman Rahman,Ray Luo,Zeinab Esmaeilpour,Erik Schenck,Chloe Zhang,Yamin Li,Menglian Zhou,Philip S. Yu,Daniel McDuff,Lindsey Sunden,Mark Malhotra,Shwetak Patel,Ahmed A. Metwally*

Main category: cs.CL

TL;DR: RubricsTree是一个可扩展的评估框架，通过100多个原子级、可临床验证的布尔规则树，结合专家对齐的分层分类法和上下文感知自适应路由器，实现对LLM驱动的个人健康代理的可靠、可审计评估，在HealthBench上带来高达~66%的相对性能提升。


<details>
  <summary>Details</summary>
Motivation: 现有的LLM评估方法面临两难：医生标注可靠但成本高且不可扩展，LLM评判可扩展但主观、不一致且临床对齐不足，迫切需要一种兼具可扩展性和专家对齐质量的评估基础设施。

Method: 构建包含100+原子级布尔规则的专家对齐分层分类法（RubricsTree），通过4000条真实用户查询的迭代人机协同审核协议生成；设计上下文感知自适应路由器，按查询激活相关加权的规则子集，实现可扩展的高吞吐评估。

Result: RubricsTree在专家对齐度上显著超过基线；可靠地惩罚上下文退化响应；作为结构化指令、文本反馈或训练奖励使用时，在HealthBench上为Gemini、GPT、Qwen模型家族带来最高~66%的相对性能提升。

Conclusion: RubricsTree为产品级个人健康AI的持续优化提供了可扩展、可审计且持续演进的评估基础设施。

Abstract: The LLM-empowered personal health agents with user health (sensor) metrics have offered a promising pathway to alleviate global disparities in healthcare access. However, large-scale clinical deployment remains constrained by an open-ended evaluation bottleneck: physician annotation is reliable but costly and unscalable, while LLM-as-a-judge evaluators are scalable but subjective, inconsistent, and sometimes clinically misaligned. We introduce RubricsTree, a scalable evaluation framework with an expert-aligned hierarchical taxonomy of over 100 atomic, clinically-verifiable Boolean rubrics, evolving from the insights of 4,000 real user queries through an iterative human-in-the-loop curation protocol with an expertise panel led by an experienced physician. A context-aware adaptive router activates only the relevant auto-weighted rubric subset per query, providing the throughput needed for scalable evaluation with expert-aligned quality. Through a systematic meta-evaluation, we show that RubricsTree (i) substantially exceeds a strong large-scale evaluation baseline in expert alignment on challenging open-ended queries; (ii) reliably penalizes contextually degraded responses; and (iii) when used as structured instructions, text feedback, or training rewards for performance optimization, yields up to ~66% relative gains on HealthBench for Gemini, GPT, and Qwen model families. RubricsTree thus provides a scalable, auditable, and evolving evaluation infrastructure required for the continuous optimization of product-level personal healthcare AI.

</details>


### [9] [ReproRepo: Scaling Reproducibility Audits with GitHub Repository Issues](https://arxiv.org/abs/2606.18237)
*Shanda Li,Qiuhong Anna Wei,Jingwu Tang,Valerie Chen,Nihar B Shah,Tim Dettmers,Yiming Yang,Ameet Talwalkar*

Main category: cs.CL

TL;DR: ReproRepo是一个可扩展的框架，利用GitHub issues作为自然监督信号，评估LLM Agent在论文代码可复现性审计方面的能力。在1149篇ML论文上测试，最佳Agent（Codex+GPT-5.5）能识别约90%论文中至少一个与人类报告的复现问题语义相关的问题。


<details>
  <summary>Details</summary>
Motivation: 现有可复现性评估基准依赖大量人工进行数据标注和评估，难以规模化扩展。需要一种自动化的、可扩展的方法来评估LLM Agent协助论文代码复现的能力。

Method: 利用GitHub上人类提交的issue作为天然监督信号，构建ReproRepo框架。从1149篇ML论文及其对应的代码仓库中收集数据，评估多个LLM Agent配置（包括Codex+GPT-5.5）识别这些真实复现问题的能力。

Result: 最佳Agent（Codex+GPT-5.5）能为约90%的论文识别出至少一个与人类报告的复现问题语义相关的问题。Agent在识别可见故障和定位正确语义区域方面效果较好，但在精确定位方面仍不足。

Conclusion: ReproRepo可作为可重用、可扩展的框架，用于未来评估LLM Agent在真实世界可复现性审计中的表现。

Abstract: Reproducing research results from papers and released code is central to scientific progress. Existing works have introduced benchmarks to evaluate whether LLM agents can assist with reproducibility, but they are difficult to scale due to their reliance on substantial manual effort for data curation and evaluation. We introduce ReproRepo, a scalable framework for reproducibility evaluation that leverages human-raised GitHub issues as naturally occurring supervision on realistic reproduction blockers. We instantiate ReproRepo on 1,149 recent machine learning papers from major conferences and evaluate four frontier model-agent configurations. Our results show that LLM agents, even without executing code, can identify many real-world reproducibility problems from paper-repository pairs: the best agent in our study, namely Codex with GPT-5.5, surfaces at least one semantically related human-reported blocker for ~90% of papers in the study. Further analysis shows that agents are particularly effective for surfacing visible failures and identifying the right semantic region, but may still be insufficient in exact localization. ReproRepo can serve as a reusable, scalable framework for future evaluations of LLM agents on real-world reproducibility auditing. Our code is released at https://github.com/LithiumDA/ReproRepo.

</details>


### [10] [Variable-Width Transformers](https://arxiv.org/abs/2606.18246)
*Zhaofeng Wu,Oliver Sieberling,Shawn Tan,Rameswar Panda,Yury Polyanskiy,Yoon Kim*

Main category: cs.CL

TL;DR: 提出了一种非均匀宽度分配的"×形"Transformer架构，在保持宽早期层和后期层的同时缩小中间层，在200M到2B参数（稠密）和3B参数（MoE）的仅解码器语言模型上，一致优于参数匹配的均匀基线，同时减少22% FLOPs和15% KV缓存。


<details>
  <summary>Details</summary>
Motivation: 现有Transformer语言模型在所有层保持均匀宽度，但不同层可能承担不同的计算角色，这种均匀分配可能不是资源最优的，因此作者希望探索非均匀的容量分配方式。

Method: 提出×形架构，中间层窄、两端层宽，使用无参数的残差重缩放机制；在仅解码器语言模型上进行实验，范围从200M到2B（稠密）和3B（MoE）参数。

Result: 该架构在语言建模损失上一致优于参数匹配的均匀基线；在损失匹配的缩放曲线下FLOPs减少22%，KV缓存和I/O成本减少15%。

Conclusion: 非均匀宽度分配可以带来更资源最优的语言模型扩展方式。

Abstract: Scaling model size, specifically depth and width, has driven significant progress in transformer-based language models. However, most architectures maintain a constant width across all layers, allocating a fixed parameter and computation budget evenly despite different layers potentially playing distinct computational roles. In this work, we empirically investigate nonuniform capacity allocation across network depth by proposing a $\times$-shaped > <former architecture. This design maintains wider early and late layers while narrowing the middle layers, utilizing a parameter-free residual resizing mechanism. Across decoder-only language models ranging from 200M to 2B parameters (dense) and 3B parameters (MoE), our > <former consistently outperforms parameter-matched uniform baselines on language modeling loss. By reducing the average layer width, this architecture also requires fewer overall FLOPs (22% reduction under fitted loss-matched scaling curves) and smaller KV cache memory and I/O cost (15% reduction). In analysis, we show that this bottleneck structure results in qualitatively different representations in residual streams. Overall, our results demonstrate that nonuniform width allocation can result in more resource-optimal scaling of language models.

</details>


<div id='cs.LG'></div>

# cs.LG [[Back]](#toc)

### [11] [Models Take Notes at Prefill: KV Cache Can Be Editable and Composable](https://arxiv.org/abs/2606.17107)
*Bojie Li*

Main category: cs.LG

TL;DR: 本文发现Transformer的KV缓存中，前缀预填充已在下游笔记中写入了基于字段条件的结论，而字段自身的key/value对决策贡献极小（<1%）。基于此提出了"可编辑"和"可组合"两种能力：可编辑指通过修正字段key/value并追加勘误来更新下游笔记；可组合指将预编译技能通过RoPE重定位拼接到任意上下文中。统一框架在保持决策一致的前提下可将延迟降低14.9倍，并与vLLM前缀缓存兼容。


<details>
  <summary>Details</summary>
Motivation: 传统前缀缓存要求严格共享前缀，单字段变更即导致整个下游缓存失效。本文希望在不重计算的情况下实现KV缓存的编辑与组合复用。

Method: 通过因果分析验证字段key/value对决策贡献极小的假设，提出"笔记模型"解释：预填充阶段字段条件已写入下游笔记向量。基于此设计字段级编辑（修正字段+追加勘误）和技能组合（RoPE重定位+拼接）。

Result: 编辑单个字段在8B模型上恢复决策准确率1.00（约1%计算量）；技能组合在12个模型上logit余弦相似度0.90-0.999；统一agent延迟降低14.9倍；vLLM基准测试中前缀缓存命中率98.5%，p90首次令牌延迟降低53-398倍。

Conclusion: KV缓存可被视为可编辑、可组合的"笔记"，不依赖字段自身向量即可保留语义，适用于多种注意力变体、量化、MoE、多模态场景，且与现有生产级前缀缓存兼容。

Abstract: Prefix caching reuses prefill only across an exactly shared prefix, so one changed field invalidates the entire downstream cache. Yet overwriting the field's own key/value vectors and reusing the rest leaves the model acting on the old value. The reason, established causally across four model families: at prefill the model has already written the field-conditioned conclusion onto downstream notes; the field's own key/value drives under 1% of the decision. Read as a notebook of memoized conclusions, two capabilities follow. (1) It is editable. A salient erratum amends the notes; and with chain-of-thought, editing the field alone recovers the decision (1.00 at 8B, ~1% compute), while without CoT it is ignored. (2) It is composable. The notes are position-portable, so a precompiled skill can be RoPE-repositioned and spliced into any context, indistinguishable from full recompute (logit cosine 0.90-0.999, twelve models) at O(L) rather than O(L^2) time-to-first-token. A unified edit+compose agent stays decision-identical to recompute at up to 14.9x lower latency. The approach applies to any per-token attention KV cache, validated across scale, quantization, Mixture-of-Experts, and multimodal caches, and extends to several attention variants through small adapters. Because the erratum is append-only, it composes with production prefix caching: in an online vLLM benchmark it keeps the prefix cache-aligned (98.5% hit-rate), cutting p90 time-to-first-token by 53-398x.

</details>


### [12] [MODE: Modality-Decomposed Expert-Level Mixed-Precision Quantization for MoE Multimodal LLMs](https://arxiv.org/abs/2606.17118)
*Yuanteng Chen,Peisong Wang,Zhilei Liu,Nanxin Zeng,Yuantian Shao,Shiqiang Lang,Tao Liu,Chuangyi Li,Qinghao Hu,Gang Li,Jing Liu,Jian Cheng*

Main category: cs.LG

TL;DR: 提出MODE框架，通过模态分解的专家级混合精度量化方法，解决MoE多模态大语言模型中的专家重要性估计偏差问题，在W3A16下性能损失控制在2.9%以内。


<details>
  <summary>Details</summary>
Motivation: MoE-MLLMs性能优异但GPU显存成本过高，现有专家级混合精度量化方法在MoE-MLLMs上性能显著下降，原因是忽略了跨模态和模态内两个层面的专家重要性估计偏差。

Method: MODE框架包含三个核心步骤：(1) 按模态分解专家选择频率，消除视觉token数值主导带来的偏差；(2) 过滤冗余视觉token获得去噪后的视觉频率；(3) 评估每模态的量化敏感性作为频率估计的互补信号，最终通过整数线性规划为每个专家分配比特宽度。

Result: 在W3A16设置下平均性能损失仅2.9%，在极端2-bit设置下取得更大增益，特别适合MoE-MLLMs。

Conclusion: 模态分解的混合精度量化能有效克服MoE-MLLMs中的偏差问题，实现模型压缩与性能保持的良好平衡。

Abstract: Mixture-of-Experts Multimodal Large Language Models (MoE-MLLMs) offer remarkable performance but incur prohibitive GPU memory costs, making compression essential. Among PTQ methods, expert-level mixed-precision quantization has proven effective for MoE-LLMs, yet suffers notable degradation on MoE-MLLMs due to two overlooked biases in expert importance estimation. (1) At the cross-modal level, the numerical dominance of vision tokens causes expert selection frequency to be dominated by vision tokens, masking experts that are critical to the text modality; (2) at the intra-vision level, the large proportion of redundant vision tokens further skew frequency statistics, obscuring experts critical for informative visual content. To bridge gaps, we propose MODE, a modality-decomposed expert-level mixed-precision quantization framework for MoE-MLLMs that decomposes expert selection frequency by modality, filters redundant vision tokens to obtain denoised visual frequency, and further evaluates quantization sensitivity per modality as a complementary signal to frequency-based estimation. These signals are integrated into an Integer Linear Programming formulation to assign per-expert bit-widths under a given budget. Extensive experiments show that MODE is particularly well-suited for MoE-MLLMs, limiting average performance loss to within 2.9% at W3A16, with larger gains at the extreme 2-bit setting.

</details>


### [13] [PowerOPD: Stabilizing On-Policy Distillation with Bounded Power Transformation](https://arxiv.org/abs/2606.17199)
*Anhao Zhao,Junlong Tong,Yingqi Fan,Ping Nie,Wenjie Li,Xiaoyu Shen*

Main category: cs.LG

TL;DR: 提出PowerOPD，通过Box-Cox幂变换构造有界、符号一致的奖励函数，替代标准on-policy蒸馏中无界的高方差log-ratio奖励，显著提升大模型蒸馏的样本效率、训练稳定性和下游性能，同时降低计算开销。


<details>
  <summary>Details</summary>
Motivation: 标准on-policy蒸馏使用基于student采样token的log-ratio奖励，该奖励无界导致梯度方差极高、训练不稳定、性能差，需要一种原生有界且稳定的奖励设计。

Method: 提出PowerOPD族奖励函数，对log-ratio应用Box-Cox幂变换（参数α>0），得到原生有界且符号一致的奖励，α→0时退化为log-ratio。

Result: 在6个数学推理基准和4个Qwen3师生对中，Avg@8/Pass@8比vanilla OPD提升+6.37/+5.71，训练加速59.2%，GPU内存减少23.1%，梯度范数降低3000倍以上。

Conclusion: PowerOPD通过简单有效的奖励变换解决了on-policy蒸馏的核心训练病理，在性能、效率和稳定性上全面超越现有方法。

Abstract: Standard on-policy distillation (OPD) for large language models estimates the reverse-KL objective using student-sampled tokens, yielding an unbiased single-sample Monte Carlo estimator that avoids vocabulary-wide computation. However, we show that this estimator suffers from severe training pathologies in practice: sample inefficiency, unstable generation dynamics, and a substantial performance gap compared to exact full-vocabulary OPD. Reward-level diagnosis traces these pathologies to the log-ratio reward, which is unbounded by construction, producing extremely high-variance gradients concentrated at early positions and persisting throughout training; standard post-hoc scaling fail as they operate only after this distortion occurs. To solve this problem, we propose PowerOPD: a family of natively bounded, sign-consistent rewards from the Box-Cox power transformation, parameterized by alpha > 0, of which the log-ratio is the degenerate alpha -> 0 limit. Across six mathematical reasoning benchmarks and four Qwen3 teacher-student pairs, PowerOPD achieves benchmark-averaged Avg@8/Pass@8 gains of up to +6.37/+5.71 over vanilla OPD, +3.01/+3.54 over post-hoc stabilization, and +2.59/+8.90 over full-vocabulary OPD, while reducing wall-clock time by 59.2% and peak GPU memory by 23.1%. Larger alpha generally improves accuracy, consistently shortens responses, and keeps gradient norms more than 3,000x smaller than vanilla OPD.

</details>


### [14] [S4oP: Operator-level Pruning of Structured State Space Models for Resource-Constrained Devices](https://arxiv.org/abs/2606.18096)
*Marco Deano,Filippo Ziche,Nicola Bombieri*

Main category: cs.LG

TL;DR: 本文提出了一种针对S4/S4D状态空间模型的结构化算子级剪枝方法，通过在训练中交错进行结构化掩码和微调，在保持性能的同时显著降低推理延迟，最高可剪枝70%的算子。


<details>
  <summary>Details</summary>
Motivation: 尽管SSM在长序列建模上表现优异，但其计算和内存需求限制了在资源受限场景下的部署，现有研究缺乏针对SSM的结构化剪枝方法。

Method: 提出增量式、算子级别的结构化剪枝方法，将结构化掩码与微调交错进行，同时监控准确率和推理延迟，在统一框架下探索效率-精度的权衡。

Result: 在多个基准数据集上，剪枝高达70%的模型算子后，在大多数情况下保留了原始模型的性能，同时显著降低了推理延迟。

Conclusion: 结构化算子剪枝是提升SSM效率的有效且此前未被探索的策略，有助于SSM在资源受限场景中的实际部署。

Abstract: Structured State Space Models (SSMs), including the S4 and S4D architectures, have recently emerged as powerful alternatives to attention-based models for capturing long-range dependencies in sequential data. Despite their strong empirical performance, deploying these models in time- and resource-constrained settings remains challenging due to their computational and memory demands. In this paper, we propose a novel incremental, operator-level pruning approach for S4- and S4D-based models that significantly reduces inference cost while preserving predictive performance. To the best of our knowledge, this is the first work to systematically investigate structured operator pruning for SSMs. Our method progressively prunes model operators by interleaving structured masking with fine-tuning, while jointly monitoring accuracy and inference latency. We implement this approach within a unified training and evaluation framework that enables systematic exploration of efficiency-accuracy trade-offs. Experiments across multiple benchmark datasets show that pruning up to 70% of the model operators preserves the performance of the original models in most cases, while substantially reducing inference latency. These results demonstrate that structured operator pruning is an effective and previously unexplored strategy for improving the efficiency of SSMs and facilitate their deployment in practical, resource-constrained scenarios.

</details>


### [15] [ProCUA-SFT Technical Report](https://arxiv.org/abs/2606.17321)
*Jaehun Jung,Ximing Lu,Brandon Cui,Muhammad Khalifa,Shaokun Zhang,Hao Zhang,Jin Xu,Amala Sanjay Deshmukh,Karan Sapra,Andrew Tao,Yejin Choi,Jan Kautz,Mingjie Liu,Yi Dong*

Main category: cs.LG

TL;DR: 提出ProCUA-SFT数据集，包含310万步级SFT样本，源自93K条合成轨迹，覆盖2484个应用组合。通过全自动流水线生成，在真实桌面环境中合成具身任务并验证可行性。微调UI-TARS 7B在OSWorld上达到45.0%，相比基线提升18.7个百分点。


<details>
  <summary>Details</summary>
Motivation: 现有最大的公开CUA轨迹数据集AgentNet（22.5K人类轨迹）在SFT中导致负迁移（OSWorld成功率从26.3%降至8-10%），需要高质量、大规模且不会导致负迁移的训练数据。

Method: 全自动流水线：(i) 在真实桌面环境中合成接地任务，种子数据包括912个SpreadsheetBench电子表格、约10K Zenodo10K演示文稿及多应用OSWorld配置；(ii) 通过二元前置条件检查验证任务可行性后执行。单一VLM（Kimi-K2.5）同时担任目标生成器、前置条件判断器和轨迹执行器。每条轨迹扩展为步级前缀样本，精确复现推理时的上下文布局。

Result: 微调UI-TARS 7B在ProCUA-SFT上训练一个epoch，OSWorld准确率达到45.0%，比基础模型提升18.7个百分点，比AgentNet训练模型高35%以上。ProCUA子集已被纳入Nemotron 3 Nano Omni模型的训练数据。

Conclusion: ProCUA-SFT有效解决了现有CUA数据集的负迁移问题，通过全自动合成流水线生成的大规模步级SFT样本显著提升了CUA性能，证明了合成数据在CUA训练中的价值。

Abstract: Training computer-use agents (CUAs) -- models that interact with graphical desktops through screenshots and keyboard/mouse actions -- requires large-scale, diverse trajectory data collected in full desktop environments. The largest public resource, AgentNet (22.5K human trajectories), leads to negative transfer when used for supervised fine-tuning (SFT): continuing training UI-TARS 7B on AgentNet causes OSWorld success rate to fall from 26.3% to 8-10%. We present ProCUA-SFT, a dataset of 3.1M step-level SFT samples distilled from 93K synthetic trajectories across 2,484 application combinations. The dataset is produced by a fully automated pipeline that (i) synthesizes grounded tasks on live desktops seeded with real-world content -- 912 spreadsheets from SpreadsheetBench, approximately 10K permissively-licensed presentations from Zenodo10K, and multi-application OSWorld configs -- and (ii) verifies each task's feasibility through binary precondition checking before rollout. A single VLM (Kimi-K2.5) serves as goal generator, precondition judge, and trajectory executor, eliminating planner-actor capability gaps. Each trajectory is expanded into step-prefix samples that exactly reproduce the context layout seen at inference time. Fine-tuning UI-TARS 7B on ProCUA-SFT for one epoch yields 45.0% on OSWorld -- an 18.7 percentage-point improvement over the base model and over 35% above AgentNet-trained counterparts. A subset of ProCUA was incorporated into the training data for the Nemotron 3 Nano Omni model, contributing to its computer-use capabilities.

</details>


### [16] [Online LLM Selection via Constrained Bandits with Time-Varying Demand](https://arxiv.org/abs/2606.17489)
*Yin Huang,Qingsong Liu,Jie Xu*

Main category: cs.LG

TL;DR: 提出了一种在线学习算法，用于在边缘-云推理系统中为异构任务选择合适的LLM，在满足硬性（预算）和软性（延迟）约束的同时最大化奖励，并提供次线性遗憾和约束违反的理论保证。


<details>
  <summary>Details</summary>
Motivation: 边缘-云推理系统中LLM部署面临模型异构、性能随机未知、任务需求时变等挑战，需在资源约束下高效在线选择模型。

Method: 将问题建模为带约束的随机赌博机学习任务，提出利用置信界估计和需求预测的在线学习算法，同时处理打包型（硬）和覆盖型（软）约束。

Result: 提供次线性遗憾和次线性覆盖约束违反的理论保证，合成工作负载实验验证了方法的有效性和鲁棒性。

Conclusion: 所提算法能在动态资源受限环境下实现奖励最大化与长期约束满足的平衡，优于静态选择策略。

Abstract: Large Language Models (LLMs) are increasingly deployed in edge-cloud inference systems to handle diverse user tasks with heterogeneous accuracy, latency, and cost profiles. Selecting the appropriate LLM for each incoming task is critical for ensuring service quality and efficient resource utilization. However, model heterogeneity, stochastic and unknown performance characteristics, and time-varying task demands make static selection strategies inadequate. Real-world deployments often impose hard resource budgets such as monetary expenditure limits, along with soft service-level requirements such as latency guarantees. These constraints introduce additional challenges for online decision-making. We formulate this problem as a constrained stochastic bandit learning task, where the learner sequentially selects models under both packing-type (hard) and covering-type (soft) constraints, while adapting to time-varying task demand. The learner operates without access to the underlying reward, cost, or latency distributions and must rely on partial feedback. We develop a novel online learning algorithm that leverages confidence-bound estimates and demand predictions to balance reward maximization with long-term constraint satisfaction. We provide theoretical guarantees showing sublinear regret and sublinear covering constraint violations compared to an offline benchmark with full information. Experimental results on synthetic workloads demonstrate the effectiveness and robustness of our approach in dynamic, resource-constrained environments.

</details>


### [17] [Reconfigurable Computing Challenge: Transformer for Jet Tagging on Versal AI Engines](https://arxiv.org/abs/2606.17500)
*Gram Koski,Sean Lipps,Zhenghua Ma,G. Abarajithan,Ryan Kastner*

Main category: cs.LG

TL;DR: 本文提出了一种在AMD Versal AI引擎上实现量化整数transformer用于喷注标记的框架，将transformer层映射为可组合的AIE构建块，并从高层Python描述自动生成Vitis图代码。


<details>
  <summary>Details</summary>
Motivation: Transformer模型在LHC喷注标记中表现优异，但在低延迟、资源受限的触发系统中部署困难，亟需高效的硬件实现方案。

Method: 将密集层和多头注意力层映射到AIE瓦片，采用量化整数计算，并开发可复用软件框架，支持从Python高层模型描述自动生成Vitis图代码。

Result: 成功在Versal AIE上实现了transformer的初始部署，并开源了框架代码。

Conclusion: 该框架为未来在AIE上部署transformer模型的研究提供了基础，展示了在触发器系统中实现低延迟推理的潜力。

Abstract: Transformer-based models achieve strong performance for jet tagging at the CERN LHC, but deploying them in low-latency, resource-constrained trigger systems is challenging. We present an initial implementation of a quantized, integer-only transformer for jet tagging on the AMD Versal AI Engine (AIE), mapping dense and multi-head attention (MHA) layers to AIE tiles. The main contribution is a reusable software framework that represents transformer layers as composable AIE building blocks and automatically generates the corresponding Vitis graph code from a high-level Python model description. This framework provides a foundation for future research and is released as open-source software at https://github.com/KastnerRG/particle_transformer_aie.

</details>


### [18] [MGUP: A Momentum-Gradient Alignment Update Policy for Stochastic Optimization](https://arxiv.org/abs/2606.17526)
*Da Chang,Ganzhao Yuan*

Main category: cs.LG

TL;DR: 提出MGUP机制，通过每轮对固定比例的参数使用大步长、其余参数使用小步长的方式增强动量优化器（如AdamW、Lion、Muon），实现细粒度选择性更新并保证收敛性，提升大模型训练效率与稳定性。


<details>
  <summary>Details</summary>
Motivation: 现有层内选择性更新缺乏通用机制，既无法实现细粒度控制也无法保证收敛性，亟需一种即插即用、有理论保障的选择性更新方法以加速和稳定大规模模型训练。

Method: MGUP在每轮迭代中将参数按梯度/动量排序，选取固定比例（如top-k%）分配大步长，其余参数使用小但非零的步长；作为即插即用模块与AdamW、Lion、Muon等优化器集成；并在标准假设下为MGUP-AdamW提供随机优化收敛性证明。

Result: 在MAE预训练、LLM预训练和下游微调等任务上，MGUP增强的优化器相比原始优化器取得了更优或更稳定的性能表现。

Conclusion: 提供了一种原理性、通用且有理论保障的层内选择性更新策略，可有效加速和稳定大规模模型训练，代码已开源。

Abstract: Efficient optimization is essential for training large language models. Although intra-layer selective updates have been explored, a general mechanism that enables fine-grained control while ensuring convergence guarantees is still lacking. To bridge this gap, we propose \textbf{MGUP}, a novel mechanism for selective updates. \textbf{MGUP} augments standard momentum-based optimizers by applying larger step-sizes to a selected fixed proportion of parameters in each iteration, while applying smaller, non-zero step-sizes to the rest. As a nearly {plug-and-play} module, \textbf{MGUP} seamlessly integrates with optimizers such as AdamW, Lion, and Muon. This yields powerful variants such as \textbf{MGUP-AdamW}, \textbf{MGUP-Lion}, and \textbf{MGUP-Muon}. Under standard assumptions, we provide theoretical convergence guarantees for \textbf{MGUP-AdamW} (without weight decay) in stochastic optimization. Extensive experiments across diverse tasks, including MAE pretraining, LLM pretraining, and downstream fine-tuning, demonstrate that our \textbf{MGUP}-enhanced optimizers achieve superior or more stable performance compared to their original base optimizers. We offer a principled, versatile, and theoretically grounded strategy for efficient intra-layer selective updates, accelerating and stabilizing the training of large-scale models. The code is publicly available at https://github.com/MaeChd/MGUP.

</details>


### [19] [TuneAhead: Predicting Fine-tuning Performance Before Full Training Begins](https://arxiv.org/abs/2606.17660)
*Yuxiang Luo,Haonan Long,Chen Wang,Qiqi Duan,Xiaotian Lin,Yanwei Xu,Yuyu Luo,Weikai Yang,Nan Tang*

Main category: cs.LG

TL;DR: TUNEAHEAD是一个轻量级框架，通过在微调前结合静态数据集描述符和动态探针特征构建元特征向量，利用预测器预估微调性能（RMSE=1.47个百分点，95.1%预测误差在±3%以内），并基于SHAP提供可解释诊断，从而支持去/不去决策以减少不必要的完整微调。


<details>
  <summary>Details</summary>
Motivation: 微调大语言模型计算密集且容易出错，性能对数据质量和超参数敏感，盲目运行甚至可能降低模型性能，因此需要一种在投入完整训练前就能预测微调性能的方法。

Method: TUNEAHEAD将每次候选微调运行编码为元特征向量，结合静态数据集描述符和来自标准化短探针的动态探针特征，使用预测器将特征映射到性能估计，并利用SHAP归因提供可解释诊断。

Result: 在Qwen2.5-7B-Instruct上的1,300+次微调实验中，TUNEAHEAD在370次测试集上RMSE为1.47个百分点，95.1%的预测误差在±3%以内，优于Early-Stop Extrapolation和ProxyLM等基线方法。

Conclusion: TUNEAHEAD能够有效预测微调性能，支持实用的去/不去筛选策略，减少不必要的完整微调同时保留最有前景的运行。

Abstract: Fine-tuning large language models (LLMs) is compute-intensive and error-prone: model performance depends sensitively on data quality and hyperparameter choices, and naïve runs can even degrade model performance. This raises a practical question:can we predict fine-tuning performance before committing to a full training run? We present TUNEAHEAD, a lightweight framework for pre-hoc prediction of fine-tuning performance. TUNEAHEAD encodes each candidate run as a meta-feature vector that combines static dataset descriptors with dynamic probe features from a short standardized probe. A predictor maps these features to performance estimates, while SHAP-based attributions provide interpretable diagnostics that reveal which specific features drive the prediction. Across 1,300+ fine-tuning runs on Qwen2.5-7B-Instruct, TUNEAHEAD consistently outperforms strong baselines such as Early-Stop Extrapolation and ProxyLM. On a held-out test set of 370 runs, TUNEAHEAD achieves an RMSE of 1.47 percentage points and places 95.1% of predictions within +3/-3 percentage points of the true score. These accurate continuous predictions support practical go/no-go screening policies that can reduce unnecessary full fine-tuning while retaining most promising runs.

</details>


### [20] [Continual Self-Improvement with Lightweight Experiential Latent Memories](https://arxiv.org/abs/2606.17803)
*Vaggelis Dorovatas,Nancy Kalaj,Rahaf Aljundi*

Main category: cs.LG

TL;DR: 本文提出一种在线学习方法，让大语言模型将推理轨迹转化为轻量级的模块化潜在记忆（软提示），无需外部监督即可持续改进，在数学推理基准上优于零样本和原始数据上下文学习基线。


<details>
  <summary>Details</summary>
Motivation: 现有大语言模型在推理时虽然生成了丰富的推理轨迹，但这些计算产物未被有效复用，模型本质上是无状态的。作者希望将瞬时的推理计算转化为持久的可复用知识，实现无需外部监督的在线持续学习。

Method: 受无监督强化学习启发，使用自生成的测试时信号（多数投票）作为奖励，对每个实例进行轻量级训练（仅几步梯度更新），将推理轨迹蒸馏为极轻量的软提示记忆（约模型参数的0.001%），并通过模块化设计避免灾难性遗忘。

Result: 在数学推理基准上显著优于零样本和原始数据上下文学习基线，性能与完整参数更新和离线训练相当，且能跨数据集有效迁移。

Conclusion: 从原始推理轨迹转向学习到的潜在表征是关键，轻量级在线蒸馏方法能高效地将推理时计算转化为持续改进的知识，且计算成本极低。

Abstract: Large language models achieve strong reasoning performance by scaling inference-time compute, yet remain fundamentally stateless, discarding the rich, self-produced reasoning traces generated during this process. We investigate whether models can instead learn online from this experience, converting transient computation (reasoning traces) into persistent reusable knowledge, and without external supervision or access to future data. We show that In-Context Learning (ICL) over raw reasoning traces fails to generalize, reflecting a fundamental limitation of token-level reuse: individual traces lack the abstraction needed for transfer, even after refinement (e.g. self-reflection). In contrast, drawing inspiration from recent works on unsupervised reinforcement learning, we find that lightweight per-instance training with self-generated test-time signals (majority voting) as rewards yields substantial gains, often surpassing full-dataset offline training, motivating a shift from raw traces to learned latent representations. Building on this insight, we propose an online method that distills inference-time compute spent on encountered problems into compact modular latent memories capturing the underlying reasoning structure. These memories are stored and retrieved for future inputs, enabling continual improvement while avoiding catastrophic forgetting through modular design. Importantly, our method is highly efficient, parametrized as extremely lightweight soft prompt memories (~0.001% of model parameters) and trained with only a few gradient steps, yet achieving performance competitive with full parametric updates and offline training. Across challenging mathematical reasoning benchmarks, our approach significantly outperforms zero-shot and raw data ICL baselines, while transferring effectively across datasets.

</details>


### [21] [KANLib -- An Modular, Extensible and Fast Kolmogorov-Arnold Network Implementation](https://arxiv.org/abs/2606.17927)
*Julian Hoever,Gregor Schiele*

Main category: cs.LG

TL;DR: KANLib是一个模块化、可扩展且计算高效的KAN架构开发与评估框架，统一了现有KAN实现（PyKAN、EfficientKAN、FastKAN），支持两种基函数类型、自适应网格缩放等功能，并在California Housing基准上复现了参考实现的预测行为且计算效率有竞争力。


<details>
  <summary>Details</summary>
Motivation: KAN虽有理论优势，但因计算成本高、现有框架功能不一致，实际研究困难，因此需要统一高效的框架。

Method: 设计模块化、可扩展的KAN框架，统一现有实现，支持自适应网格缩放、网格扩展、细粒度架构定制，兼容PyTorch工作流。

Result: 在California Housing基准上复现了参考KAN实现的预测行为，计算效率有竞争力，且支持架构变体探索对预测性能影响很小。

Conclusion: KANLib为可扩展KAN架构的未来研究提供了稳健基础。

Abstract: Kolmogorov-Arnold Networks (KANs) have recently emerged as a promising alternative to traditional multilayer perceptrons by replacing linear weights with learnable univariate functions. Despite their theoretical advantages in interpretability and expressiveness, practical research of KANs remains difficult due to high computational costs and inconsistent feature support across existing frameworks. This paper introduces KANLib, a modular, extensible, and computationally efficient framework for developing and evaluating KAN architectures. KANLib unifies core concepts from existing implementations, including PyKAN, EfficientKAN, and FastKAN, within a consistent software architecture that emphasizes flexibility, feature parity, and high performance. The framework supports two basis function types, adaptive grid rescaling, grid extension, and fine-grained architectural customization while maintaining compatibility with standard PyTorch workflows. Experimental evaluation on the California Housing benchmark demonstrates that KANLib reproduces the predictive behavior of established reference KAN implementations while achieving competitive computational efficiency. Furthermore, the framework enables the exploration of architectural variations beyond standard KAN formulations with only minor impacts on predictive performance. Overall, KANLib provides a robust foundation for future research on scalable and extensible KAN architectures.

</details>


### [22] [SoftMoE: Soft Differentiable Routing for Mixture-of-Experts in LLMs](https://arxiv.org/abs/2606.17952)
*Mikołaj Zasada,Łukasz Struski,Jacek Tabor,Marcin Kurdziel*

Main category: cs.LG

TL;DR: SoftMoE 提出了一种基于 soft top-k LapSum 松弛的稀疏 MoE 方法，将离散路由替换为可微分的连续路由，并通过全局预算约束学习每层激活的专家数量，在保持自回归建模兼容性的同时，以更少的专家激活实现与稀疏 MoE 相当或更优的性能。


<details>
  <summary>Details</summary>
Motivation: 传统稀疏 MoE 使用离散 top-k 路由不可微分，强制每层固定数量的激活专家，导致计算资源利用低效，无法进行梯度优化的路由分配。

Method: 提出 SoftMoE，使用截断的 soft top-k LapSum 松弛替代离散 top-k 路由，使路由可微分；进一步参数化每层平均激活专家数并施加全局预算约束，让模型自主学习跨层的专家容量分配。

Result: SoftMoE 在语言建模和下游任务上达到与稀疏 MoE 相当或更优的性能，同时显著减少激活的专家数量；学习到的分配高度非均匀，深层激活更多专家。

Conclusion: SoftMoE 作为一种可微分的 MoE 方法，能更高效地分配计算预算，且完全兼容自回归语言模型，是一种实用的稀疏化替代方案。

Abstract: Sparse Mixture-of-Experts (MoE) architectures enable scaling LLM parameters under a fixed inference budget by activating only a small subset of experts via top-$k$ routing. While this preserves causality and suits autoregressive language models, the discrete top-$k$ operator is not differentiable, forcing a fixed number of active experts per input and resulting in inefficient use of computation. We propose SoftMoE, which replaces discrete routing with a truncated soft top-$k$ LapSum relaxation, allowing gradient-based optimization of expert routing. We further parameterize the mean number of active experts per layer and impose a global budget constraint, enabling the model to learn how to allocate expert capacity across layers. SoftMoE remains fully compatible with autoregressive modeling and achieves performance comparable to or better than sparse MoE on language modeling and downstream tasks, while activating significantly fewer experts. Notably, the learned allocation is highly non-uniform, with later layers activating more experts. The source code is publicly available$^\dagger$.

</details>


### [23] [LoopCoder-v2: Only Loop Once for Efficient Test-Time Computation Scaling](https://arxiv.org/abs/2606.18023)
*Jian Yang,Shawn Guo,Wei Zhang,Tianyu Zheng,Yaxin Du,Haau-Sing Li,Jiajun Wu,Yue Song,Yan Xing,Qingsong Cai,Zelong Huang,Chuan Hao,Ran Tao,Xianglong Liu,Wayne Xin Zhao,Mingjie Tang,Weifeng Lv,Ming Zhou,Bryan Dai*

Main category: cs.LG

TL;DR: 本文研究并行循环Transformer（PLT）的循环次数选择问题，通过训练LoopCoder-v2（7B参数、18T token）发现2次循环效果最佳，超过2次性能反而下降，并从增益-代价角度解释这一饱和现象。


<details>
  <summary>Details</summary>
Motivation: 并行循环Transformer（PLT）通过跨循环位置偏移（CLP）和共享KV门控滑动窗口注意力降低循环带来的延迟和KV缓存开销，使得循环次数成为可调设计参数。但如何选择最优循环次数尚不清楚，需要系统研究。

Method: 从零训练LoopCoder-v2模型族（7B参数），分别使用1、2、3、4次循环，在18T token上预训练后进行指令微调和评估，并结合增益-代价分析框架诊断不同循环次数的效果。

Result: 2次循环变体相比无循环基线在代码生成、代码推理、Agentic软件工程和工具使用等基准测试中全面提升，SWE-bench Verified从43.0提升至64.4，Multi-SWE从14.0提升至31.0。3次及以上循环性能反而下降。诊断表明第2次循环提供主要精细化更新，后续循环产生递减振荡更新且表征多样性降低，而CLP导致的偏移成本基本固定，因此净收益随循环次数增加而降低。

Conclusion: PLT的最优循环次数为2次，增益-代价权衡解释了循环次数饱和现象，该分析框架可为循环次数选择提供诊断依据。

Abstract: Looped Transformers scale latent computation by repeatedly applying shared blocks, but sequential looping increases latency and KV-cache memory with the loop count. Parallel loop Transformers (PLT) alleviate this cost through cross-loop position offsets (CLP) and shared-KV gated sliding-window attention, making loop count a practical design choice. We therefore study PLT loop-count selection through a gain--cost view: an extra loop may refine representations, but CLP also introduces a positional mismatch at each loop boundary. We instantiate this study by training LoopCoder-v2, a family of 7B PLT coders with different loop counts, from scratch on 18T tokens, followed by matched instruction tuning and evaluation. Empirically, the two-loop variant delivers broad gains over the non-looped baseline across code generation, code reasoning, agentic software engineering, and tool-use benchmarks, improving SWE-bench Verified from 43.0 to 64.4 points and Multi-SWE from 14.0 to 31.0 points. In contrast, variants with three or more loops regress, revealing a strongly non-monotonic loop-count effect. Our diagnostics show that loop 2 provides the main productive refinement, while later loops yield diminishing, oscillatory updates and reduced representational diversity. Because the CLP-induced mismatch remains roughly fixed as refinement gains shrink, the offset cost increasingly dominates. This gain--cost trade-off explains PLT's saturation at two loops and provides diagnostics for loop-count selection.

</details>


### [24] [Ternary Mamba: Grouped Quantization-Aware Training of W1.58A16 State Space Models](https://arxiv.org/abs/2606.18114)
*Ramprasath Ganesaraja,Sahil Dilip Panse,Swathika N*

Main category: cs.LG

TL;DR: 本文提出了一种利用量化感知训练（QAT）和知识蒸馏（KD）从预训练检查点出发压缩Mamba-2 SSM模型至三值化的方法，仅需1亿token（4 GPU小时）即可达到接近从头训练的精度，并发现了零比例坍塌这一新问题。


<details>
  <summary>Details</summary>
Motivation: 现有SSM模型（如Mamba-2）虽推理效率高，但内存占用限制了边缘部署；先前三值化SSM工作（Slender-Mamba）需从头训练150B token，成本高昂。本文旨在证明利用预训练检查点进行QAT可以大幅降低数据需求。

Method: 使用分组量化感知训练（QAT），结合冻结FP16教师模型的知识蒸馏，将Mamba-2 1.3B模型压缩至3.61倍（2687MB到744MB），仅训练102M token（4 GPU小时，单卡H100）。

Result: 达到48.1%零样本准确率（7任务平均），接近Bi-Mamba的48.4%（±0.9pp置信区间内）；发现零比例坍塌这一新不稳定现象；后验校正策略在SSM上因循环误差累积而失效。

Conclusion: 三值SSM不需要昂贵的从头训练，从预训练检查点出发进行QAT+KD是一种数据高效的替代方案。

Abstract: State Space Models (SSMs) such as Mamba-2 offer linear-time inference but their memory footprint limits edge deployment. Prior ternary SSM work (Slender-Mamba) trains from scratch on 150B tokens; we show a pretrained checkpoint suffices, reducing the marginal token budget by 1,000x. Using grouped quantization-aware training (QAT) with knowledge distillation from a frozen FP16 teacher, we compress Mamba-2 1.3B to 3.61x (2,687 to 744 MB) and achieve 48.1% zero-shot accuracy (7-task average) in just 102M tokens (4 GPU-hours, single H100) -- approaching Bi-Mamba's 48.4% (within +/-0.9pp CI). This QAT-from-pretrained setting reveals zero-ratio collapse, a novel instability caused by learnable quantization scales that does not arise in from-scratch training. We further show that post-hoc correction strategies effective for Transformers fail for SSMs due to error accumulation through the recurrence. These results demonstrate that ternary SSMs do not require expensive from-scratch training: QAT from pretrained checkpoints with KD is a data-efficient alternative.

</details>


### [25] [Embedded Machine Learning for Microcontroller-Class Edge Devices: Data, Feature, Evaluation, and Deployment Pipelines](https://arxiv.org/abs/2606.18122)
*Mostafa Darvishi*

Main category: cs.LG

TL;DR: 本文系统性地介绍了面向微控制器平台的嵌入式机器学习工作流程，涵盖采样缓冲、特征提取、类别不平衡验证、模型/运行时协同设计以及流式部署等工程决策，并以惯性运动识别和关键词识别两个案例说明。


<details>
  <summary>Details</summary>
Motivation: 将机器学习推理从云端迁移到资源受限的嵌入式设备上，这些设备在内存、能耗和延迟方面有严格限制，需要系统化的工程方法来指导设计与部署。

Method: 采用系统化综合方法，阐述嵌入式ML工作流的各个环节（采样缓冲、特征提取、模型/运行时协同设计、流式部署），并通过两个代表性信号家族（惯性运动识别和关键词识别）进行说明。

Result: 给出了面向微控制器平台的嵌入式ML工作流的系统化综合，包括关键工程决策的分析。

Conclusion: 总结出面向设备端鲁棒推理的实用设计规则，包括数据整理、量化、阈值设置、调度和现场监控。

Abstract: Embedded machine learning moves inference from cloud services to resource-constrained devices that must acquire data, preprocess signals, run a model, and act within tight limits on memory, energy, and latency. This paper presents a systems-oriented synthesis of an embedded machine-learning workflow for microcontroller-class platforms. The emphasis is placed on engineering decisions that are often hidden in generic machine-learning introductions: sampling and buffering, feature extraction as dimensionality reduction, validation under class imbalance, model/runtime co-design, and streaming deployment. Two representative signal families are used throughout the paper. The first is inertial motion recognition, where a two-second, three-axis accelerometer window is transformed from raw samples into root-mean-square and spectral features before classification. The second is keyword spotting, where audio is sampled, anti-aliased, transformed into mel-frequency cepstral coefficients, and processed by a compact one-dimensional convolutional network. The paper concludes with practical design rules for robust on-device inference, including data curation, quantization, thresholding, scheduling, and field monitoring.

</details>


<div id='cs.AI'></div>

# cs.AI [[Back]](#toc)

### [26] [SkillChain-Gym: A Benchmark for Reskilling-Aware Production-Inventory Control under Disruptions](https://arxiv.org/abs/2606.17266)
*Carlos Eduardo Sanoja*

Main category: cs.AI

TL;DR: SkillChain-Gym是一个面向技能再培训感知的生产库存控制问题的基准测试平台，包含工人技能动态、认证遗忘、培训与生产共享时间预算等特征，通过多种策略评估揭示了不同场景下无单一策略占优的结论。


<details>
  <summary>Details</summary>
Motivation: 现有运营基准通常将劳动力视为外生变量，而考虑技能与学习的劳动力规划模型缺乏可复用的测试平台，亟需一个标准化的基准来研究技能再培训与生产的联合决策问题。

Method: 构建SkillChain-Gym基准规范，包括单站点环境、工人技能状态动态、硬阈值认证、技能遗忘、培训与生产共享时间预算；提供种子控制的中断场景、三种可行性模式、确定性回放及多维度指标；评估了纯生产策略、反应式自适应、水填充自适应和静态保险策略等。

Result: 结果呈现场景依赖性而非单一排序：培训感知策略优于纯生产基线；遗忘环境下必须进行维护性培训；自适应培训在瓶颈可见时有效，而静态交叉培训在突发冲击下表现优异；容量松弛和遗忘速率决定策略边界。

Conclusion: 没有策略类别能在所有场景中占优，这激励了能决定何时购买技能保险、何时做出反应的预测驱动型控制器设计。

Abstract: Production planning increasingly has to treat workforce capability as a decision variable: certifications lapse when skills are not maintained, new products require skills the current workforce does not hold, and reskilling competes for the same worker hours needed for production. Existing operations benchmarks usually treat labor as exogenous, while workforce-planning models with skills and learning are rarely released as reusable testbeds. We introduce SkillChain-Gym, a benchmark specification for reskilling-aware production-inventory control: a single-site environment with stylized worker skill-state dynamics, hard threshold certification, forgetting, and capacity-consuming training actions constrained by the same per-worker time budget as production. The benchmark includes seed-controlled disruption scenarios, three feasibility modes with projection diagnostics, deterministic replay, and metrics covering operations, resilience, capability growth, and training-access distribution. We evaluate production-only, reactive adaptive, water-filling adaptive, and static-insurance policies with budget variants over 60-shift horizons with paired statistical tests. The results are regime-dependent rather than a ranking. Training-capable policies dominate the production-only baseline, and maintenance training is necessary under forgetting even without disruptions. Among training-capable classes, adaptive training helps when bottlenecks are visible in the forecast, while a lean static cross-training plan, a deliberately favorable comparator whose structure encodes relevant skill contingencies, acts as strong insurance under surprise shocks and absenteeism. Capacity slack and the forgetting rate govern the boundary between these regimes. No policy class dominates across regimes, motivating forecast-driven controllers that decide when to buy skill insurance and when to react.

</details>


### [27] [Distributed General-Purpose Agent Networks: Architecture, Key Mechanisms, and Prototypes](https://arxiv.org/abs/2606.17368)
*Shengli Zhang,Deen Ma,Zibin Lin,Taotao Wang*

Main category: cs.AI

TL;DR: 本文研究分布式通用代理网络——一种开放的对等网络，异构代理可在其中自主发现、建立信任、协商合作规则并执行开放式任务。提出以协议适配层为核心的分层架构，并针对协作发现、治理机制和开放任务执行三个核心问题给出了技术路线。


<details>
  <summary>Details</summary>
Motivation: 单一代理受限于本地数据、工具权限、运行时环境和治理边界，无法充分发挥能力，需要构建开放的分布式代理网络来实现更广泛的协作。

Method: 提出分层架构（协议适配层连接上层语义与下层网络操作），针对三个核心问题给出技术路线：基于无体八卦与顺序日志的语义公告传播、BAID身份绑定与MG-EigenTrust声誉的多主题治理、Stackelberg机制生成循环。

Result: 报告了BAID分层验证的原型开销结果以及MG-EigenTrust在跨主题伪装-合谋攻击下的机制级仿真结果。

Conclusion: 该框架为开放、可信、可扩展的代理协作提供了系统级基础。

Abstract: Large language models have accelerated the transition from passive conversational assistants to autonomous agents that can understand goals, plan actions, invoke tools, and execute multi-step tasks. Yet the capability of a single agent remains constrained by its local data, tool permissions, runtime environment, and governance boundary. This paper studies distributed general-purpose agent networks: open peer-to-peer networks in which heterogeneous agents deployed on personal devices, edge nodes, or autonomous computing environments can discover one another, establish trust, negotiate cooperation rules, and execute open-ended tasks. We argue that such networks cannot be obtained by simply combining existing peer-to-peer overlays with conventional multi-agent systems. Unlike traditional P2P networks, agent networks must propagate semantic declarations about intentions, capabilities, states, and cooperation constraints. We therefore propose a layered architecture centered on a protocol adaptation layer that connects upper-level task semantics with lower-level network operations. Based on this architecture, the paper identifies three core mechanism problems: semantic announcement propagation for collaborator discovery, verifiable identity and multi-topic reputation for cooperation governance, and semantic-gradient mechanism design for open task execution. For each problem, we present a technical route, including bodyless gossip with sequential logs, BAID-based identity binding with MG-EigenTrust reputation, and a Stackelberg-style mechanism-generation loop driven by semantic attribution feedback. We further report prototype overhead results for BAID-style tiered verification and mechanism-level simulations of MG-EigenTrust under cross-topic disguise-collusion attacks. The resulting framework provides a system-level foundation for open, trustworthy, and scalable agent collaboration.

</details>


### [28] [Dissecting model behavior through agent trajectories](https://arxiv.org/abs/2606.17454)
*Gaurav Gupta,Vatshank Chaturvedi,Jun Huan,Anoop Deoras*

Main category: cs.AI

TL;DR: 本文提出"意图-执行鸿沟"概念，指出AI Agent性能不仅是模型问题，更是系统工程问题。作者开发了Simple Strands Agent (SSA)框架，通过对齐模型意图与执行行为，在多个Agent基准测试上复现或超越了官方报告的性能。基于138k轨迹分析，揭示了不同模型在问题解决行为上的差异。


<details>
  <summary>Details</summary>
Motivation: 当前AI Agent性能被视为建模问题，但实际表现严重受限于模型与执行框架之间的不匹配，这种"意图-执行鸿沟"导致模型能力无法充分发挥。

Method: 提出SSA框架，设计跨模型家族（Claude、Gemini、GPT、Grok、Qwen）通用的Agent harness模式，通过代码状态空间表示轨迹，分析编辑频率、测试活动、阶段转换等细粒度指标。

Result: 在SWE-Pro、SWE-Verified和Terminal-Bench-2上复现或超越多个模型提供商的pass@1性能；基于138k轨迹分析发现不同模型在问题解决行为上存在显著差异。

Conclusion: Agent性能提升的关键在于缩小意图-执行鸿沟，即优化模型与harness之间的对齐，这比工具和循环等其他harness设计方面更为重要。

Abstract: AI agent performance is not just a modeling problem, it is fundamentally a systems problem. The advanced capabilities of models are realized through agent harnesses. Therefore, a gap between model assumptions and harness behavior can easily prevent the model's full capabilities from translating into agent performance. We formalize this as the `intent-execution' gap: the mismatch between what the model intends and what the harness executes, and vice versa. We argue that minimizing this intent-execution gap is as important as other aspects of harness design such as tools and execution loops. To illustrate the impact of this harness-model alignment, we develop a simple and customizable harness called `Simple Strands Agent' (SSA). SSA aims to find the bulk of common patterns which generalize across different model families (such as Claude, Gemini, GPT, Grok, Qwen), as well as a small number of model-specific preferences. We make two contributions: (i) we $\textbf{reproduce or improve on the pass@1}$ performance reported by diverse model-provider families on popular agentic benchmarks (SWE-Pro, SWE-Verified and Terminal-Bench-2), and (ii) building on an $\textbf{analysis of 138k trajectories generated by SSA}$, we look beyond the $\texttt{pass@1}$ numbers which tend to be relatively even across frontier models. By representing agent trajectories in code state-spaces, we observe model-level differences in problem-solving behavior. Finer-grained metrics such as edit frequency, testing activity, and phase-transitions reveal how individual models allocate effort across different stages of autonomous problem solving.

</details>


### [29] [SEAGym: An Evaluation Environment for Self-Evolving LLM Agents](https://arxiv.org/abs/2606.17546)
*Congjie Zheng,Chuanyi Xue,Bin Liang,Jun Yang,Changshui Zhang*

Main category: cs.AI

TL;DR: SEAGym 是一个用于评估自进化 LLM Agent 的 harness（执行层）更新效果的环境，通过训练、验证、测试、重放和成本等多个维度衡量更新是否带来可复用改进，避免仅依赖孤立任务分数或单一曲线。


<details>
  <summary>Details</summary>
Motivation: 现有评估方式将 Agent 的 harness 更新简化为孤立任务分数或单一序列曲线，无法判断更新是否产生可复用改进、过拟合近期任务、增加成本或损害旧行为。

Method: 构建 SEAGym 评估环境，将 Harbor 兼容的 Benchmark 转化为动态自进化任务源，包含训练批次、冻结更新验证、分布内/分布外迁移测试、重放诊断、快照和指标记录。在 Terminal-Bench 2.0 和 HLE 上对比 ACE、TF-GRPO 和 AHE。

Result: 不同评估视角提供互补信号：频繁更新可能无法提升留出性能，有用中间快照可能随后崩溃，数据源多样性和模型后端会影响 harness 可靠性。

Conclusion: SEAGym 为 Agent harness 的自演化提供了更全面的评估体系，揭示了现有简化评估方式忽略的关键问题。

Abstract: Self-evolving LLM-based agents improve mainly by changing their agent harness: the structured execution layer around a base model, including prompts, memory, tools, middleware, runtime state, and the model-tool interaction loop. Existing evaluations often reduce this process to isolated task scores or a single sequential curve, obscuring whether an update produces reusable improvement, overfits recent tasks, increases cost, or harms older behavior. We introduce SEAGym, an evaluation environment for measuring agent harness updates across training, validation, test, replay, and cost records. SEAGym turns Harbor-compatible benchmarks into dynamic self-evolution task sources with train batches, frozen update-validation, held-out ID and OOD transfer views, replay diagnostics, and saved snapshot and metric records. Instantiating SEAGym on Terminal-Bench 2.0 and HLE, we compare ACE, TF-GRPO, and AHE under a shared epoch/batch protocol. The results show that these evaluation views provide complementary signals about the evolution process: frequent updates may fail to improve held-out performance, useful intermediate snapshots may collapse later, and source diversity and model backend can affect harness reliability.

</details>


### [30] [DeepInsight: A Unified Evaluation Infrastructure Across the Physical AI Stack](https://arxiv.org/abs/2606.17574)
*Siyi Li,Chunyu Sun,Jiahao Zhang,Yuchen Kang,Wuliang Wang,Yu Qiu,Rui Jiang,Haitao Cui,Jie Chen*

Main category: cs.AI

TL;DR: DeepInsight是一个统一的评估基础设施，能够在单个运行时上覆盖从单次基础模型解码步骤到全身控制数千物理滴答的整个物理AI堆栈（跨度超三个数量级），通过任务、资源和结果三个抽象层来保留各子系统的异构性，并在生产环境中部署于完整的类人机器人堆栈，实现了跨层回归诊断。


<details>
  <summary>Details</summary>
Motivation: 现有框架无法覆盖物理AI堆栈中跨度超三个数量级的评估需求（从LLM解码到全身控制），各段使用不同评估框架导致无法诊断跨层回归问题。

Method: 设计三个统一的抽象（任务、资源、结果），每个抽象由所有子系统共享的不变量实现：一个episode驱动器、一个资源句柄协议（LLM推理和沙箱运行时统一）、一个跟踪身份方案。所有层写入同一个共享跟踪。

Result: 在生产环境中部署于完整的人形机器人堆栈的三个层；在基础模型端可复现同行框架结果，单节点运行速度更快，近线性跨节点扩展；跨层回归可在共享跟踪上定位。

Conclusion: DeepInsight通过统一的评估基础设施实现了跨层诊断能力，这是分段的独立评估框架无法复现的。

Abstract: Evaluating a Physical AI stack spans operators that differ by more than three orders of magnitude -- from a single foundation-model decoding step to thousands of physics ticks of whole-body control -- varying orthogonally in modality, reward semantics, and resource profile. No existing framework spans this range, so the stack is evaluated today by stitching together separate harnesses that share neither runtime nor scoring, preserving each segment's local validity but losing the shared identity needed to diagnose cross-layer regressions. We present DeepInsight, an evaluation infrastructure that serves this full spectrum on a single runtime. Rather than homogenize the regimes, it preserves their heterogeneity behind three narrow abstractions -- task, resource, and result -- each realized as one invariant shared by every subsystem: one episode driver, one resource-handle protocol implemented by every expensive backend (LLM inference and sandboxed runtimes alike), and one trace identity scheme under which every event is written. Deployed in production across all three layers of an embodied humanoid stack, this single set of invariants onboards new benchmarks largely by configuration. Where mature peer orchestrators exist -- at the foundation-model end -- it reproduces published references and peer-framework readings within their own spread, runs the same suites faster on a single node, and scales near-linearly across nodes. Its distinctive return is diagnostic: because every layer writes into one shared trace, a regression that begins in one layer and surfaces in another stays localizable on that trace -- a cross-layer payoff no federation of per-segment harnesses can reproduce.

</details>


### [31] [Beyond Domains: Reusing Web Skills via Transferable Interaction Patterns](https://arxiv.org/abs/2606.17645)
*Shiqi He,Yue Cui,Feijie Wu,Xinyu Ma,Jiaheng Lu,Yaliang Li,Bolin Ding,Mosharaf Chowdhury*

Main category: cs.AI

TL;DR: SkillMigrator是一种通过布局结构匹配来学习、迁移和复用Web技能的LLM Agent，将成功轨迹归纳为可迁移交互模式（TIP），在测试时通过布局相似性检索TIP并落地到实际页面，相比SOTA在同等成功率下将LLM action数减少8-10%。


<details>
  <summary>Details</summary>
Motivation: 现有LLM Web Agent每个低层级动作都需要一次LLM调用，导致长horizon任务中延迟和成本高；尽管已有Web技能库的尝试，但检索主要依赖指令相似性或粗粒度站点元数据，导致技能复用率低，无法充分减少步骤和token消耗。

Method: SkillMigrator将成功轨迹归纳为"可迁移交互模式（TIP）"，每个TIP包含技能本身及其诱导时刻的页面结构草图。测试时通过布局结构相似性（而非元素引用）检索相关TIP，并将TIP中的参考锚点落地到当前页面对应元素上；底层仍使用可访问性快照和固定工具调用。

Result: 在WebArena和Mind2Web两个基准上，与SOTA方法相比，SkillMigrator在保持匹配成功率的同时，将成功轨迹上的平均LLM action数减少8-10%。

Conclusion: 基于布局结构匹配的技能迁移机制能有效提升技能库的跨站点复用率，在降低延迟和成本方面比现有技能检索方式更优。

Abstract: Large language model (LLM) web agents are usually deployed as tool callers: each turn, the model reads a fresh page observation and emits one structured tool action. When every action is a low-level primitive, horizons grow quickly and so do policy-facing LLM completions, dominating latency and cost on benchmarks such as Mind2Web and WebArena. Recent systems therefore wrap repeated interaction fragments as web skills: callable tools built from successful trajectories or induced programs, so one call can replace several primitives. However, prior skill libraries are still triggered mainly by instruction similarity or coarse site metadata, which yields low skill reuse on held-out sites and leaves much of the potential step and token reduction on the table.
  We present SkillMigrator, an agent that learns reusable web skills and transfers them across sites by matching layout structure rather than specific element references. Each induced skill is stored as a transferable interaction pattern (TIP): the skill paired with a structural sketch of the snapshot at induction time. At test time, SkillMigrator retrieves TIPs by layout similarity and grounds their references on the live page. The rest of the stack is standard: accessibility-snapshot observations with stable references, and fixed tool calling over primitives plus skill invocations. Compared with the state-of-the-art approaches, SkillMigrator reduces the average LLM-action count on successful trajectories by 8-10% across both WebArena and Mind2Web at matched success rate.

</details>


### [32] [FlowRAG: Synergizing Explicit Reasoning via Frequency-Aware Multi-Granularity Graph Flow](https://arxiv.org/abs/2606.17856)
*Bihao Zhan,Zongsheng Cao,Jie Zhou,Bo Zhang,Liang He*

Main category: cs.AI

TL;DR: FlowRAG 提出了一种语义感知的四级异构图检索增强生成框架，通过摘要节点粗粒度语义枢纽、双粒度激活模块和频率感知加权流模块，提升了抽象查询下的语义召回率和多跳推理可靠性，在复杂推理基准上达到SOTA。


<details>
  <summary>Details</summary>
Motivation: 现有GraphRAG方法以实体图为主，存在两个问题：(1) 用户查询抽象且语义稀疏时检索不足；(2) 多跳推理中噪声激活导致实体间过渡断裂，推断链条不可靠。

Method: (1) 构建四级异构图：段落、摘要、句子、实体，摘要节点作为粗粒度语义枢纽；(2) 双粒度激活模块：结合摘要-查询对齐和句子级匹配，鲁棒激活语义相关实体；(3) 频率感知加权流模块：通过段落内词频加权实体-段落边，裁剪噪声连接，提取高置信度推理路径作为显式逻辑骨架。

Result: 在复杂推理基准上取得了state-of-the-art的性能。

Conclusion: FlowRAG通过语义感知的检索框架有效解决了抽象查询语义稀疏和多跳推理脆弱性问题，验证了四级图结构和频率感知流机制的有效性。

Abstract: Graph-based retrieval-augmented generation (GraphRAG) is effective for knowledge-intensive and multi-hop query tasks; however, many existing methods primarily seed entity-based graphs and rely on implicit semantic relevance propagation. This often (i) under-retrieves when user queries are abstract and semantically sparse at the entity level, and (ii) suffers from brittle multi-hop reasoning, where noisy activations can derail entity-to-entity transitions and corrupt the inferred relation chain, yielding unreliable conclusions. To this end, we propose \texttt{FlowRAG}, a semantic-aware retrieval framework that improves both semantic recall and explicit reasoning. Specifically, \texttt{FlowRAG} constructs a quad-level heterogeneous graph over passages, summaries, sentences, and entities, where summary nodes serve as a coarse semantic hub. At retrieval time, a dual-granularity activation module combines summary--query alignment with sentence-level matching to activate relevant entities under paraphrase and abstraction robustly. We then introduce a frequency-aware weighted flow module that routes relevance through entity--passage links weighted by within-passage term frequency, pruning noisy connections and extracting high-confidence reasoning paths as an explicit logic skeleton for generation. Extensive experiments show that \texttt{FlowRAG} obtains state-of-the-art performance on complex reasoning benchmarks.

</details>


### [33] [How Inference Compute Shapes Frontier LLM Evaluation](https://arxiv.org/abs/2606.17930)
*Jessica McFadyen,Ole Jorgensen,Harry Coppock,Kevin Wei,Cozmin Ududec*

Main category: cs.AI

TL;DR: 本文系统研究了前沿语言模型在复杂基准测试中，推理时计算资源（token预算、上下文压缩、重复提交）对性能的影响，发现更大推理预算能显著提升性能，固定预算评估会低估模型真实能力，建议评估应报告推理计算量与性能的函数关系。


<details>
  <summary>Details</summary>
Motivation: 当前AI评估趋向于需要工具使用和迭代求解的复杂任务，性能对推理时计算资源敏感，但许多评估仍在单一限制性预算下报告性能，低分可能反映评估设置而非模型真实能力。

Method: 在涵盖软件工程、数学、医学和网络安全等领域的7个基准上评估12个前沿语言模型，采用三种推理时扩展干预：更大的token预算、上下文压缩、重复提交尝试，并通过模型自身或最小正确性反馈进行引导。

Result: 更大token预算在多个领域显著提升性能；固定预算评估会低估新模型能力；不同基准对推理扩展方法的敏感性不同，重复提交广泛有效但其他方法因基准而异。

Conclusion: 基准分数是协议依赖的，评估应报告能力作为推理时计算的函数，明确指定协议选择，并在匹配预算的大计算范围内比较模型代际，特别是在安全或政策相关场景中。

Abstract: AI evaluations are shifting toward harder tasks that benefit from longer trajectories involving tool use and iterative problem solving. As a result, performance is increasingly sensitive to the amount and allocation of compute available at test time ("inference compute"). Yet many evaluations still report performance at a single restrictive budget, meaning that low scores may reflect the evaluation setup rather than the model's underlying capability. To test this, we evaluate up to 12 frontier language models on seven challenging benchmarks spanning software engineering, mathematics, medicine, and cybersecurity. We use a controlled setup combining three simple inference-scaling interventions: larger token budgets, context compaction, and repeated submission attempts, guided either by the model itself or by minimal correctness feedback. We find three main results. First, larger token budgets substantially improve performance on benchmarks across multiple domains, including cybersecurity, FrontierMath, Humanity's Last Exam, and TerminalBench. Second, fixed-budget evaluations can increasingly understate frontier capability as models advance. Newer models reach higher performance at large budgets, where they unlock harder tasks and solve them more reliably. Third, benchmarks differ in which inference-scaling methods help most: repeated submission broadly improves performance, but the value of larger token budgets, external feedback, and parallel attempts varies by benchmark. Overall, our results show that benchmark scores are protocol-dependent. We therefore argue that evaluations should report capability as a function of inference-time compute, specify protocol choices explicitly, and compare model generations over a large shared compute range at matched budgets, especially in safety- or policy-relevant settings.

</details>


### [34] [A Unified Framework for Context-Aware and Relation-Aware Graph Retrieval-Augmented Generation](https://arxiv.org/abs/2606.18075)
*Haoyang Zhong,Yifei Sun,Antong Zhang,Chunping Wang,Lei Chen,Yang Yang*

Main category: cs.AI

TL;DR: HyGRAG是一个分层图RAG框架，通过构建层次化索引结构（包含块节点和实体节点）、迭代聚类生成LLM摘要、上下文与关系感知的检索机制以及基于附加算法的动态更新，解决了现有图RAG方法中缺乏真正知识融合的问题，在多跳推理任务上平均准确率提升9.7%。


<details>
  <summary>Details</summary>
Motivation: 现有基于图的RAG方法（实体中心和块中心）都在原始文本表征上进行操作，通过相似性搜索分别检索信息，无法从信息综合中产生突现理解，缺乏真正的知识融合。

Method: 设计了包含块节点和实体节点的混合图上的层次化索引结构，通过迭代聚类生成LLM摘要；设计跨所有抽象级别搜索并通过社区成员关系扩展的上下文与关系感知检索；通过基于附加算法的局部再摘要实现动态知识更新。

Result: 在多跳推理任务上平均准确率提升9.7%，同时保持合理的效率。

Conclusion: HyGRAG通过层次化图结构和检索机制有效实现了超越源文档的知识融合与突现理解。

Abstract: Retrieval-Augmented Generation (RAG) has emerged as a paradigm for enhancing large language models (LLMs) with external knowledge, yet existing graph-based methods face a fundamental limitation: entity-centric and chunk-centric approaches operate on representations anchored to original text without true knowledge fusion. While entity-centric methods connect logically related content and chunk-centric methods preserve context, both retrieve information separately through similarity search, missing emergent understanding from their synthesis. In this paper, we propose HyGRAG, a hierarchical graph RAG framework that transcends source documents by addressing three core challenges: constructing summaries that genuinely integrate contextual and relational information, leveraging these synthesized representations to access emergent knowledge during retrieval, and efficiently updating hierarchical structures for dynamic corpora. Specifically, we design hierarchical index structures over hybrid graphs with both chunk and entity nodes, then iteratively cluster them and generate LLM-based summaries. Then, we design context and relation-aware retrieval that searches across all abstraction levels while expanding through community membership. Moreover, we enable dynamic knowledge update through attachment-based algorithms with only local re-summarization. Experimental results show that HyGRAG improves the average accuracy of multi-hop reasoning tasks by 9.7%, while maintaining reasonable efficiency.

</details>


### [35] [WEQA: Wearable hEalth Question Answering with Query-Adaptive Agentic Reasoning](https://arxiv.org/abs/2606.18147)
*Yuwei Zhang,Tong Xia,Bianca Emmerich,Yu Yvonne Wu,Dimitris Spathis,Xin Liu,Daniel McDuff,Cecilia Mascolo*

Main category: cs.AI

TL;DR: WEQA 是一个查询自适应智能体框架，通过 LLM 推理结合可穿戴数据分析工具，统一处理多模态可穿戴健康数据的问答任务，在基准测试中比 LLM 基线准确率高 24%。


<details>
  <summary>Details</summary>
Motivation: 现有 LLM 在医疗问答上表现优异，但处理可穿戴健康数据（连续、高维、纵向）时存在文本分布不对齐、推理流程僵化、难以应对多样传感器模态和用户意图等问题。

Method: 提出 WEQA 框架，包含 LLM 控制器合成执行计划、动态路由查询到合适的传感器分析和预训练模型组合，并进行基于外部知识的 grounded 响应审计。

Result: WEQA 在四个公开可穿戴数据集上比 LLM 和智能体基线准确率高 24%；12 位医学专家和 8 位用户的盲研表明在有用性和临床合理性上有显著提升。

Conclusion: 查询自适应的智能体框架能有效弥合 LLM 与可穿戴健康数据之间的鸿沟，是一种有前景的医疗 AI 系统方向。

Abstract: Language models are remarkably capable at medical question answering, in some cases surpassing the accuracy of general physicians. However, answering questions about wearable health data remains challenging and understudied, as these ubiquitous sensors produce continuous, high-dimensional, and longitudinal data, which is non-trivial to align with text-centric distributions in LLM pretraining. The diversity of sensor modalities and user intents cannot be effectively handled by a fixed reasoning workflow or a single pretrained foundation model. To address these challenges, we propose WEQA, a query-adaptive agent framework that unifies LLM reasoning with specialized wearable analytical and modeling tools. An LLM controller is employed to synthesize execution plans and dynamically route each query to the appropriate combination of sensor analysis and pretrained models, and perform grounded response auditing with external knowledge. We also curate a benchmark spanning four open wearable datasets comprising analytic and predictive tasks in three different health domains. Experiments show that our framework is 24% more accurate than LLM and agentic baselines, and a blinded study with 12 medical experts and 8 users shows substantial gains in usefulness and clinical soundness.

</details>


<div id='cs.DC'></div>

# cs.DC [[Back]](#toc)

### [36] [Evaluating LLM Coding Agents on SZ-Family Lossy Compression Across Architectures](https://arxiv.org/abs/2606.17058)
*Changqing Li,Shouwei Gao,Kai Zhao,Sheng Di,Wenqian Dong*

Main category: cs.DC

TL;DR: 本文评估了基于LLM的编码智能体在高性能计算场景下对SZ系列有损压缩核的CUDA工作负载（SZp和SZx）进行代码翻译与优化的效果，分析了其在NVIDIA GPU和Cerebras晶圆级加速器上的迭代行为、提示敏感性及失败模式，发现跨架构差异性显著：GPU上强模型吞吐高但对提示敏感，Cerebras上主要挑战是生成可运行程序，且LLM对模块化核比紧密耦合核更有效。


<details>
  <summary>Details</summary>
Motivation: LLM编码智能体在代码翻译和优化中应用日益广泛，但在性能关键的高性能计算场景中的有效性尚缺乏系统评估，现有研究未充分考虑异构计算架构带来的差异化挑战。

Method: 选取SZ系列误差有界有损压缩核中的两种代表性CUDA工作负载（SZp和SZx），分别在NVIDIA GPU和Cerebras晶圆级加速器上测试单智能体迭代生成，分析吞吐量、迭代模式、提示敏感性及失败模式。

Result: GPU上强模型吞吐更高但对提示精度敏感；Cerebras上主要难点是产生可运行程序；LLM智能体对模块化核（SZx）效果优于紧密耦合位级流水线核（SZp）。

Conclusion: 评估LLM编码智能体用于HPC需同时考虑性能结果和架构特异性鲁棒性，在基于线程的平台上的成功无法直接迁移到空间加速器。

Abstract: Large language model (LLM) coding agents are increasingly applied to code translation and optimization, yet their effectiveness in performance-critical high-performance computing (HPC) settings remains poorly characterized. This paper evaluates LLM-based coding workflows on SZ-family error-bounded lossy compression kernels, which combine numerical constraints with memory-intensive and control-flow-heavy implementations. We study two representative CUDA workloads (SZp and SZx) and target two heterogeneous execution platforms: NVIDIA GPUs and Cerebras wafer-scale accelerators. Focusing on single-agent iterative generation, we analyze not only final throughput but also agent runtime behavior, including iteration patterns, sensitivity to prompt specification, and characteristic failure modes. Our results reveal a pronounced cross-architecture divergence. On GPUs, stronger models can achieve substantially higher throughput but exhibit increased sensitivity to prompt precision and optimization guidance, whereas on Cerebras the dominant challenge lies in producing runnable programs under a PE-centric spatial execution model. We further observe that LLM agents are more effective on modular kernels (SZx) than on tightly coupled bit-level pipelines (SZp), where structural dependencies hinder optimization progress. These findings suggest that evaluating LLM coding agents for HPC requires accounting for both performance outcomes and architecture-specific robustness, and that success on thread-based platforms does not directly transfer to spatial accelerators.

</details>


### [37] [Towards Distributed Inference of LLMs on a P2P Network](https://arxiv.org/abs/2606.17059)
*Shabari S Nair,Krishanu Saini*

Main category: cs.DC

TL;DR: 提出了一种去中心化的前缀缓存感知路由方案，用于点对点LLM服务，每个节点维护本地基数树并通过周期性反熵异步刷新对等缓存估计，请求被路由到最长估计前缀匹配的节点，无需集中协调或KV缓存传输。


<details>
  <summary>Details</summary>
Motivation: 前缀缓存可以减少LLM推理延迟，但集群级别的缓存复用因缓存跨节点分区而具有挑战性，现有方案需要集中协调或缓存传输，开销大。

Method: 每个节点维护本地基数树记录自己的缓存前缀，并通过周期性反熵异步获取对等节点的缓存估计；请求根据最长估计前缀匹配路由到对应节点；采用弱一致性模型，过时元数据仅导致缓存未命中而非错误输出。

Result: 在模拟MMLU工作负载上，去中心化路由在低通信延迟和倾斜前缀分布下改善了延迟，但高网络延迟和亲和性引起的热点限制了其收益。

Conclusion: 去中心化的前缀缓存感知路由在特定条件下有效，但网络延迟和热点问题是其局限。

Abstract: Prefix caching can reduce LLM inference latency by reusing KV caches across requests with shared prompts, but cluster-scale reuse is challenging because caches are partitioned across nodes. We propose a decentralized, prefix-cache-aware routing scheme for peer-to-peer LLM serving. Each node maintains a local radix tree of its own cached prefixes and asynchronously refreshed estimates of peer caches using periodic anti-entropy. Requests are routed to the node with the longest estimated prefix match, without centralized coordination or KV-cache transfer. Stale metadata only causes cache misses, not incorrect outputs, making weak consistency sufficient for correctness. Evaluation on simulated MMLU workloads show that decentralized routing improves latency under low communication delay and skewed prefix distributions, while high network latency and affinity-induced hotspots limit its benefits.

</details>


### [38] [Space-Efficient Lock-Free Linear-Probing Hash Table](https://arxiv.org/abs/2606.17315)
*Hagit Attiya,Rotem Oshman,Noa Schiller*

Main category: cs.DC

TL;DR: 提出了一种无锁的线性探测哈希表，支持无等待查找，仅需少量元数据，在保持顺序线性探测核心优点的同时优雅处理并发冲突。


<details>
  <summary>Details</summary>
Motivation: 并发线性探测哈希表设计困难，现有方案要么限制并发性，要么依赖大量元数据损害空间效率，缺乏兼具强活性保证和空间效率的解决方案。

Method: 设计了一种无锁线性探测哈希表，使用少量元数据（LL/SC下常数比特，CAS下对数比特），支持线性化、无锁的插入和删除操作以及无等待的查找操作，无需重建表即可回收已删除元素空间。

Result: 分析了无相同键并发插入情况下的平摊步复杂度，证明每个操作的期望平摊步复杂度与顺序线性探测相匹配（直到每个键的争用点）。

Conclusion: 该算法在保持空间效率的同时实现了强并发保证，为并发线性探测哈希表提供了实用的设计思路。

Abstract: Linear probing is one of the simplest and most space-efficient approaches to hash table design, and is widely used in sequential settings due to its compact memory layout. However, designing a concurrent linear-probing hash table with strong liveness guarantees has proved difficult, and only a handful of such algorithms have been proposed, all of which either restrict concurrency or rely on large per-entry metadata, thereby compromising space efficiency.
  We present a lock-free linear-probing hash table with wait-free lookups that retains the core advantages of sequential linear probing while handling contention gracefully. Our design uses only a small amount of metadata per table entry: a constant number of additional bits when using LL/SC, or a logarithmic number of bits when using CAS. The algorithm is linearizable and lock-free, supports insert, delete, and wait-free lookup operations, and is able to safely reclaim space used by deleted elements without rebuilding the table.
  We analyze the amortized step complexity of our hash table assuming no concurrent insertions of the same key, and show that each operation has expected amortized step complexity matching that of sequential linear probing, up to the point contention per key.

</details>


### [39] [RISE: Relay Inference and Online Scheduling for Efficient Edge-Device Collaborative Diffusion Model Services](https://arxiv.org/abs/2606.17378)
*Zilan Huang,Zhiqing Tang,Hanshuai Cui,Tian Wang,Yuan Wu,Weijia Jia,Wei Zhao*

Main category: cs.DC

TL;DR: RISE提出了一种边缘-设备协同的文本到图像扩散模型服务方法，通过训练无关的中继推理机制（大模型在边缘端处理早期去噪步骤，小模型在设备端进行细节优化）结合上下文感知的调度器，在保证生成质量的同时实现最高2.1倍加速。


<details>
  <summary>Details</summary>
Motivation: 现有的文本到图像扩散模型部署要么使用大模型保证质量但延迟高，要么使用轻量模型速度快但语义连贯性差，且缺乏跨边缘服务器和用户设备的工作负载拆分方案。

Method: 提出RISE方法：1）训练无关的中继推理机制，利用同一模型族内的共享潜空间，边缘大模型处理早期语义结构去噪步骤，设备端小模型处理细节优化；2）上下文感知的调度器，基于提示复杂度、用户偏好、网络质量和节点负载选择最佳中继配置。

Result: 在两项基准测试中，RISE的中继机制最高实现2.1倍加速同时保持完整模型质量；上下文感知调度器在混合工作负载下有效平衡质量和延迟。

Conclusion: RISE证明了通过边缘-设备协同推理可以在不牺牲质量的前提下显著降低扩散模型服务的延迟，为边缘部署提供了实用方案。

Abstract: Text-to-image diffusion models are increasingly deployed at the network edge to serve heterogeneous workloads with diverse quality and latency requirements. However, existing deployment strategies choose either large edge-side models with high fidelity but high latency or lightweight device-side models that offer speed at the cost of semantic coherence. Moreover, these approaches rarely split the denoising workload between models of different sizes across edge servers and user devices. To bridge this gap, we propose RISE, a method for edge-device diffusion model services that combines relay inference with online scheduling. Driven by the finding that the latent intensity exhibits minimal deviation after a model handoff, RISE uses a training-free relay mechanism that exploits the shared latent space within a model family: the large model on the edge handles the early denoising steps that shape semantic structure, then passes the intermediate latent to a small device-side model for detail refinement. To deploy this mechanism as a practical service, a contextual bandit scheduler selects the best relay configuration based on prompt complexity, user preferences, network quality and real-time node loads. Experiments on two benchmarks show that RISE's relay mechanism achieves up to 2.1$\times$ speedup while preserving full-model quality, and its context-aware scheduler effectively balances quality and latency under mixed workloads.

</details>


### [40] [SpecGen: Accelerating Agentic Kernel Optimization with Speculative Generation](https://arxiv.org/abs/2606.17518)
*Jihu Guo,Sitian Lu,Tenghui Ma,Wei Gao,Zhisheng Ye,Xingcheng Zhang,Dahua Lin*

Main category: cs.DC

TL;DR: SpecGen是一个基于投机生成的智能体内核优化系统，通过从推理轨迹中分叉非推理生成、提前终止推理、动态资源分配和远程KV缓存复用等技术，解决LLM驱动的GPU内核调优中生成延迟长、反馈不足、资源利用率低三大系统级瓶颈。


<details>
  <summary>Details</summary>
Motivation: 现有基于推理LLM的智能体内核优化存在三个系统级低效问题：推理生成延迟长、性能反馈不足、验证/剖析资源利用率低，限制了搜索效率。

Method: SpecGen提出投机生成，在推理轨迹的适当时机触发点分叉非推理生成以产生候选内核，并行验证和剖析；当找到满意内核时提前终止推理；动态重分配GPU池并优先处理请求以降低反馈延迟；利用验证/剖析GPU的空闲内存作为远程KV缓存存储，避免前缀重计算。

Result: 在H200上使用两个推理LLM的实验表明，SpecGen相比基线系统减少了端到端时间，同时产生更多反馈、提高资源利用率并在固定时间和token预算下提升内核加速比。

Conclusion: SpecGen通过投机生成有效解决了智能体内核优化中的系统级瓶颈，显著提升了搜索效率和资源利用率。

Abstract: Agentic kernel optimization automates manual GPU kernel tuning via iterative generation, validation, and profiling with reasoning LLMs, casting the optimization task as feedback-guided search. However, our workload characterization reveals three system-level inefficiencies that limit search efficiency: (1) long generation latency due to LLM reasoning, (2) insufficient profiling feedback, and (3) underutilized validation/profiling resources. Our key insight is that the ongoing reasoning generation exposes a window for producing additional candidate kernels before it completes, allowing the system to terminate reasoning early once a satisfactory kernel appears.
  We present SpecGen, an agentic kernel optimization system with \emph{speculative generation}. First, SpecGen forks non-reasoning generations at well-chosen trigger points in the reasoning trace to yield kernels, increasing the candidate kernel count per iteration. These kernels are validated and profiled in parallel with the ongoing reasoning, increasing profiling feedback, and keeping resources busy during generation. When a kernel meets the termination criterion, SpecGen terminates the reasoning generation early to reduce the generation latency. Second, SpecGen dynamically reallocates validation and profiling GPU pools based on the arrival rate and prioritizes requests to reduce profiling feedback latency under bursty speculative generation load. Furthermore, SpecGen utilizes spare memory of the validation/profiling GPUs as remote KV cache storage to eliminate prefix recomputation of speculative generations under limited memory budget. Experiments with two reasoning LLMs on H200 show that SpecGen reduces end-to-end time over three baseline systems, while producing more profiling feedback, increasing resource utilization, and improving kernel speedup under a fixed time and token budget.

</details>


### [41] [AoiZora: Topology-Aware Auto-Parallel Optimization for Inference of Diffusion Transformers](https://arxiv.org/abs/2606.17566)
*Kaijian Wang,Yuanyuan Xu,Fanjiang Ye,Ye Cao,Jingwei Zuo,T. S. Eugene Ng,Yarong Mu,Yuke Wang*

Main category: cs.DC

TL;DR: AoiZora是一个编译器中介的拓扑规划器，通过将逻辑分片与物理放置重新连接，优化TPU子切片上的低延迟视频扩散推理，在TPU v5e上实现了最高1.42倍的延迟降低。


<details>
  <summary>Details</summary>
Motivation: 视频扩散模型已成为关键生成服务负载，但单设备上无法实现低延迟推理，需要将去噪步骤分布到多个加速器上。现有的自动并行系统仅搜索逻辑设备网格，忽略分片在物理TPU互连上的实际布局，导致性能损失。

Method: AoiZora首先通过廉价预编译IR消除弱分片候选，然后编译幸存者并使用编译后的HLO结合拓扑感知通信模型对物理放置进行排序，最终选择最优方案沿普通编译器路径实现。

Result: 在TPU v5e子切片上，AoiZora将Wan 2.1一步去噪延迟相比现有解决方案降低了1.42倍。

Conclusion: 重新连接逻辑分片与物理放置可以显著提升视频扩散推理在TPU子切片上的性能，且无需修改模型代码、编译器降级、集合内核和网络路由。

Abstract: Video diffusion has quickly grown into a key generative serving workload, yet producing each clip demands many denoising iterations over large spatio-temporal latents, which puts low-latency inference out of reach on a single device. A denoising step is therefore typically distributed across multiple accelerators, and TPU sub-slices have become an attractive and practical fabric for doing so. Current auto-parallel systems, however, search almost exclusively over logical device meshes and disregard how a chosen sharding is actually laid out on the physical TPU interconnect -- an oversight that leaves large, topology-dependent performance on the table. We address this gap with AoiZora, a compiler-mediated topology planner built for low-latency video diffusion inference on TPU sub-slices. Its guiding principle is to reconnect logical sharding with physical placement by drawing on different points in the compilation flow: AoiZora first eliminates weak sharding candidates from inexpensive pre-compilation IRs, then compiles only the ones that survive and orders their physical placements using compiled HLO together with a topology-aware communication model. The winning plan is realized along the ordinary compiler path, leaving model code, compiler lowering, collective kernels, and network routing entirely intact. On TPU v5e sub-slices, AoiZora reduces Wan 2.1 one-step denoising latency by as much as 1.42x relative to existing solutions.

</details>


### [42] [From GPU to Microcontroller: Online Ridge Regression for Edge-Deployable Traffic Prediction](https://arxiv.org/abs/2606.17613)
*Suresh Purini,Archit Narwadkar,Deepak Gangadharan*

Main category: cs.DC

TL;DR: 本文提出一种轻量级交通流预测方法，用每传感器的Ridge回归+RLS在线自适应替代复杂神经网络，仅需444参数/传感器（比GLMST少80倍），在ESP32微控制器上即可运行，冷启动训练7.4s，预测更新<2ms。


<details>
  <summary>Details</summary>
Motivation: 现有交通流预测模型（GCN、MLP）需要集中式GPU训练，不适合资源受限的智能交通部署场景。作者通过参数分析发现GLMST模型容量远超任务需求，因此探索更轻量化的方案。

Method: 用每传感器的Ridge回归（结合水平对齐的周期性特征）替代神经网络，配合递归最小二乘法（RLS）实现在线自适应。每个传感器模型自包含，仅需线性代数运算。

Result: 在四个PEMS基准测试中三个取得最佳MAPE，第四个差距在1%以内。ESP32上冷启动训练7.4s，预测更新<2ms且零堆分配；Raspberry Pi 5上冷启动训练0.21s，预测更新0.26ms。

Conclusion: 复杂神经网络对交通流预测是过度的，简单线性模型+在线自适应即可在边缘硬件上达到SOTA性能，展示了高效AI推理在资源受限设备上的可行性。

Abstract: State-of-the-art traffic flow forecasting models, including Graph Convolutional Networks and graph-less MLPs, require centralized GPU training across all sensors, making them impractical for resource-constrained intelligent transportation deployments. We show that much of this complexity is unnecessary. A parametric analysis of the recent graph-less model GLMST reveals that reducing its internal embedding dimension from 64 to 4 degrades MAPE by less than one percentage point, suggesting that the model's effective capacity far exceeds what the task requires. Motivated by this finding, we replace the neural architecture entirely with per-sensor Ridge regression using horizon-aligned periodic features, combined with Recursive Least Squares (RLS) for online adaptation. With only 444 parameters per sensor (80x fewer than GLMST) and test-time online adaptation, our method achieves the best MAPE on three of four PEMS benchmarks, and remains within one percentage point on the fourth. Because each sensor's model is self-contained and involves only elementary linear algebra, the entire pipeline (training, inference, and online adaptation) runs on edge hardware without a GPU. An ESP32 microcontroller (160 MHz, 520 KB SRAM) completes cold-start training in 7.4s and each predict-and-update in under 2ms with zero heap allocation; a single Raspberry Pi 5 core completes cold-start training in 0.21s and each predict-and-update in 0.26ms.

</details>


### [43] [LUMEN: Coordinated Failure Recovery for Distributed LLM Serving](https://arxiv.org/abs/2606.17787)
*Zhang Cao,Shujie Han,Juncheng Zhang,Yuanming Ren,Yongkun Li,Patrick P. C. Lee*

Main category: cs.DC

TL;DR: LUMEN 是一个面向大语言模型推理集群的容错系统，将故障恢复视为负载感知的协同问题，在检查点放置、中断请求分配和服务容量恢复三个决策点进行优化，显著提升了服务质量和恢复速度。


<details>
  <summary>Details</summary>
Motivation: 大规模 LLM 推理集群中工作节点故障频发，现有容错方案（从头重试或从固定邻居加载检查点）未考虑集群当前负载，且恢复期间恢复节点处于空闲状态，造成资源浪费和服务质量下降。

Method: LUMEN 将故障恢复建模为负载感知的协同优化问题，涵盖三个决策点：(1) 故障前的检查点放置策略；(2) 故障时中断请求的分配策略；(3) 模型重载期间服务容量的恢复策略。通过协调这三个决策，实现负载均衡的快速恢复。

Result: 通过原型实验和大规模仿真，LUMEN 在服务时间和恢复时间上均取得显著改进。

Conclusion: 将容错恢复设计为负载感知的协同问题，而非简单的检查点恢复，能在大规模 LLM 推理集群中更高效地应对节点故障。

Abstract: Modern large language model (LLM) serving clusters distribute inference requests across multiple worker processes on different GPUs, but failures are prevalent at scale. When a worker fails, the cluster simultaneously loses the failed worker's GPU-resident key-value (KV) caches and serving capacity, leaving surviving workers to absorb the redirected traffic while re-running interrupted requests from scratch. Existing fault-tolerant systems either restart interrupted requests from scratch or restore KV caches from checkpoints stored on a fixed neighboring worker, but both approaches route recovery work without considering current cluster load and leave the recovering worker idle during model reload. We present LUMEN, a fault-tolerant LLM serving system that treats recovery as a load-aware coordination problem across three decision points: checkpoint placement before failures, interrupted-request distribution at failure time, and serving capacity restoration during model reload. We evaluate LUMEN using both prototype experiments and large-scale simulations and demonstrate significant improvements in serving and recovery times.

</details>


### [44] [RouteBalance: Fused Model Routing and Load Balancing for Heterogeneous LLM Serving](https://arxiv.org/abs/2606.17949)
*Wei Da,Evangelia Kalyvianaki*

Main category: cs.DC

TL;DR: RouteBalance 是一个融合模型路由与负载均衡的联合调度层，在异构 LLM 服务集群中同时优化质量、延迟和成本。通过批处理预测器和推测性实例状态，在请求热路径上实现约32ms的低开销决策，在13实例28 GPU集群上达到三目标前沿的上界区域。


<details>
  <summary>Details</summary>
Motivation: 现有异构 LLM 服务栈将调度分为两层独立优化：模型路由器根据质量和成本选择模型但忽略实例负载，负载均衡器优化队列但忽略质量。这种隔离优化导致无法联合权衡质量-延迟-成本三元目标。

Method: RouteBalance 提出一个服务感知的调度层，将模型路由和负载均衡融合为单个在线分配问题，直接对具体模型实例做决策。采用批处理进程内预测器堆栈和推测性实例状态技术，将联合决策开销控制在请求热路径的约32ms（12 req/s）。

Result: 在13实例28 GPU异构集群上，RouteBalance 达到三目标前沿的上界区域。在质量优先时 DeepEval 0.419（比最强基线高+0.013），在成本优先时与最低成本基线持平。平衡模式下延迟2.8s、吞吐30 req/s，在高负载下领先增强版 BEST-Route 2.6-4.1倍。

Conclusion: 联合调度比分离优化能更有效权衡质量-延迟-成本三元目标。收益主要来自在模型选择时对延迟定价，学习型预测器提供校准和SLO余量而非前沿提升。

Abstract: Heterogeneous LLM serving stacks split scheduling into two layers that optimize in isolation: model routers pick a model from quality and cost signals while ignoring instance load, and serving load balancers optimize queues while ignoring quality. We present RouteBalance, a serving-aware scheduling layer that fuses both into a single online assignment over concrete model instances, jointly trading off quality, latency, and cost. A batched in-process predictor stack and dead-reckoned instance state keep the joint decision cheap on the request hot path ($\approx$32 ms at 12 req/s). On a 13-instance, 28-GPU heterogeneous cluster serving four model sizes, a single deployed RouteBalance stack traces the upper region of the three-way quality-cost-throughput frontier. Sweeping one weight vector reaches both the highest routing-decision quality (DeepEval $0.419$, $+0.013$ over the strongest baseline, $95\%$ CI $[{+}0.005,{+}0.022]$; the ordering holds when a second judge re-scores the actually served text) and, at its cost-priority corner, per-request cost that ties the cheapest baseline. With router engineering equalized against concurrent-scoring baseline variants we build, its balanced preset serves at $2.8$ s and $30$ req/s, leading $2.6$ to $4.1\times$ ahead of enhanced BEST-Route at high load. (Deploying those routers as published, one serial scoring call per request, makes them collapse $23\times$ under load, a deployment-architecture effect we isolate separately, not the routing result.) A four-arm isolation shows the benefit follows from pricing latency at model-selection time; the learned predictors contribute calibration and SLO headroom rather than the headline frontier. Code: https://github.com/AKafakA/route-balance

</details>


### [45] [Latency Prediction for LLM Inference on NPU Systems](https://arxiv.org/abs/2606.18042)
*Juhyun Park,Seungwoo Jeong,Jingyu Lee,Kyungyong Lee*

Main category: cs.DC

TL;DR: LENS是一个针对NPU的LLM推理延迟预测器，无需微架构或编译器信息即可预测延迟，平均预测误差2.15%。


<details>
  <summary>Details</summary>
Motivation: 部署LLM需要探索大量配置空间，但现有延迟预测方法无法直接应用于NPU，因为NPU存在不公开的微架构、不可预测的编译器优化以及bucketing导致的非线性延迟问题。

Method: LENS通过每个bucket进行两次端到端测量，然后组合这些结果来预测任意输入输出长度组合的延迟。

Result: 在多个厂商的NPU、多种LLM和多样化工作负载上验证，平均预测误差为2.15%，优于两个方法相关的基线。

Conclusion: LENS为NPU上的LLM推理延迟预测提供了有效方法，无需依赖微架构或编译器内部信息。

Abstract: Deploying Large Language Models (LLMs) requires exploring a large configuration space spanning parallelization strategies, batching techniques, and scheduling policies. Exhaustive measurement across this space is impractical, making latency prediction essential for system optimization. While NPUs have emerged as accelerators designed for LLM inference, no prediction methodology has been established for them. Specifically, applying prior work to LLM inference latency prediction on NPUs faces three challenges: undisclosed microarchitecture of commercial NPUs, unpredictable compiler optimizations, and latency non-linearity induced by bucketing.
  We present LENS, a latency estimator that predicts NPU inference latency without information on the microarchitecture or compiler, and captures the non-linear latency induced by bucketing. LENS profiles each bucket with two end-to-end (E2E) measurements and composes the results to predict latency for arbitrary input-output length combinations. We validate LENS across NPUs from multiple vendors, several LLMs, and diverse workloads, achieving a mean prediction error of 2.15\%. We further compare LENS against two methodologically related baselines, confirming the validity of its approach.

</details>


<div id='cs.SE'></div>

# cs.SE [[Back]](#toc)

### [46] [LogCopilot: Automating Log Aggregation Analysis through Large Language Models](https://arxiv.org/abs/2606.17094)
*Senyu Xie,Chenxi Zhang,Tong Zhou,Jiacheng Liu,Xiaoyu Hong,Qingshan Li,Xin Peng*

Main category: cs.SE

TL;DR: 本文提出LogCopilot，一个基于大语言模型的自动化日志聚合分析框架，通过层次化知识库和工具调用，将自然语言日志分析指令转化为LogQL查询，实现自动化分析，在四个数据集上平均准确率达76.8%。


<details>
  <summary>Details</summary>
Motivation: 随着系统规模和复杂性增加，日志分析变得困难。现有日志聚合系统（如Grafana Loki、ELK）需要工程师编写DSL查询语句，这既耗时又需要深入了解DSL语法和日志细节，因此需要自动化解决方案。

Method: LogCopilot基于大语言模型，构建层次化知识库来表示日志中的关键知识，通过知识检索和工具调用，接收自然语言指令并自动生成和执行LogQL查询，完成日志聚合分析。

Result: 在四个日志数据集上的评估表明，LogCopilot平均准确率达76.8%，优于基线方法，并且在LogQL查询生成方面表现有效。

Conclusion: LogCopilot能够有效利用LLM自动化日志聚合分析，减少工程师手动编写查询的工作量，为工业系统日志分析提供可行方案。

Abstract: Logs record the runtime behavior of software and are widely used in various tasks such as debugging, testing, and fault diagnosis. With the increase in system size and complexity, log analysis has gradually become a challenging task. Current industrial systems typically use log aggregation systems such as Grafana Loki and ELK to simplify the log collection and analysis process. Engineers write queries using the DSL query language provided by these systems can complete a variety of log analysis tasks. However, writing these queries is often time-consuming and labor-intensive, as it requires engineers to have a thorough understanding of the DSL syntax and the detailed information contained in the logs. To address these challenges, this paper proposes LogCopilot, an automated log aggregation analysis framework based on large language models (LLMs). LogCopilot accepts natural language log analysis instructions and accomplishes automated log analysis through knowledge retrieval and tool calling. LogCopilot constructs a hierarchical knowledge base to represent and provide key knowledge in logs. And it achieves automated log aggregation analysis by generating and executing LogQL queries. The evaluation based on four log datasets confirm the effectiveness of LogCopilot, which achieves an average accuracy of 76.8% and outperforms baseline approaches. Moreover, experiment results shows that LogCopilot is effective in LogQL query generation.

</details>


### [47] [FacProcessTwin: An LLM-Based System for Process Twin Development](https://arxiv.org/abs/2606.17666)
*Yash Pulse,Yong-Bin Kang,Abhik Banerjee,Prem Prakash Jayaraman*

Main category: cs.SE

TL;DR: FacProcessTwin 是一个利用大语言模型从工厂文档和操作员自然语言输入中自动构建过程孪生（process twin）的系统，可生成完整过程模型并绑定实时运营数据，在澳大利亚食品制造商的16条生产线案例中达到95.2%的F1准确率，开发时间仅为手工的1/6，且通过人在回路治理避免了安全关键绑定错误。


<details>
  <summary>Details</summary>
Motivation: 过程孪生相比资产孪生更有潜力提升整体流程效率，但开发成本高昂，需要精确建模整个生产过程及其设备、产品设置和工艺变体，并绑定实时数据。现有方法缺乏自动化手段来降低这一开发成本。

Method: 利用LLM解析工厂过程文档和操作员自然语言输入，自动生成完整过程模型；然后自动将模型中每个步骤绑定到实时运营数据源；最后以交互式过程图形式呈现，支持人工监控和纠正安全关键绑定决策。

Result: 在澳大利亚食品制造商的16条生产线上评估，涵盖冷藏、冷冻和无菌常温三类产品及同产品内工艺变体：过程模型生成F1达95.2%；开发时间约为手动的1/6；在歧义标签上，单次基线75%的错误绑定被FacProcessTwin通过交由操作员决策而完全避免。

Conclusion: LLM结合人在回路治理可以有效降低过程孪生的开发成本同时保证安全关键绑定的正确性，为制造业数字化转型提供了一条实用的自动化路径。

Abstract: Process twins provide real-time representations of entire production processes. By capturing how process steps interact, rather than monitoring a single machine in isolation as an asset-based digital twin does, they have the potential to drive efficiency gains across the whole process. However, developing a process twin is costly. It requires accurately modelling the entire production process: its process steps, the equipment and product-specific settings each step uses, and its process variations. The resulting model must then be bound to live operational data. We present FacProcessTwin, a system that leverages a large language model (LLM) to reduce this development time, building a process twin from a plant's process documentation and natural-language input from an operator. FacProcessTwin generates this complete process model and then automatically binds its process steps to live operational data. The generated model and its data bindings are rendered as an interactive process diagram through which manufacturing personnel can monitor and correct the system's autonomous decisions, such as resolving uncertainty at safety-critical binding steps. We evaluate FacProcessTwin through a real-world case study of an Australian food manufacturer, covering 16 production process flows that span chilled, frozen, and aseptic shelf-stable product categories and include process variations within the same product. The results show that FacProcessTwin generates these process models accurately (a mean F1 of 95.2% against ground truth) and builds each twin in roughly a sixth of the manual time. Its human-in-the-loop governance then keeps the safety-critical bindings correct: at ambiguous tags where a single-pass baseline silently mis-binds 75.0% of the time, FacProcessTwin defers to the operator and mis-binds none.

</details>


### [48] [Planning to Hammer: Difficulty-Aware Decomposition for Automating Rocq Proofs](https://arxiv.org/abs/2606.17981)
*Ning Zhang,Nongyu Di,Zenan Li,Yuan Yao,Xiaoxing Ma*

Main category: cs.SE

TL;DR: Quarry是一个基于规划的证据合成框架，将LLM提出的高层次证明分解与CoqHammer等自动化策略相结合，通过可证明性感知排序和递归子引理证明，有效提升了Rocq交互式定理证明的自动化程度，在多个基准测试上比最强基线提升7%-13%。


<details>
  <summary>Details</summary>
Motivation: 随着AI生成代码的普及，形式化验证（特别是交互式定理证明）对软件正确性至关重要，但机器可检验的证明生产仍是瓶颈。LLM擅长高级策略但缺乏局部严谨性，而自动化策略（如CoqHammer）能可靠处理局部目标但缺乏长程规划能力，需要将两者结合起来。

Method: Quarry将证据规划与证据执行分离：LLM主动提出多个带有任意子引理的证明分解方案，在Rocq中通过临时承认子引理进行类型检查，然后使用基于证明状态的难度模型（估计hammer可解性）对候选方案排序，最后在有限预算内递归证明子引理，将长证明转化为一系列hammer可解的义务。

Result: 在统一10分钟墙钟预算下，Quarry在三个Rocq基准测试上的成功率比最强基线高出7%-13%。基于规划的分解结合可解性感知排序显著提升了自动化程度，同时保持了可预测的成本。

Conclusion: 可靠的证据自动化可以通过协调神经规划与符号执行来实现，而非替代其中任何一方。

Abstract: As AI-generated code proliferates, formal verification, particularly through interactive theorem provers such as Rocq and Isabelle, becomes increasingly important for ensuring software correctness. However, producing machine-checked proofs in such provers remains a bottleneck. Existing solutions bring complementary strengths to proof automation: large language models (LLMs) can propose high-level proof strategies but lack local rigor, while automated tactics such as CoqHammer can reliably discharge many local goals but lack long-range planning capabilities. To combine the best of both worlds, we present Quarry, a planning-based proof synthesis framework that separates proof planning from proof execution. Specifically, Quarry asks an LLM to actively propose multiple proof decompositions with arbitrary sublemmas, type-checks them in Rocq under temporarily admitted sublemmas, and ranks candidates using a proof-state-based difficulty model that estimates hammer solvability. It then recursively proves sublemmas within a bounded budget, effectively turning long proofs into sequences of hammer-solvable obligations. We implement Quarry on top of SerAPI and CoqHammer and evaluate it using multiple frontier LLMs across multiple benchmarks. The experimental results show that planning-based decomposition with solvability-aware ranking substantially improves automation while maintaining predictable cost. Under a uniform 10-minute wall-clock budget, Quarry improves over the strongest baseline by 7% to 13% in success rate across three Rocq benchmarks. These results demonstrate that reliable proof automation can be achieved by coordinating neural planning with symbolic execution rather than replacing either.

</details>
