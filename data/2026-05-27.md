<div id=toc></div>

# Table of Contents

- [cs.CL](#cs.CL) [Total: 15]
- [cs.SE](#cs.SE) [Total: 9]
- [cs.AI](#cs.AI) [Total: 17]
- [cs.LG](#cs.LG) [Total: 22]
- [cs.DC](#cs.DC) [Total: 13]


<div id='cs.CL'></div>

# cs.CL [[Back]](#toc)

### [1] [SPEAR: Code-Augmented Agentic Prompt Optimization](https://arxiv.org/abs/2605.26275)
*Mengyin Lu,Cong Feng,Huimin Han,Guangming Lu,Yu Sun,Xiaonan Ding,Shihui Long,Fengyi Li,Tanvi Motwani*

Main category: cs.CL

TL;DR: 提出SPEAR，一种基于CodeAct范式的自由形式智能提示优化器，通过Python沙箱执行结构化错误分析，并在多个工业级LLM-as-judge任务和学术基准上取得显著提升。


<details>
  <summary>Details</summary>
Motivation: 现有自动提示工程(APE)方法将优化器本身视为固定流水线，缺乏灵活性，无法自适应地进行结构化错误分析和策略调整。

Method: 将CodeAct的代码即行动范式引入APE，提出SPEAR——一个配备evaluate、python、set_prompt、finish四个工具的智能体优化器。核心创新是Python沙箱工具，允许优化器在评估DataFrame上执行任意Python代码进行混淆矩阵、错误聚类等结构化分析。同时引入自动回滚和可选保护指标两个护栏机制保证单调改进。

Result: 在三个工业级LLM-as-judge套件（13个裁判任务）上全部获胜（工具选择κ 0.857 vs 0.359；过滤相关性F1-macro 0.815 vs 0.763）；BBH-7平均准确率0.938 vs GEPA 0.628和TextGrad 0.484；消融实验显示Python工具是复杂裁判任务的最大贡献因素。

Conclusion: SPEAR通过智能体自由形式优化和Python沙箱的结构化分析能力，在提示优化任务上显著超越现有方法，且Python工具带来的类别混淆聚合能力是长上下文LLM无法直接从原始DataFrame中可靠提取的。

Abstract: Automatic prompt engineering (APE) rewrites prompts to improve downstream task performance, but existing APE loops treat the optimizer itself as a fixed pipeline. We port the code-as-action paradigm of CodeAct (Wang et al., 2024a) to APE and propose SPEAR (Sandboxed Prompt Engineer with Active Roll-back), a free-form agentic optimizer with four tools -- evaluate, python, set_prompt, finish -- that decides autonomously how and when to use them. The distinctive tool is the Python sandbox: the optimizer writes and executes arbitrary Python on the current evaluation DataFrame, performing structural error analysis (confusion matrices, error clustering, per group metrics) the agent itself authors. Two guardrails turn the long-horizon agent into a monotone-improving optimizer: auto-rollback on metric regression, and an optional guard metric floor. We evaluate on three industrial LLM-as-judge suites (13 judge tasks across recruiter-intake, conversational-memory, and query-refinement systems) plus seven BBH tasks and GSM8K. SPEAR wins every industrial task on the primary metric ($κ$ 0.857 vs 0.359 on tool-selection; F1-macro 0.815 vs 0.763 on filter-relevance; $κ$ 0.254 vs 0.218 on the hardest extraction dimension). On BBH-7 SPEAR averages 0.938 accuracy vs GEPA 0.628 and TextGrad 0.484. Ablations show the Python tool is the largest single lever on complex judge tasks ($Δ\approx +0.79κ$ on the 5-class tool-selection judge, $Δ\approx +0.35κ$ on the hardest extraction dimension when removed); its irreplaceable contribution is class-pair confusion aggregation that a long-context LLM cannot extract reliably from the raw eval DataFrame.

</details>


### [2] [RICE-PO: Turning Retrieval Interactions into Credit Signals for Reasoning Agents](https://arxiv.org/abs/2605.26352)
*Mingchen Li,Hansi Zeng,Zhuo Qian,Jiatan Huang,Hamed Zamani,Hong Yu*

Main category: cs.CL

TL;DR: 提出RICE-PO框架，通过将检索交互转化为局部学习信号，解决推理型检索智能体中潜在推理步骤的信用分配问题，无需批评网络即可优化策略。


<details>
  <summary>Details</summary>
Motivation: 现有检索智能体训练中，可执行动作（查询/摘要）可直接评估，但潜在推理步骤不可观测且仅间接影响结果，导致最终奖励无法可靠地分配给真正起作用的推理步骤，存在信用分配不对称问题。

Method: RICE-PO：无批评网络的策略优化框架。选择高不确定性的可执行动作作为锚点，利用检索指标评估局部反事实分支，仅当推理到动作的影响强且未来残差效应稳定时，才将信用传播到潜在推理步骤。

Result: 在BRIGHT和BEIR数据集上，相同检索器设置下，RICE-PO consistently优于基于提示的智能体和基于分组的强化学习基线。

Conclusion: 智能体-环境交互结构本身可以为训练推理型检索智能体提供有用的监督信号。

Abstract: Retrieval is increasingly moving from one-shot matching toward interactive reasoning, where language agents iteratively inspect evidence, reformulate queries, and search again. Training such agents raises a credit-assignment challenge: executable actions such as queries or summaries can be directly evaluated by the retriever, while latent reasoning steps are not directly observable and only affect future executable actions. This asymmetry makes outcome-level reward assignment unreliable, as the same final reward may credit reasoning steps that did not actually shape retrieval success. We propose RICE-PO, a critic-free policy optimization framework that converts retrieval interactions into localized learning signals. RICE-PO selects high-uncertainty executable actions as anchors, evaluates local counterfactual branches using retrieval metrics, and propagates credit to latent reasoning steps only when reasoning-to-action influence is strong and future residual effects are stable. On BRIGHT and BEIR, RICE-PO consistently outperforms prompt-based agents and group-based RL baselines under the same retriever setting. These results show that the structure of agent-environment interaction itself can provide useful supervision for training reasoning-based retrieval agents.

</details>


### [3] [In-Context Optimization for Retrieval-Augmented Generation: A Gradient-Descent Perspective](https://arxiv.org/abs/2605.26356)
*Mingchen Li,Jiatan Huang,Chuxu Zhang,Liang Zhao,Hong Yu*

Main category: cs.CL

TL;DR: 本文从理论上证明线性自注意力层可实现检索增强生成（RAG）的梯度下降更新，将RAG视为上下文优化过程，并提出一种轻量级前向更新方法，在冻结的RAG大模型上提升检索-生成接口效果。


<details>
  <summary>Details</summary>
Motivation: 现有RAG将检索文档视为静态证据，忽略了其作为上下文自适应信号的潜力；而上下文学习已被证明与隐式梯度下降有关，本文旨在弥合两者之间的鸿沟。

Method: 通过理论分析证明线性自注意力层等价于统一线性化RAG目标上的单步梯度下降；基于此设计轻量级前向更新方法，在冻结检索器和LLM的情况下，预测上下文条件化的生成侧接口更新。

Result: 在七个QA基准、两个检索器和两个冻结LLM骨干上，该方法优于共享接口基线，可迁移到未见任务，且以远低于测试时梯度适应的每次查询成本达到了接近的性能。

Conclusion: RAG与上下文优化存在严格的理论对应关系，且这种视角可转化为实用的轻量级方法，提升冻结RAG模型的检索-生成协同效果。

Abstract: In-context learning has recently been linked to implicit gradient descent in linear self-attention models, suggesting that context can induce a forward-pass update. Retrieval-augmented generation (RAG) also relies on context, but retrieved documents are usually treated as static evidence rather than signals for adaptation. We study RAG as an in-context optimization process. First, we show that one linear self-attention layer can implement one gradient-descent step on a unified linearized RAG objective covering both projection-based and dot-product retrieval interfaces. This gives an exact regime where retrieval-augmented prediction and in-context optimization coincide. We use this result not as a literal model of LLM computation, but as a guide for adapting the interaction between queries and retrieved evidence. We then test the boundary of this correspondence: it remains stable under controlled linear extensions, but becomes feature-distribution dependent under nonlinear architectures. Finally, we turn this view into a lightweight method for frozen RAG LLMs. The method keeps the retriever and backbone fixed, and predicts a context-conditioned update to a generator-side evidence-use interface. Across seven QA benchmarks, two retrievers, and two frozen LLM backbones, this forward-only update improves a shared-interface baseline, transfers to held-out tasks, and approaches test-time gradient adaptation at much lower per-query cost.

</details>


### [4] [MicroSpec: Accelerating Speculative Decoding with Lightweight In-Context Vocabularies](https://arxiv.org/abs/2605.26444)
*Zhiyang Chen,Daliang Xu,Yinyuan Zhang,Chenghua Wang,Mengwei Xu,Yun Ma*

Main category: cs.CL

TL;DR: MicroSpec是一种无需训练的推理加速技术，通过动态构建紧凑的上下文相关词汇表（平均3k tokens，降低40倍以上），结合软硬件协同设计，在推测解码中将草稿推理延迟降低51.6%，端到端加速比达1.12-1.32x。


<details>
  <summary>Details</summary>
Motivation: LLM通常使用超过10万token的词汇表，在推测解码的最终线性投影层造成严重的计算瓶颈。现有词汇剪枝方法依赖固定或粗粒度的子词汇表，需要约3万个活跃token才能保持草稿模型质量，效率不足。

Method: 提出MicroSpec：1) 利用语言生成中的自然时间局部性，在每个解码步骤动态构建紧凑的上下文敏感活跃词汇表；2) 通过异步收集和GPU驻留状态管理来减轻稀疏内存访问的开销；3) 作为即插即用增强模块，无需额外训练参数。

Result: 平均词汇量减少40倍以上（降至3k token以下），草稿推理延迟降低51.6%，端到端加速比1.12-1.32x（相对于EAGLE-2），超越更复杂的基于训练的剪枝基线。

Conclusion: MicroSpec有效地解决了LLM推测解码中的词汇表计算瓶颈，通过训练无关的软硬件协同方法实现了显著的加速效果，且易于集成到现有系统中。

Abstract: Large language models typically employ vocabularies of over 100k tokens, which creates a major computational bottleneck at the final linear projection layer when performing speculative decoding. Current methods for vocabulary pruning depend on either fixed or coarse-grained sub-vocabularies, requiring around 30k active tokens to preserve the quality of the draft model. We introduce MicroSpec, a training-free technique that overcomes this limitation by building a compact, context-sensitive active vocabulary on the fly for every decoding step. Exploiting the natural temporal locality found in language generation, MicroSpec attains high token coverage while reducing the average vocabulary size by more than 40x (down to under 3k tokens), all without any additional trained parameters. To translate this high sparsity into actual speedups on contemporary hardware, we present a co-designed system and algorithm that mitigates the overhead of sparse memory accesses via asynchronous gathering and GPU-resident state management. Acting as a plug-and-play enhancement, MicroSpec reduces draft inference latency by 51.6% on average, achieving an end-to-end speedup of 1.12-1.32x relative to the leading speculative decoding approach EAGLE-2 on various benchmarks, while also surpassing more sophisticated training-based pruning baselines.

</details>


### [5] [Verilog-Evolve: Feedback-Driven and Skill-Evolving Verilog Generation](https://arxiv.org/abs/2605.26498)
*Zehua Pei,Hui-Ling Zhen,Yu Zhang,Sinno Jialin Pan,Mingxuan Yuan,Bei Yu*

Main category: cs.CL

TL;DR: Verilog-Evolve是一个反馈驱动的Verilog生成框架，通过迭代细化、可执行反馈（功能仿真、Yosys综合、ABC时序代理、GEMM指标）和跨会话技能演化，从自然语言描述生成更实用、可综合、时序友好的RTL设计。


<details>
  <summary>Details</summary>
Motivation: 现有LLM生成Verilog的流程仅做孤立采样和功能检查，不足以生成实际RTL设计所需的正确、可综合、时序友好且面向下游硬件目标的代码。

Method: 提出Verilog-Evolve框架：对每个任务生成多样候选版本，用功能仿真、Yosys综合、ABC时序代理和GEMM指标进行可执行反馈评估，按可配置评分提升最佳候选为主版本；跨任务维护模块化技能指引，根据任务和反馈上下文检索技能，通过创建/改进/跳过决策和验证报告从历史记录中演化候选技能。

Result: 在VerilogEval和混合精度GEMM任务上，Verilog-Evolve提升了最终功能成功率和晋升稳定性，生成更下游友好的RTL。验证门控技能演化进一步改善了GEMM下游质量，在所有评估模式中取得最佳下游分数和GEMM保持率。

Conclusion: 反馈驱动的迭代细化与跨会话技能演化能显著提升LLM生成Verilog的实用性和下游硬件友好性。

Abstract: Large language models (LLMs) have improved Verilog generation from natural-language specifications, but most pipelines still treat generation as isolated sampling followed by functional checking. This is insufficient for practical RTL design, where useful Verilog must be correct, synthesizable, timing-conscious, and friendly to downstream hardware objectives. We present Verilog-Evolve, a feedback-driven framework for versioned Verilog refinement and cross-session skill evolution. For each task, Verilog-Evolve generates diverse minor candidates, evaluates them with executable feedback from functional simulation, Yosys synthesis, ABC timing proxy, and optional GEMM metrics, then promotes the best candidate into a major version under configurable scoring. To improve across tasks, the system maintains modular skill guidance, retrieves skills according to task and feedback context, and evolves candidate skills from logged histories through create/improve/skip decisions and verifier reports. Experiments on VerilogEval and mixed-precision GEMM tasks show that Verilog-Evolve improves final functional success and promotion stability while producing more downstream-friendly RTL under open-source synthesis, timing-proxy, and netlist-level GEMM objectives. Validation-gated skill evolution further improves GEMM downstream quality and achieves the best downstream score and GEMM held-out pass rate among the evaluated skill modes.

</details>


### [6] [Bounded Path Context: A Controlled Study of Visible Path History in LLM-Based Knowledge Graph Question Answering](https://arxiv.org/abs/2605.26645)
*Xihang Shan,Ye Luo*

Main category: cs.CL

TL;DR: 本文提出Bounded Path Context (BPC)，在LLM-based KGQA中将路径序列化长度作为可调超参数，实验表明限制历史跳数（K=1）在WebQSP和CWQ上超越完整历史提示，同时减少9.7%-12.1%的输入token。


<details>
  <summary>Details</summary>
Motivation: 现有LLM-based KGQA方法默认将完整路径序列化到每个路由提示中，但控制器本已通过符号状态维护完整路径，这导致冗余token消耗和潜在干扰。

Method: BPC将控制器（符号内存维护完整路径）与关系选择提示解耦，提示仅包含问题、当前实体、候选关系和最多K跳历史，通过控制实验扫描K值进行对比。

Result: K=1在WebQSP上F1 0.487 vs 完整历史0.472；K=0在CWQ上F1 0.287 vs 0.274，分别减少9.7%和12.1%输入token；71-84%样本不受历史长度影响。

Conclusion: 路径序列化长度应作为可调接口变量而非默认假设，短历史既节省token又可能提升性能。

Abstract: LLM-based knowledge-graph question answering (KGQA) delegates graph traversal to language models, turning each question into a sequence of local relation-selection decisions repeated across beams and hops. A common but untested default is to serialize the complete partial path into every routing prompt, even though the controller already maintains this path as exact symbolic state. Bounded Path Context (BPC) decouples these two roles: the controller retains full paths in symbolic memory for answer extraction and audit, while the relation-selection prompt exposes only the question, the current entity, outgoing relation candidates, and at most the last K hops. A controlled sweep over K -- fixing graph neighborhoods, beam budget, depth, decoding, and answer-extraction format -- shows that bounded histories match or exceed full-history prompting on complete WebQSP and CWQ test sets with Qwen3.5-9B-AWQ: K=1 achieves 0.487 answer-set F1 on WebQSP versus 0.472 for full history, and K=0 reaches 0.287 on CWQ versus 0.274, with 9.7% and 12.1% fewer input tokens respectively. At the 4B scale, K=1 remains the strongest setting on both benchmarks. Per-example analysis reveals that 71-84% of examples are unaffected by history length, while the affected cases expose when prior hops disambiguate versus distract. These results suggest that path serialization length is better treated as a tunable interface variable than as a default assumption in LLM-based graph controllers.

</details>


### [7] [NestedKV: Nested Memory Routing for Long-Context KV Cache Compression](https://arxiv.org/abs/2605.26678)
*Hong Chen,Xiang Liu,Yubo Gao,Yuxuan Fan,Bo Wang,Yuanlin Chu,Yuanguo Lin,Xuming Hu*

Main category: cs.CL

TL;DR: NestedKV是一种训练免费的KV缓存压缩方法，通过多时间尺度的余弦异常得分和头自适应混合机制，在保留少量缓存时显著优于现有方法。


<details>
  <summary>Details</summary>
Motivation: 长上下文语言模型受限于KV缓存的内存开销，现有训练免费压缩方法仅依赖单一重要性信号（注意力、近因、层级分配或键独特性），在面对全局独特、局部 episodic 或即时相关的上下文时表现脆弱。

Method: NestedKV维护全局、块级别和滑动窗口三种粒度的键锚点，通过多时间尺度余弦异常对token打分，结合头自适应混合和惊喜门控token路由，并采用自适应每头预算，无需训练或修改LLM。

Result: 在RULER、LooGLE、LongBench等多个基准上，NestedKV在保留缓存较小时表现最强。Qwen3-4B上，在r=0.75时比KeyDiff提升高达19.10分（RULER）和19.29分（LongBench）；r=0.95时LongBench得分37.32 vs KeyDiff的17.55。

Conclusion: NestedKV通过多尺度异常检测和自适应分配，有效提升了训练免费KV缓存压缩的性能，尤其在小缓存场景下优势显著。

Abstract: Long-context language models are limited by the memory footprint of the key-value (KV) cache. Existing training-free KV compression methods usually rank tokens by one importance signal -- attention, recency, layer-wise allocation, or key distinctiveness -- which becomes brittle when useful context is globally distinctive, locally episodic, or immediately relevant. We introduce NestedKV, a key-only KV cache compression method inspired by the Continuum Memory System in Nested Learning. NestedKV maintains global, block-level, and sliding-window key anchors, scores tokens by multi-time-scale cosine anomaly, and combines the resulting rankings with a training-free outer learner using head-adaptive mixing and surprise-gated token routing. The score is paired with adaptive per-head budgets and requires no training or LLM modification. Across RULER (4k--32k), LooGLE, LongBench, LongBench-E, InfiniteBench, and MMLU-Pro on Qwen3 and Llama-3.2 models, NestedKV is strongest when the retained cache is small. On Qwen3-4B, it improves over KeyDiff by up to 19.10 points on RULER and 19.29 on LongBench at $r=0.75$; at $r=0.95$, it retains 37.32 on LongBench versus 17.55 for KeyDiff.

</details>


### [8] [Uncertainty-Aware Budget Allocation for Adaptive Test-Time Reasoning](https://arxiv.org/abs/2605.26849)
*Manh Nguyen,Sunil Gupta,Hung Le*

Main category: cs.CL

TL;DR: 提出不确定性感知预算分配（UAB）框架，通过基于输出对数概率的平均负对数似然（ANLL）估计每个问题的难度，采用边际贪心算法在固定采样预算下为不确定问题分配更多采样，从而提升LLM推理准确率，无需额外模型或推理调用。


<details>
  <summary>Details</summary>
Motivation: 现有多次采样方法对所有问题均匀分配计算资源，导致简单问题过采样而难题采样不足，效率低下。

Method: 两阶段框架：Phase 1每个问题进行一次生成，利用其平均负对数似然（ANLL）作为难度信号；Phase 2采用边际贪心算法求解凹覆盖最大化问题，为不确定性高的问题分配更多采样预算。

Result: 在6个模型（1.5B-27B参数）和5个推理基准上，平均准确率提升最多+3%，单个基准提升最多+5%，低资源场景增益最大。

Conclusion: UAB是一种高效、零额外开销的预算分配方法，可显著提升多次采样推理的效率。

Abstract: Sampling multiple responses improves language model reasoning, but uniform compute allocation is inefficient: easy questions are over-sampled while hard questions remain under-explored. We propose Uncertainty-Aware Budget Allocation (UAB), a concave integer optimization framework that reallocates a fixed sampling budget based on per-question uncertainty estimated at no additional inference cost. In Phase 1, every question receives one generation; its average negative log-likelihood (ANLL), extracted directly from output log-probabilities, serves as a difficulty signal while the generation contributes to the final vote. In Phase 2, the remaining budget is allocated by a marginal-greedy algorithm that solves a concave coverage-maximization surrogate exactly: uncertain questions receive more sampling budget while confident questions receive fewer additional samples. Evaluated on six open-weight and black-box models spanning 1.5B to 27B parameters and five reasoning benchmarks covering math, logic, and preference tasks, UAB outperforms baselines by up to +3% in average accuracy and up to +5% on individual benchmarks, with the largest gains in low-resource settings, requiring no auxiliary model or additional LLM call. Code is publicly available at https://github.com/manhitv/UAB.

</details>


### [9] [Efficient Agentic Reinforcement Learning with On-Policy Intrinsic Knowledge Boundary Enhancement](https://arxiv.org/abs/2605.26952)
*Dingwei Chen,Zefang Zong,Zhipeng Ma,Leo Luo,Yang Li,Chengming Li,Peng Chen,Jie Jiang*

Main category: cs.CL

TL;DR: AKBE是一种在线策略方法，通过在训练中动态探测模型内在知识边界（有工具/无工具双路径展开），引导LLM高效使用工具，在7个QA基准上平均准确率提升1.85，工具调用减少18%。


<details>
  <summary>Details</summary>
Motivation: 现有agentic RL训练会导致模型产生冗余工具调用，模糊了模型内在知识边界，即模型无法区分何时需要工具、何时仅靠参数化知识即可。粗粒度的奖励塑形方法会导致reward hacking。

Method: 提出AKBE方法，采用在线策略的双路径（有工具/无工具）展开来动态探测模型知识边界，按轨迹正确性分类并构建针对性监督信号，无缝集成到agentic RL训练循环中。

Result: 在7个QA基准上，AKBE相比标准agentic RL平均准确率提升1.85，工具调用减少18%，工具生产力提升25%，且无准确率-效率权衡。

Conclusion: AKBE能有效提升LLM agent的工具使用效率与准确性，具有良好的即插即用兼容性，适用于不同RL算法。

Abstract: Agentic reinforcement learning (RL) has proven effective for training LLM-based agents with external tool-use capabilities. However, we identify that agentic RL training induces increasing redundant tool calls and blurs the model's intrinsic knowledge boundary, where the model fails to distinguish when tools are needed versus when parametric knowledge suffices. Existing solutions based on reward shaping create coarse-grained optimization targets that tend to incentivize indiscriminate tool-call suppression, leading to reward hacking. In this paper, we propose AKBE (Agentic Knowledge Boundary Enhancement), an on-policy method that dynamically probes the model's intrinsic knowledge boundary through dual-path (with-tool and no-tool) rollouts during training. We define the knowledge boundary as the per-instance determination of whether tools are required and the minimum tool calls necessary. By comparing correctness across paths, AKBE categorizes trajectories and constructs targeted supervisory signals that guide efficient tool-use patterns for each question. These signals are integrated seamlessly into the agentic RL training loop. Experiments on seven QA benchmarks demonstrate that AKBE improves task accuracy by +1.85 on average and reduces tool calls by 18% over standard agentic RL, yielding 25% higher tool productivity without any accuracy-efficiency trade-off. Further analysis suggests its plug-and-play compatibility across different RL algorithms and the mechanism of each signal category. Our code is available at https://github.com/CuSO4-Chen/AKBE.

</details>


### [10] [Share More, Search Less: Collaborative Parallel Thinking for Efficient Test-Time Scaling](https://arxiv.org/abs/2605.27030)
*Xinglin Wang,Hao Lin,Shaoxiong Feng,Peiwen Yuan,Yiwei Li,Jiayi Shi,Yueqi Zhang,Chuyi Tan,Ji Zhang,Boyuan Pan,Yao Hu,Kan Li*

Main category: cs.CL

TL;DR: 提出Collaborative Parallel Thinking (CPT)框架，通过搜索时跨并行分支的信息共享机制，减少冗余探索，提升大语言模型在测试时扩展中的推理效率。


<details>
  <summary>Details</summary>
Motivation: 现有并行测试时扩展方法中，各搜索分支相互隔离，中间发现无法及时共享，导致大量冗余探索和低效的决策信息收集。

Method: CPT是一种免训练推理框架，从活跃分支中提取紧凑中间信息，维护去重的查询级信息池，并通过输入上下文广播池内容，使后续搜索步骤的各分支能复用其他分支的发现。

Result: 在HMMT和AIME基准上，CPT在不同rollout预算和模型规模下，建立了比强基线更优的准确率-延迟帕累托前沿。

Conclusion: 搜索时协作是高效并行测试时扩展的有效方向。

Abstract: Test-Time Scaling (TTS) enhances the reasoning capabilities of large language models by allocating additional inference compute to explore the solution space. However, existing parallel TTS methods typically keep branches isolated during search: intermediate discoveries remain branch-private and cannot guide other branches in time. This information isolation causes substantial redundant exploration, as branches repeatedly rediscover information already found elsewhere and require more search steps to collect complete decision information needed to reach correct answers. To bridge this gap, we propose \textbf{Collaborative Parallel Thinking (CPT)}, a training-free inference framework that enables search-time information sharing across parallel branches. CPT extracts compact intermediate information from ongoing branches, maintains a deduplicated query-level information pool, and broadcasts pool entries through the input context, allowing each branch in subsequent search steps to reuse discoveries made by other branches rather than rediscover the same information. Empirically, experiments on HMMT and AIME benchmarks show that CPT establishes a stronger accuracy--latency Pareto frontier than strong baselines across rollout budgets and model scales, highlighting search-time collaboration as an effective direction for efficient parallel TTS.

</details>


### [11] [Large Language Model-Powered Query-Driven Event Timeline Summarization in Industrial Search](https://arxiv.org/abs/2605.27066)
*Mingyue Wang,Xingyu Xie,Hang Yang,Li Gao,Lixin Su,Ge Chen,Dawei Yin,Daiting Shi*

Main category: cs.CL

TL;DR: QDET是百度搜索部署的查询驱动事件时间线摘要生产系统，通过多任务微调（7B模型）和强化学习摘要压缩，在时间线摘要任务上以1%参数达到DeepSeek-R1-671B水平，在线A/B测试显示CTR提升5.5%。


<details>
  <summary>Details</summary>
Motivation: 搜索引擎需要处理关于热点新闻的查询，理解事件随时间演变至关重要。传统主题中心方法追求全面覆盖，但用户查询往往聚焦具体事件，因此需要构建与查询密切相关的聚焦事件时间线。

Method: 提出QDET系统，包含两个关键创新：(1)多任务监督微调，结合时间排序、因果判断和时间线补全三个辅助任务，使紧凑模型在专业领域匹配大模型性能；(2)基于强化学习的事件简洁摘要生成，在严格长度约束下保持语义质量。

Result: 7B模型F1得分76.2%，略超DeepSeek-R1-671B的76.1%，参数量仅1%；长度合规率88.2%，约束满足超671B模型7.7个点；在线A/B测试CTR提升5.5%，停留时间延长4.6%，探索深度增加4.4%。

Conclusion: 领域特定优化可以在大幅降低计算成本的情况下，产出质量可媲美大模型的生产级模型；时间线理解能力可迁移到热度预测等下游任务。

Abstract: Understanding how events evolve over time is essential for search engines handling queries about trending news. We present QDET (Query-Driven Event Timeline Summarization), a production system deployed on Baidu Search that constructs focused event timelines to explain specific query events. Unlike traditional topic-centric approaches that aim for comprehensive coverage, QDET identifies and organizes sub-events closely relevant to the query from noisy candidate sets formed by millions of documents retrieved daily. QDET incorporates two key innovations: (1) multi-task supervised fine-tuning with three auxiliary tasks-temporal ordering, causal judgment, and timeline completion-that enable compact models to match the performance of much larger general-purpose models in specialized domains; (2) reinforcement learning-based event concise summarization that enforces strict length constraints while maintaining semantic quality, achieving 88.2% length compliance and outperforming 671B-scale models by 7.7 points in constraint satisfaction. Our fine-tuned 7B parameter model achieves 76.2% F1 score on timeline summarization, slightly surpassing the zero-shot performance of DeepSeek-R1-671B (76.1% F1) while using only 1% of its parameters-demonstrating that domain-specific optimization enables production-ready models with comparable quality at drastically reduced computational costs. Online A/B tests on Baidu Search validate real-world effectiveness, showing 5.5% CTR improvement, 4.6% longer dwell time, and 4.4% deeper exploration compared to single-task baselines. We further demonstrate that timeline understanding transfers to heat prediction, confirming effective knowledge transfer to downstream tasks.

</details>


### [12] [LLMs Are Already Good Tutors: Training-Free Prompt Optimization for Pedagogical Math Tutoring](https://arxiv.org/abs/2605.27088)
*Unggi Lee,Minchul Shin,Yeil Jeong,Sookbun Lee,Jeongsu Moon,Kyungtae Joo,Eunjoo Lee,Hoilym Kwon*

Main category: cs.CL

TL;DR: 本文探索了无需训练的提示词优化方法（仅通过API调用进化系统提示词）作为数学辅导场景中需要多GPU的RL训练方案的替代方案。在12种方法中，所有最佳配置均超越了最强RL基线，其中ParetoGrad在多个指标上取得了最佳平衡。行为分析显示训练无关方法更依赖教学知识模式，但意图级脚手架能力有所下降。


<details>
  <summary>Details</summary>
Motivation: 对齐LLM进行数学辅导通常需要基于RL的多GPU训练，成本高昂。探索是否可以通过仅优化系统提示词（训练无关、仅API调用）来实现同等甚至更好的效果，以降低计算开销。

Method: 改编7种已发布方法并提出5种教育专用方法，共12种提示优化方法，在5种条件下对2个OOD基准套件进行评估。使用82编码教育行为编码本进行行为分析。

Result: 所有12种方法的最佳配置均超越了最强RL基线（R_total=0.633）。ParetoGrad在后测解决率、泄漏控制和帮助性之间取得最佳帕累托平衡。训练无关方法的教学知识模式使用率是RL模型的2-3倍，但意图级脚手架降低约10个百分点。

Conclusion: 仅通过提示词优化即可高效开发教学对齐的LLM辅导系统，所需计算资源极少，可作为RL训练的有效替代方案。

Abstract: Aligning LLMs for math tutoring typically requires RL-based training with multi-GPU infrastructure. We investigate whether training-free prompt optimization-evolving only the system prompt via API calls-can serve as a practical alternative. We adapt 7 published methods and propose 5 education-specialized methods, evaluating these 12 methods under 5 conditions on 2 OOD benchmark suites. All 12 best-per-method configurations surpass the strongest RL-trained baseline (R_total = 0.633), and our ParetoGrad achieves the best Pareto balance across post-test solve rate, leak control, and helpfulness, rather than dominating any single component. Behavioral analysis with an 82-code educational codebook reveals that training-free methods rely on teaching-knowledge patterns at 2-3x the rate of RL-trained models, with a compensating ~10 percentage-point reduction in intent-level scaffolding. We also find a task-dependent reasoning mode effect consistent across training-free and RL-based paradigms. Our approach enables efficient development of pedagogically aligned LLM tutors with prompts alone and minimal compute.

</details>


### [13] [The Coverage Illusion: From Pre-retrieval Routing Failure to Post-retrieval Cascades in a Production RAG System](https://arxiv.org/abs/2605.27220)
*Zafar Hussain,Kristoffer Nielbo*

Main category: cs.CL

TL;DR: 本文研究了RAG流水线中查询增强（如HyDE和查询扩展）在实际生产流量中的必要性。通过对丹麦国家百科全书的案例研究，发现合成查询与真实用户查询之间存在结构分布差异（称为"覆盖幻觉"），合成查询认为90%以上需要LLM增强，而真实生产流量中仅27.8%需要。作者提出了一种后检索级联策略，按成本递增顺序执行工作流，仅在无文档返回时才升级到LLM增强，无需额外训练或基础设施，在质量提升的同时将延迟降低31.8%，并使72.2%的真实用户查询无需LLM增强即可完成。


<details>
  <summary>Details</summary>
Motivation: 现有RAG查询增强方法（HyDE、查询扩展）对每个查询都应用LLM，导致推理成本和延迟大幅增加，但在实际生产流量中这种开销的必要性缺乏实证研究。作者希望揭示合成查询评估与真实生产流量之间的差距，并设计更高效的检索策略。

Method: 基于丹麦国家百科全书的案例研究，评估五种检索工作流在20,000个查询-工作流对上的表现。提出"后检索级联"策略：按成本递增顺序执行工作流（先执行最便宜的方法），仅在当前步骤无文档返回时才升级到LLM增强，无需训练开销或辅助服务基础设施。

Result: 合成查询显示90%以上需要LLM增强，但真实生产流量中仅27.8%需要。后检索级联策略在质量上比Always-HyDE高出+0.140 Composite Overall分，延迟降低31.8%，72.2%的真实用户查询无需LLM增强。

Conclusion: 合成查询与真实查询之间存在"覆盖幻觉"——因分布结构不匹配导致对LLM增强需求的严重高估。预检索路由无法解决该问题（是否为LLM增强需检索后决策），后检索级联策略是一种高效、低成本的实际部署方案。

Abstract: In modern RAG pipelines, query augmentation methods such as HyDE and query expansion are applied to every query, resulting in substantial LLM inference costs and increased end-to-end latency. The empirical justification for this overhead in real production traffic remains largely unexplored. We present a case study of the Danish National Encyclopedia, evaluating five retrieval workflows over 20,000 query-workflow pairs from production traffic and synthetic conditions. In this system, synthetic queries suggest that LLM augmentation is needed for over 90% of queries to achieve high retrieval coverage. However, under our production deferral policy, only 27.8% of real user queries need LLM augmentation. We call this gap the Coverage Illusion and attribute it to a structural mismatch between synthetic and real query distributions. Pre-retrieval routing cannot resolve this gap, as the need for LLM augmentation is only revealed after searching the index, a result confirmed by our evaluation of four machine learning paradigms. The coverage gap, undetectable from the query alone, motivates a post-retrieval cascade that runs workflows in cheapest-first order and escalates to LLM augmentation only when a step returns no documents. Operating entirely without training overhead or secondary serving infrastructure, the cascade improves quality by +0.140 Composite Overall points over Always-HyDE, reduces latency by 31.8%, and serves 72.2% of real user queries without LLM augmentation.

</details>


### [14] [Pair-In, Pair-Out: Latent Multi-Token Prediction for Efficient LLMs](https://arxiv.org/abs/2605.27255)
*Wenhui Tan,Minghao Li,Xiaoqian Ma,Siqi Fan,Xiusheng Huang,Liujie Zhang,Ruihua Song,Weihang Chen*

Main category: cs.CL

TL;DR: 提出PIPO（Pair-In, Pair-Out）方法，统一了输入侧的隐层压缩和输出侧的多token预测（MTP），通过轻量级置信度头替代昂贵的验证器开销，在多种基准上实现了最高2.64倍的首token延迟加速和2.07倍的每token延迟加速。


<details>
  <summary>Details</summary>
Motivation: 现有方法将输入侧（隐层压缩）和输出侧（推测解码和多token预测）独立研究，且输出侧方法需要昂贵的验证器来校验MTP生成的不可靠草稿token。PIPO旨在统一两侧并移除验证器开销。

Method: 将隐层压缩器和MTP头视为镜像操作：压缩器将两个输入token折叠为一个隐层表示，MTP头将一个隐层状态展开为额外输出token。训练轻量级置信度头来判断草稿token是否应被接受，并通过On-Policy Distillation自然匹配推测解码的拒绝采样准则，以极低成本训练置信度头。

Result: 在AIME 2025、GPQA-Diamond、LiveCodeBench v6、LongBench v2上，基于Qwen3.5-4B和9B主干，PIPO将pass@4提升最多+7.15点，同时实现最高2.64倍首token延迟和2.07倍每token延迟加速。

Conclusion: PIPO统一了输入输出侧的优化，通过无验证器的轻量级机制实现了显著的推理加速效果。

Abstract: Long chain-of-thought reasoning has made autoregressive decoding the dominant inference cost of modern large language models. Existing methods target either the input side (latent compression) or the output side (speculative decoding and multi-token prediction, MTP), but the two lines of work have been pursued independently. Moreover, output-side methods must incur an expensive verifier pass to validate the unreliable draft tokens predicted by MTP. To address these issues, we propose \textbf{Pair-In, Pair-Out (PIPO)}, which unifies both sides by viewing a latent compressor and an MTP head as mirror-image operations: the compressor folds two input tokens into one latent representation, while the MTP head unfolds one hidden state into one additional output token. To remove the verifier cost without sacrificing reliability, PIPO trains a lightweight confidence head that decides whether draft tokens should be accepted. We observe that On-Policy Distillation (OPD) naturally matches the rejection-sampling criterion of speculative decoding, so the confidence head can be trained alongside OPD with negligible extra cost. Experiments on AIME 2025, GPQA-Diamond, LiveCodeBench v6, and LongBench v2 with Qwen3.5-4B and 9B backbones show that PIPO improves pass@4 over regular decoding by up to $+7.15$ points, while delivering up to $2.64\times$ first-token-latency and $2.07\times$ per-token-latency speedups.

</details>


### [15] [FinHarness: An Inline Lifecycle Safety Harness for Finance LLM Agents](https://arxiv.org/abs/2605.27333)
*Haoxuan Jia,Yang Liu,Bin Chong,Yingguang Yang,Yancheng Chen,Jiayu Liang,Qian Li,Hanning Lu,Kefu Xu,Hao Zheng,Chongyang Zhang,Hao Peng,Philip S. Yu*

Main category: cs.CL

TL;DR: FinHarness 是一个内联安全框架，通过查询监控、工具监控和级联模块，对金融 LLM 智能体进行端到端安全防护，在降低攻击成功率的同时保持良性审批率，并大幅减少高级判断调用次数。


<details>
  <summary>Details</summary>
Motivation: 金融领域的 LLM 智能体需要同时阻止提示注入导致的未授权操作和批准合法的多步业务流程，但现有边界过滤器容易遗漏中间步骤的关键工具调用，而事后 LLM 判断又无法及时干预且计算成本随轨迹长度线性增长。

Method: 提出 FinHarness，包含三个组件：Query Monitor 融合单轮意图与跨轮漂移检测；Tool Monitor 评估每个候选工具调用；Cascade 模块整合每步风险并自适应地在轻量级和高级 LLM 判断器之间路由验证。触发的风险因素作为事前证据重新注入智能体输入，使其自行拒绝、重新规划或批准。

Result: 在 FinVault 基准上，路由后的 FinHarness 将攻击成功率（ASR）从 38.3% 降至 15.0%，良性审批率从 41.1% 轻微下降至 39.3%，高级判断调用次数比始终使用高级判断的消融实验减少 4.7 倍。

Conclusion: FinHarness 能有效保障金融 LLM 智能体的安全性，在降低攻击成功率的同时保持良性审批率，并通过自适应路由大幅降低计算开销。

Abstract: Finance LLM agents must simultaneously block prompt-induced unauthorized actions and approve legitimate multi-step business workflows. However, boundary filters often miss irreversible mid-trajectory tool calls, while post-hoc LLM judges perform auditing only after termination -- too late for intervention and at a computational cost that scales linearly with trace length. We present FinHarness, an inline safety harness that wraps a finance agent end-to-end with three components: a Query Monitor that fuses single-turn intent with cross-turn drift, a Tool Monitor that evaluates each prospective tool call, and a Cascade module that integrates per-step risk and adaptively routes verification between a lightweight and an advanced-tier LLM judge. Fired risk factors are re-injected into the agent input as ex-ante evidence, enabling the agent to refuse, re-plan, or approve on its own. On FinVault, routed FinHarness cuts ASR from 38.3% to 15.0% while largely preserving benign approval ($41.1\% \to 39.3\%$), and uses $4.7\times$ fewer advanced-judge calls than an always-advanced ablation.

</details>


<div id='cs.SE'></div>

# cs.SE [[Back]](#toc)

### [16] [Tool-Schema Compression Enables Agentic RAG Under Constrained Context Budgets](https://arxiv.org/abs/2605.26165)
*Furkan Sakizli*

Main category: cs.SE

TL;DR: 系统性研究了Agentic RAG系统中工具定义与上下文窗口之间的资源冲突问题，提出工具模式压缩（TSCG）方法，在8K上下文预算下将精确匹配率提升最高+24.7pp，且在32K下效果持平，展示了压缩作为必要基础设施层的价值。


<details>
  <summary>Details</summary>
Motivation: Agentic RAG系统中，语言模型需同时容纳工具定义和检索增强生成内容，但工具模式占用大量上下文窗口，导致关键资源冲突，此前缺乏系统性研究。

Method: 在6,566次受控API调用中评估14个模型（1.5B-32B参数及前沿API模型），跨越8K/16K/32K三种上下文预算、28个工具定义，应用TSCG保守模式压缩（节省44-50%模式token），并在HotpotQA上进行外部验证和前沿扩展测试。

Result: 8K下JSON模式导致上下文溢出，平均EM仅2.6%，压缩后恢复RAG功能，平均提升+20.5pp（六模型完整启用者达+24.7pp）；32K下两者差异≤1pp；HotpotQA上+48pp EM；压缩后工具容量从~494个扩展至800+个。

Conclusion: 工具模式压缩是受约束上下文部署中Agentic RAG的必要基础设施层，效果仅由预算驱动，开源了所有代码、数据和检查点。

Abstract: Agentic RAG systems that equip language models with dozens to hundreds of tool definitions face a critical resource conflict: tool schemas consume the same context window needed for retrieval-augmented generation. We present the first systematic study of this tool-context trade-off, evaluating 14 models spanning 1.5B-32B local models plus one frontier API model across 6,566 controlled API calls at three context budgets (8K, 16K, 32K) with 28 tool definitions. Applying TSCG conservative-profile compression (44-50% schema token savings), we observe a binary enablement effect: at 8K tokens, JSON-schema tool definitions overflow the context window entirely, yielding near-zero EM (2.6% average), while compressed schemas restore RAG functionality with +20.5 pp average exact-match lift across all eight models (+24.7 pp among the six exhibiting full enablement). At 32K -- where both formats fit -- four of five tested models show delta <= 1 pp, confirming the effect is purely budget-driven. External validation on HotpotQA (50 multi-hop questions) shows +48 pp EM under the same overflow scenario. Frontier scaling tests demonstrate that JSON schemas overflow at ~494 tools while compressed schemas remain operational beyond 800 tools. Our results establish tool-schema compression as a necessary infrastructure layer for agentic RAG in constrained-context deployments. All code, data, and checkpoints are publicly available.

</details>


### [17] [SetupX: Can LLM Agents Learn from Past Failures in Functionality-Correct Code Repository Setup?](https://arxiv.org/abs/2605.26186)
*Zihang Zhou,Ziqian Ren,Yukai Wu,Yingjie Xiong,Wei Zhou,Chao Peng,Dong Zhang,Bingheng Yan,Xuanhe Zhou,Fan Wu*

Main category: cs.SE

TL;DR: SetupX 是一个基于经验学习的仓库环境配置框架，通过自进化经验表示、投机执行和检察官-法官验证协议，自动化解决跨仓库环境配置中的依赖冲突、构建失败等问题，在基准测试中达到92%的通过率。


<details>
  <summary>Details</summary>
Motivation: 现有 LLM Agent 在仓库环境配置任务中面临三大挑战：跨仓库经验迁移困难、非可逆状态变更下的多步试错修复、以及难以区分配置失败与仓库本身缺陷。当前方法缺乏有效的经验复用和鲁棒验证机制。

Method: SetupX 包含三个核心组件：(1) 自进化经验表示（XPU），一种双模态知识单元，编码设置信号、文本指导和可执行动作，支持跨仓库动态迁移；(2) 经验增强的投机执行，基于 LIFO Docker 快照栈实现安全回滚；(3) 检察官-法官验证协议，将证据收集与最终判断分离，实现更可靠的设置验证。

Result: 在精心构建的基准测试中，SetupX 达到92%的通过率，比最强基线高出19%以上。特别是在涉及多个容器间协调服务的复杂多仓库配置场景中表现优异。

Conclusion: SetupX 通过经验学习和结构化验证机制，有效解决了自动化仓库环境配置中的关键难题，为跨仓库环境搭建提供了可迁移、可回滚、可验证的解决方案。

Abstract: Functionality-correct repository setup aims to configure execution environments (e.g., dependencies, build scripts) to successfully execute a repository's documented features. It presents significant challenges due to diverse, repository-specific failures, including dependency incompatibilities, missing toolchains, incomplete installations, and verification-strategy mismatches. Existing LLM agents struggle to robustly resolve these issues, specifically failing to support (1) cross-repository experience transfer, (2) multi-step trial-and-repair under non-invertible state changes, and (3) robust verification of setup outcomes to distinguish setup-induced failures from repository bugs. To address this, we introduce SetupX, an experiential learning-based setup framework. First, we construct a Self-Evolving Experience Representation (XPU), a dual-modality knowledge unit encoding setup signals, textual guidance, executable actions to dynamically transfer verified environment fixes to unseen repositories. Second, we employ Experience-Augmented Speculative Execution backed by a LIFO Docker snapshot stack, enabling the agent to proactively trial fixes and safely roll back to known-good states. Third, we introduce a Prosecutor-Judge Verification Protocol that separates evidence collection from final judgment, enabling more reliable setup verification beyond superficial build-time metrics. Evaluation results on carefully-crafted benchmarks show SetupX achieves highest performance (e.g., 92% pass rate) and outperforms the strongest baseline by over 19%. Crucially, SetupX excels in complex multi-repository setup requiring coordinating multiple interconnected services across different containers. The code repository is available at https://github.com/OpenDataBox/SetupX.

</details>


### [18] [Verus-SpecGym: An Agentic Environment for Evaluating Specification Autoformalization](https://arxiv.org/abs/2605.26457)
*Anmol Agarwal,Natalie Neamtu,Pranjal Aggarwal,Seungone Kim,Jannis Limperg,Cedric Flamant,Kanna Shimizu,Bryan Parno,Sean Welleck*

Main category: cs.SE

TL;DR: 本文提出Verus-SpecBench基准和Verus-SpecGym智能体环境，研究LLM智能体能否将非正式编程问题自动形式化为正确规范。最强模型Gemini 3.1 Pro解决77.8%任务，但规范自动形式化仍然脆弱，且LLM裁判会遗漏26%的失败案例。


<details>
  <summary>Details</summary>
Motivation: AI编码智能体生成的代码即使通过形式验证，也无法保证形式规范本身与用户意图一致。因此需要研究规范自动形式化——LLM能否将非正式编程问题翻译为忠实的形式规范。

Method: 构建Verus-SpecBench（581个来自Codeforces的规范编写任务，面向Rust验证器Verus）和Verus-SpecGym智能体环境；扩展Verus的exec_spec机制使生成的规范可作为Rust代码执行；利用官方Codeforces测试和"hack"对抗案例进行评估。

Result: Gemini 3.1 Pro解决77.8%任务，其他前沿模型51.1-57.8%，开源模型仅21.5-25.5%。模型生成的规范会遗漏重要输入假设、接受错误输出、拒绝有效输出。LLM裁判遗漏26%的失败案例。

Conclusion: 规范自动形式化对前沿智能体已可触及但仍然脆弱，即使在模型能生成正确代码的问题上也是如此。

Abstract: AI coding agents are increasingly used to write real-world software, but ensuring that their outputs are correct remains a fundamental challenge. Formal verification offers a promising path: an agent generates code together with a machine-checked proof, guaranteeing that the code satisfies a formal specification. However, there is no guarantee that the formal spec itself matches the user's intent. In this work, we study specification autoformalization: whether LLM agents can translate informal programming problems into faithful formal specifications. We introduce Verus-SpecBench, a benchmark of 581 spec-writing tasks derived from Codeforces problems targeting Verus, a verifier for Rust, and Verus-SpecGym, an agentic environment in which models interact with Verus, bash, & the filesystem to develop these specs. The central challenge is evaluation: expert-written reference specs are expensive to write, & LLM judges can miss subtle mistakes. We address this by (a) extending Verus's exec_spec mechanism so that generated specs can be executed as Rust code, & (b) testing them against official Codeforces tests & adversarial cases extracted from Codeforces "hacks", which are edge cases written by competitors to break incorrect solutions. On Verus-SpecBench, the strongest model, Gemini 3.1 Pro, solves 77.8% of tasks, other frontier models solve 51.1--57.8% & OSS models reach only 21.5--25.5%. Our analysis of failure modes shows that model-generated specs can omit important input assumptions, accept incorrect outputs, & reject valid ones. We also find that LLM-as-a-judge evaluation misses 26% of the failures our evaluator catches. Overall, our results suggest that spec autoformalization is within reach for frontier agents but remains brittle even on problems where they can already generate correct code. The code, data, & logs can be found at https://github.com/formal-verif-is-cool/verus-spec-gym

</details>


### [19] [FuzzPilot: Plateau-Triggered Recipe Validation for Structured Text Fuzzing](https://arxiv.org/abs/2605.26539)
*Zhiyi Yao*

Main category: cs.SE

TL;DR: FuzzPilot 是一个 AFL++ 的控制器，通过在覆盖率平台期时使用快照-评估-推广机制（包括基于语言模型的候选突变配方）来加速模糊测试，但在 cJSON 饱和目标上实验结果显示覆盖率提升不显著，指标差异无统计学意义。


<details>
  <summary>Details</summary>
Motivation: AFL++ 等模糊测试工具在突变热路径上进行昂贵推理会降低性能，且覆盖率容易陷入平台期，需要一种能离线进行智能突变策略搜索并仅推广有效策略的控制器。

Method: FuzzPilot 在覆盖率平台期快照语料库，生成候选突变配方（JSON 格式，包含算子权重、字节范围、语料选择规则、字典令牌等），在短时 AFL++ 微竞速中评估，仅推广正验证奖励的配方。配方可来自本地规则或 LLM 代理（结合 Ghidra 反编译上下文）。

Result: 在 cJSON 饱和目标上（269 边覆盖率天花板），FuzzPilot 相比基线 AFL++ 吞吐量保持约 1.06x，中位平台期描述性缩短（1,384s vs 2,532s），但 Mann-Whitney p=0.42 不显著。20 个模型提案配方全部因零奖励未被推广。

Conclusion: FuzzPilot 当前的平台期缩短更可能来自快照重启机制而非模型或配方突变器，本文应视为一份可审计的实现报告和后续非饱和目标评估的基线。

Abstract: FuzzPilot is a controller for AFL++ that moves expensive reasoning out of the mutation hot path. When coverage plateaus, it snapshots the corpus, prepares candidate mutation recipes, evaluates them in short isolated AFL++ micro-campaigns, and promotes only recipes with positive validation reward. Recipes are JSON data, not generated code: a native custom mutator consumes operator weights, byte ranges, corpus-selection rules, and dictionary tokens. Candidate recipes can come from local rules or from a language-model agent, with Ghidra-derived constants and decompiled context as target hints. This preprint reports a deliberately narrow cJSON evaluation. We compare vanilla AFL++ and the full FuzzPilot agent over five 14,400 s repetitions per arm. cJSON is saturated: baseline AFL++ reaches the exposed 269-edge ceiling at a median of about 2,500 s. The experiments therefore do not show that language-model proposals improve coverage or generalize beyond cJSON. Within this scope, FuzzPilot preserves throughput (median execs_per_sec about 1.06x baseline), shows a descriptively shorter median plateau (1,384 s versus 2,532 s), but the difference is not statistically significant at N=5 (Mann-Whitney p=0.42). The validation gate evaluated 20 model-proposed recipes and promoted none because all rewards were zero. The observed plateau reduction is more likely due to controller snapshot and restart machinery than to the model or recipe mutator. This version is best read as an auditable implementation report and baseline for ongoing non-saturated-target evaluation.

</details>


### [20] [TrajAudit: Automated Failure Diagnosis for Agentic Coding Systems](https://arxiv.org/abs/2605.26563)
*Minxing Wang,Xiaofei Xie,Yintong Huo*

Main category: cs.SE

TL;DR: 提出了TrajAudit，首个面向仓库级编程轨迹的故障诊断框架，通过信息过滤和先验知识生成两个模块辅助调查者智能体处理噪声长上下文，在RootSE基准测试中定位准确率提升超24.4个百分点，token消耗降低至少18%。


<details>
  <summary>Details</summary>
Motivation: 现有自动化故障诊断方法在处理仓库级编程任务时，轨迹包含大量噪声（冗余结构、冗长代码上下文）且长度极长，而LLM的长上下文推理能力有限，导致诊断效果随轨迹长度和复杂度增加显著下降。

Method: TrajAudit包含一个调查者智能体，由两个模块支持：一是通过模式匹配和关键词检测过滤故障无关信息；二是从测试失败报告中生成初步诊断作为先验知识，帮助处理噪声长上下文。调查者可按需调用工具检索过滤后的内容，保留关键信息同时最小化噪声。

Result: 在包含93个真实故障实例的RootSE基准上，TrajAudit定位准确率超所有基线24.4个百分点以上，token消耗减少至少18%。

Conclusion: 该工作为智能体软件工程中的故障管理提供了基础性资源和研究方向，展示了在复杂仓库级编程轨迹诊断上的实用效果。

Abstract: Agentic systems have been widely studied to automate software engineering jobs such as bug fixing. As these systems increasingly tackle complex tasks, understanding where and why they fail becomes essential for iterative refinement and operational reliability. Existing automated failure diagnosis approaches leverage task execution trajectories, yet their effectiveness degrades substantially as trajectory length and complexity increase. For repository-level coding tasks specifically, trajectories are laden with noise, such as redundant program structure and verbose code context. Moreover, these trajectories are very long, while long-context reasoning remains a known weakness of LLMs.
  To address these two challenges, we propose TrajAudit, the first failure diagnosis framework for repository-level coding trajectories. TrajAudit employs an investigator agent supported by two modules: one filters failure-irrelevant information through pattern matching and keyword detection, and the other generates a preliminary diagnosis from test failure reports as prior knowledge, helping the agent handle noisy long contexts. The investigator agent can further invoke tools to retrieve filtered content on demand, ensuring that critical information is preserved while noise is minimized. We also introduce RootSE, a benchmark of 93 real-world agentic failure instances sourced from software maintenance tasks, representing the most complex trajectory diagnosis benchmark to date. Experiments on RootSE show that TrajAudit outperforms all existing baselines by over 24.4 percentage points in localization accuracy, while reducing token consumption by at least 18%, demonstrating its practical effectiveness. We hope this work draws community attention to failure management in agentic software engineering and provides a foundational resource for future research.

</details>


### [21] [Energy-Aware Decision Making in Software Stack Upgrades](https://arxiv.org/abs/2605.26609)
*Mirko Stocker,Michael Wahler*

Main category: cs.SE

TL;DR: 本文提出了一个系统性的方法，用于测量核心软件栈组件（如Spring Boot和JVM）不同版本对应用程序能耗的影响。通过基于Spring Petclinic REST应用的案例研究，发现不同版本组合下能耗差异显著，Java 21/23引入的虚拟线程可带来显著节能效果。


<details>
  <summary>Details</summary>
Motivation: 软件栈升级对能耗的影响尚不明确，而能源消耗是组织追求可持续发展时日益关注的问题，当前缺乏系统性的测量方法。

Method: 提出了自动化基准测试方法，评估框架版本、运行时版本和执行平台的不同组合，并通过Spring Petclinic REST应用作为案例进行实证测量。

Result: 不同Spring Boot和JVM版本组合下能耗差异显著，存在意外的能效回归；Java 21/23的虚拟线程无需修改应用即可实现大幅节能。

Conclusion: 软件升级可以显著影响能耗，测量能耗为软件维护和演进中的决策提供了有价值的依据。

Abstract: Software stack upgrades are a routine part of software maintenance and evolution, typically motivated by improved performance, stability, or functionality. Yet their impact on energy consumption - a growing concern for organizations pursuing sustainability - remains poorly understood. This paper presents a systematic method for measuring how different versions of core software stack components, such as Spring Boot and the Java Virtual Machine (JVM), influence the energy consumption of applications. Our approach evaluates combinations of framework versions, runtime versions, and execution platforms through automated benchmarking. Using a case study based on the Spring Petclinic REST application, we show that energy consumption varies substantially across Spring Boot and JVM versions, in some cases producing unexpected regressions. Notably, newer JVM releases and virtual threads (introduced in Java 21 and 23) yielded significant energy savings without requiring application changes. These results demonstrate that software upgrades can meaningfully affect energy usage and that measuring energy consumption provides valuable evidence for decision making in software maintenance and evolution.

</details>


### [22] [HTMLCure: Turning Browser Experience into State Guided Repair for Interactive HTML](https://arxiv.org/abs/2605.26807)
*Jiajun Wu,Jian Yang,Tuney Zheng,Wei Zhang,Haowen Wang,Yihang Lou,Xianglong Liu*

Main category: cs.SE

TL;DR: HTMLCure是一个浏览器体验框架，通过交互式执行、状态诊断和闭环修复，将LLM生成的HTML页面从表层正确提升为可交互、可用的高质量页面，在多个benchmark上达到接近强参考系统的性能。


<details>
  <summary>Details</summary>
Motivation: LLM生成的HTML页面往往只是表面正确，在滚动、悬停、点击、缩放等交互操作下会失效，现有方法依赖截图评估无法捕获这些失败，且简单过滤会丢弃大量可修复的页面。

Method: HTMLCure包含评估器和修复引擎：评估器在不同视口和交互状态下执行页面，记录确定性浏览器证据，为VLM提供执行轨迹中的关键帧；修复引擎诊断当前页面，选择特定于状态的修复策略，执行候选方案并导出质量清除的页面用于SFT。

Result: 在97K提示语料上，将可用种子扩展到63703个高质量页面，最终构建40K精炼SFT集。HTMLCure-27B-Refined在HTMLBench-400上达到50.6分，45.2%确定性测试通过率，与Kimi-K2.6和GPT-5.4等强参考系统处于同一性能区间；在MiniAppBench上达到81.2平均分，比原始27B SFT提升15.3分。

Conclusion: 通过交互式评估和闭环修复机制，可以有效提升LLM生成HTML页面的实际可用性，为大规模高质量SFT数据构建提供了可行方法。

Abstract: LLMs can now produce full HTML pages, but many of those pages are only superficially correct: they render once, then fail under scroll, hover, click, resize, or gameplay. Evaluation from screenshots can miss these failures, and filtering discards many pages that are still repairable. We introduce HTMLCure, a browser experience framework that evaluates HTML after the system has interacted with it. The evaluator executes the page across viewports and interaction states, records deterministic browser evidence, and gives the VLM curated keyframes from the executed trajectory rather than isolated screenshots. The same state signal drives a closed loop repair engine: HTMLCure diagnoses the current page, chooses a state specific repair family, runs each candidate again, and exports quality cleared pages for SFT. On a 97K prompt corpus, this expands the directly usable seed into a candidate pool of 63703 quality cleared pages, from which we construct the final refined SFT set of 40K pages. Under the same backbone and training recipe, HTMLCure-27B-Refined reaches 50.6 on HTMLBench-400 with 45.2% deterministic test case pass, placing it in the same performance band as strong reference rows such as Kimi-K2.6 and GPT-5.4. On the released MiniAppBench validation split, it reaches 81.2 average, improving raw 27B SFT by 15.3 points and approaching the level of strong reference systems.

</details>


### [23] [EviACT: An Evidence-to-Action Framework for Agentic Program Repair](https://arxiv.org/abs/2605.27238)
*Qianru Meng,Xiao Zhang,Zhaochun Ren,Joost Visser*

Main category: cs.SE

TL;DR: 提出了EviACT框架，通过协调三个证据驱动的护栏（检索支架、编译门、测试驱动门）来增强基于LLM智能体的自动程序修复，在多个基准测试上提升了修复率并降低了API成本。


<details>
  <summary>Details</summary>
Motivation: 现有的基于智能体的自动程序修复系统难以有效利用执行证据来指导缺陷定位、补丁生成和验证，导致修复效果和效率受限。

Method: 提出EviACT框架，包含三个证据驱动的护栏：检索支架用于定位修复上下文，编译门用于过滤无效编辑，测试驱动门在完整回归测试前检查目标测试恢复情况，三者协同工作形成证据到行动的链条。

Result: 在四个基准测试上，EviACT相比最强基线提升修复率1.6-6.0个百分点，API成本降低70.1-88.6%。

Conclusion: 协调的证据到行动链使得基于智能体的自动程序修复更加有效和高效。

Abstract: LLM-based agents have moved automated program repair (APR) from fixed-context patch generation to interactive repository-level repair. However, existing agentic APR systems still struggle to use execution evidence to guide localization, patch generation, and validation. We propose EviACT (Evidence-to-Action), an agentic APR framework that coordinates three evidence-driven guardrails across repair stages. The retrieval scaffold grounds repair context, the compile gate filters invalid edits, and the test-driven gate checks target-test recovery before full regression. Across four benchmarks, EviACT improves resolve rate over the strongest reported comparable baselines by 1.6-6.0 percentage points and shows 70.1-88.6% lower reported per-bug API cost where baseline costs are available. Ablations and diagnostics suggest that these gains are associated with the coordinated evidence-to-action chain, making agentic APR more effective and efficient.

</details>


### [24] [Governed Evolution of Agent Runtimes through Executable Operational Cognition](https://arxiv.org/abs/2605.27328)
*Mariano Garralda-Barrio*

Main category: cs.SE

TL;DR: 本文提出一个用于多智能体系统的治理化运行时演化框架，将智能体生成的代码制品视为持久化运行时能力，并通过HarnessMutation机制实现受控的、可审计的、可回滚的运行时自适应演化。


<details>
  <summary>Details</summary>
Motivation: 现有智能体系统将代码视为可执行的操作基板而非一次性输出，但缺乏对这些制品的治理、生命周期管理和操作演化的规范框架。

Method: 形式化智能体生成制品为持久化运行时能力，提出HarnessMutation作为受治理的生命周期感知运行时自适应机制，支持显式验证、可追溯、评估和回滚约束。

Result: 给出了一个概念性框架，展示如何将受控演化作为有界、可观察的过程在持久化操作记忆上运行，并可在现代智能体运行时而向治理的编排系统上操作化。

Conclusion: 提供了自适应性基础设施的概念基础，使其演化保持显式、可审计和受约束，推动了智能体系统从无限制自修改向可治理运行时演化的转变。

Abstract: Recent advances in agentic systems increasingly treat code as an executable operational substrate rather than as a disposable output artifact. Prior work such as \emph{Code as Agent Harness} frames validated agent-generated artifacts as runtime entities that can be created, executed, revised, persisted, and reused within long-running cognitive loops. However, the governance, lifecycle management, and operational evolution of such artifacts remain under-specified.
  This paper proposes a framework for governed runtime evolution in multi-agent systems through executable operational cognition. We formalize agent-generated artifacts as persistent runtime capabilities that progressively become part of the operational substrate rather than transient intermediate outputs. Building on this perspective, we introduce \emph{HarnessMutation} as a governed mechanism for lifecycle-aware runtime adaptation operating under explicit validation, traceability, evaluation, and rollback constraints.
  Rather than treating runtime adaptation as unrestricted self-modification, the proposed framework models evolution as a bounded and observable process over persistent operational memory. It further shows how these ideas can be operationalized over modern agent runtimes and governance-oriented orchestration systems, providing a conceptual foundation for adaptive infrastructures whose evolution remains explicit, auditable, and constrained.

</details>


<div id='cs.AI'></div>

# cs.AI [[Back]](#toc)

### [25] [Is Agent Memory a Database? Rethinking Data Foundations for Long-Term AI Agent Memory](https://arxiv.org/abs/2605.26252)
*Abdelghny Orogat,Essam Mansour*

Main category: cs.AI

TL;DR: 本文提出长期AI代理记忆应作为数据管理工作负载，而非简单存储。形式化为GEM（受控演化记忆），包含四个状态级算子（摄入、修订、遗忘、检索）和六个正确性条件，证明记录级系统无法满足这些条件，并给出原型MemState。


<details>
  <summary>Details</summary>
Motivation: 现有代理记忆系统和数据库范式将记忆视为存储，分别将正确性定位于记录、嵌入或边，每种只满足部分长期记忆需求，导致四种失败模式：无节制增长、缺少语义修订、容量驱动遗忘、只读检索。

Method: 形式化GEM抽象，定义四个状态级算子替代记录级操作，提出六个正确性条件约束状态演化；通过三个结构性观察证明记录级系统无法满足条件；在属性图后端实现原型MemState。

Result: MemState原型验证了GEM的可行性，并暴露出原生引擎的差距；指出记忆中心数据管理作为工作负载的三个研究方向。

Conclusion: 长期AI代理记忆需要全新的数据管理范式，GEM提供了正确性框架，但需要原生引擎实现才能完全满足需求。

Abstract: Long-running AI agents need persistent memory. Memory supports learning across sessions, reduces repeated context injection, and enables auditing of past decisions. Current agent memory systems and database paradigms treat memory as storage. They localize correctness at records, embeddings, or edges. Each supplies only some of the capabilities that long-term memory requires. The result is four recurring failure modes: unregulated growth, missing semantic revision, capacity-driven forgetting, and read-only retrieval. In our vision, long-term agent memory is a new data-management workload. Its correctness is a property of the state trajectory, not of individual records. We formalize this as Governed Evolving Memory (GEM). GEM replaces record-level database operations with four state-level operators: ingestion, revision, forgetting, and retrieval. Six correctness conditions govern how the state evolves. Three structural observations establish that no record-level system can satisfy these conditions, regardless of the storage model. We realize the abstraction in MemState, a prototype on a property-graph backend. MemState validates feasibility and exposes the gap to a native engine. We outline three research directions that define memory-centric data management as a workload.

</details>


### [26] [Your Agents Are Aging Too: Agent Lifespan Engineering for Deployed Systems](https://arxiv.org/abs/2605.26302)
*Jianing Zhu,Yeonju Ro,John Robertson,Kevin Wang,Junbo Li,Haris Vikalo,Aditya Akella,Zhangyang Wang*

Main category: cs.AI

TL;DR: 提出AgingBench基准，用于评估长寿命AI代理在部署后的可靠性退化问题，通过四种老化机制和诊断工具分析代理的长期表现。


<details>
  <summary>Details</summary>
Motivation: 现有基准只评估模型初始化时的性能，忽略了长寿命AI代理在持续运行中因状态变化导致的可靠性退化问题。

Method: 设计AgingBench，包含四种老化机制（压缩、干扰、修订、维护），使用时间依赖图和反事实探针生成内存流水线的诊断画像。

Result: 在7个场景、14个模型、多种内存策略下进行约400次运行（8-200轮会话），发现代理老化是多维的：行为测试可能正常但事实精度下降，派生状态跟踪可能急剧崩溃，同一错误答案需要不同修复策略。

Conclusion: 可靠的代理部署需要生命周期评估、机制级诊断和阶段针对性修复，而不仅仅是更强的初始模型。

Abstract: Long-lived AI agents are increasingly deployed as persistent operational systems, yet they are still evaluated like freshly initialized models. Day-one benchmarks miss a basic systems question: how long does an agent remain reliable after deployment? Even when model weights are frozen, an agent's effective state keeps changing as it compresses interaction history, retrieves from a growing memory store, revises facts after updates, and undergoes routine maintenance. Reliability therefore becomes a lifespan property of the full agent harness, not only a snapshot property of the base model. We introduce AgingBench, a longitudinal reliability benchmark for agent lifespan engineering: measuring not only whether deployed agents degrade, but what form the degradation takes and where repair should target. AgingBench organizes agent aging into four mechanisms: compression aging, interference aging, revision aging, and maintenance aging. To diagnose these failures, AgingBench uses temporal dependency graphs and paired counterfactual probes that produce diagnostic profiles for the write, retrieval, and utilization stages of the memory pipeline. Across 7 scenarios, 14 models, multiple memory policies, and both runner-controlled and autonomous agents, over ~400 runs spanning 8 - 200 sessions show that agent aging is not one-dimensional: behavioral tests can remain clean while factual precision decays; derived-state tracking can collapse sharply within a single model; and the same wrong answer can require different repairs depending on what the diagnostic profile points to. These results suggest that reliable agent deployment requires lifespan evaluation, mechanism-level diagnosis, and stage-targeted repair, not only stronger day-one models.

</details>


### [27] [Anchor: Mitigating Artifact Drift in Agent Benchmark Generation](https://arxiv.org/abs/2605.26321)
*Maksim Ivanov,Abhijay Rana*

Main category: cs.AI

TL;DR: 本文提出了Anchor，一个将领域专家对业务流程的规范形式化为约束优化程序的任务生成管线，能联合生成自然语言指令、环境配置、可验证的解决方案和状态验证器。基于此构建了ERP-Bench基准测试，包含300个长周期企业资源规划任务，并评估了前沿模型的表现。


<details>
  <summary>Details</summary>
Motivation: 当前AI Agent的企业工作训练和评估环境难以平衡真实性、可验证性和规模，且存在"工件漂移"问题（指令、环境、预言机和验证器不一致），导致环境不可解、可奖励篡改或不一致。

Method: Anchor管线将领域专家的业务流程规范形式化为约束优化程序，从单个参数化规范中联合生成自然语言指令、环境配置、求解器认证的基准解和基于状态的验证器。通过调整参数生成难度可控且有已知最优解的任务。

Result: 构建了ERP-Bench基准（300个长周期采购和制造任务），前沿模型在26.1%的试验中满足显式任务约束，但仅17.4%达到完全最优解。生成参数可以预测实际难度。

Conclusion: Anchor和ERP-Bench提供了构建可审计评估环境的具体方法，用于评估具有经济价值的AI Agent工作。

Abstract: AI agents are beginning to complete valuable, long-horizon business operations tasks, but training and evaluation environments for enterprise work still struggle to balance realism, verifiability, and scale. Environment and task creation frequently suffers from a failure mode we call artifact drift: when instructions, environments, oracles, and verifiers are created by loosely coupled processes, they frequently disagree on what a task requires, producing environments that are unsolvable, reward-hackable, or inconsistent. We introduce Anchor, a task-generation pipeline that formalizes domain experts' specifications of business workflows into constraint optimization programs. From a single parametric specification, the pipeline jointly produces a natural-language instruction, environment configuration, solver-certified ground-truth solution, and state-based verifier. With Anchor, altering parameters yields new tasks with controlled difficulty and known optimal solutions, producing harness-agnostic environments whose rewards depend solely on end-state business correctness. We apply Anchor to produce ERP-Bench: a benchmark of 300 long-horizon tasks spanning procurement and manufacturing workflows in a production-grade ERP system. We find that generation parameters predict realized difficulty, and that frontier models satisfy explicit task constraints in 26.1% of trials but reach a fully optimal solution in only 17.4% of trials. Overall, we show that Anchor and ERP-Bench offer a concrete recipe for building auditable evaluation environments for economically valuable agent work. We release the task generator and ERP-Bench dataset at erpbench.ai

</details>


### [28] [JobBench: Aligning Agent Work With Human Will](https://arxiv.org/abs/2605.26329)
*Yuetai Li,Yichen Feng,Zhangchen Xu,Zixian Ma,Kaiyuan Zheng,Fengqing Jiang,Xinghua Sun,Rulin Shao,Zichen Chen,Yue Huang,Xinyang Han,Brian Lee,Kayla Xu,Shenglai Zeng,Hang Hua,Xiangliang Zhang,Basel Alomair,Ranjay Krishna,Luke Zettlemoyer,Pang Wei Koh,Bhaskar Ramasubramanian,Luyao Niu,Xiang Yue,Radha Poovendran*

Main category: cs.AI

TL;DR: JobBench是一个评估AI Agent在真实职业工作流中执行高优先级委派任务能力的基准测试，覆盖35个职业的130个任务，最强模型（Claude Opus 4.7 + Claude Code）仅达45.9%。


<details>
  <summary>Details</summary>
Motivation: 现有AI Agent基准测试主要基于经济价值（GDP）来衡量，讲述的是"替代人类"的故事。作者希望从人类实际需求出发，评估AI Agent完成专家认为最值得委派的任务的能力，推动研究方向从"替代"转向"增强"。

Method: JobBench覆盖35个职业的130个Agentic任务，每个任务包含异构参考文件的工作空间，要求Agent在杂乱信息流中推理。输出由基于事实的链式评分标准（平均每任务35.6个二元标准）进行评分。

Result: 评估了36个模型，最强模型Claude Opus 4.7在Claude Code下仅达到45.9%的得分，说明当前AI Agent在真实职业工作流中的能力仍有很大提升空间。

Conclusion: 希望JobBench能推动社区将劳动力市场效应目标从"替代"转向"增强"：构建做人类真正想要委派的任务的Agent，而不仅仅是做最具经济价值的任务。

Abstract: Current benchmarks for occupational AI agents are scoped primarily by economic values, telling a replacement story. We introduce JobBench, which evaluates AI agents on the workflows that experts identify as high-priority for delegation, empowering humans based on their needs instead of replacing them with GDP value. JobBench covers 130 agentic tasks across 35 occupations. Each task is packaged as a workspace of heterogeneous reference files, requiring the agent to reason through the cluttered information streams of real professional work. Outputs are graded by a fact-anchored chain of rubrics, averaging 35.6 binary criteria per task. We evaluate 36 models; the strongest, Claude Opus~4.7 under Claude Code, reaches only 45.9 %. We hope JobBench shifts the community's target labour-market effect from replacement to enhancement: building agents that do what humans actually want delegated, not only what is most economically valuable.

</details>


### [29] [The MiniMax-M2 Series: Mini Activations Unleashing Max Real-World Intelligence](https://arxiv.org/abs/2605.26494)
*MiniMax,:,Aili Chen,Aonian Li,Baichuan Zhou,Bangwei Gong,Binyang Jiang,Boji Dan,Changqing Yu,Chao Wang,Cheng Ma,Cheng Zhong,Cheng Zhu,Chengjun Xiao,Chengyi Yang,Chengyu Du,Chenyang Zhang,Chi Zhang,Chuangyi Huang,Chunhao Zhang,Chunhui Du,Chunyu Zhao,Congchao Guo,Da Chen,Deming Ding,Dianjun Sun,Dongyu Zhang,Enhui Yang,Fei Yu,Guang Zheng,Guodong Zheng,Guohong Li,Haichao Zhu,Haigang Zhou,Haimo Zhang,Han Ding,Hao Zhang,Haohai Sun,Haolin Lyu,Haonan Lu,Haoyu Wang,Huajie Shi,Huiyang Li,Jiacheng Chen,Jian Zhang,Jiaqi Zhuang,Jiaren Cai,Jiaxin Pan,Jiayao Li,Jiayuan Song,Jichuan Zhang,Jie Wang,Jihao Gu,Jin Zhu,Jingwei Dong,Jingyang Li,Jingyu Zhang,Jingze Zhuang,Jinhao Tian,Jinli Liu,Jinyi Hu,Jun Tao,Jun Zhang,Junbin Ruan,Junhao Xu,Junjie Yan,Junteng Liu,Junxian He,Kang Xu,Ke Ji,Ke Yang,Kecheng Xiao,Keyu Duan,Keyu Li,Le Han,Letian Ruan,Li Yuan,Lianfei Yu,Liheng Feng,Lijie Mo,Lin Li,Lingye Bao,Lingyu Yang,Lingyuan Zhou,Loki,Lu Chen,Lunbin Ceng,Ming Li,Ming Zhong,Mingliang Tao,Mingyuan Chi,Mujie Lin,Nan Hu,Ningxin Chen,Peiyin Zhu,Peng Gao,Pengcheng Gao,Pengfei Li,Penglin Li,Pengyu Zhao,Qibin Ren,Qidi Xu,Qihan Ren,Qile Li,Qin Wang,Quanliang Chen,Qunhong Ceng,Rong Tian,Rui Dong,Ruitao Leng,Ruize Zhang,Shanqi Liu,Shaoyu Chen,Sheng Jia,Shun Yao,Shuoran Zhao,Shuqi Yu,Sichen Li,Sicheng Pan,Songquan Zhu,Tengfei Li,Tian Xie,Tiancheng Qin,Tianrun Liang,Wei Liu,Weiqi Xu,Weitao Li,Weixiang Chen,Weiyu Cheng,Weiyu Zhang,Wenhu Chen,Wenqian Zhao,Xiancai Chen,Xiangjun Song,Xiangyuan Wang,Xiao Luo,Xiao Su,Xiaobo Li,Xiaodong Han,Xiaojie Wu,Xihao Song,Xingyi Han,Xinyu Guan,Xuan Lu,Xun Zou,Xunhao Lai,Xutong Li,Yan Gong,Yang Wang,Yang Xu,Yangsen Wang,Ye Tang,Yicheng Chen,Yinran Qiu,Yiqi Shi,Yiting Guo,Yiwen Huang,Yixuan Wang,Yongyi Hu,Yu Gao,Yu Zhang,Yuanxiang Ying,Yuanzhen Zhang,Yubo Wang,Yuchen Song,Yufeng Yang,Yuhang Meng,Yuhang Miao,Yuhao Li,Yujie Liu,Yulin Hu,Yunan Huang,Yunji Li,Yunyi Huang,Yusen Zhang,Yusu Hong,Yutao Xie,Yutong Zhang,Yuwen Liao,Yuxuan Shi,Yuze Wenren,Zebin Li,Zehan Li,Zejian Luo,Zeyu Jin,Zeyuan Sun,Zhanpeng Zhou,Zhaochen Su,Zhendong Li,Zhengmao Zhu,Zhengyuan Peng,Zhenhua Fan,Zhi Zhang,Zhichao Xu,Zhiheng Lv,Zhikang Xu,Zhitao He,Zhiwei He,Zhongyuan Li,Zibo Gao,Zijia Wu,Zijian Song,Zijian Zhou,Zijun Sun,Zishan Huang,Ziying Chen,Ziyue Ge*

Main category: cs.AI

TL;DR: MiniMax-M2 系列是一个混合专家语言模型家族，总参数 229.9B 但每 token 仅激活 9.8B。M2 系列端到端设计用于智能体部署，包含三个核心组件：智能体驱动的数据管道、可扩展的智能体原生强化学习系统 Forge，以及最新的 M2.7 检查点实现了自我进化能力。该系列在智能体编程、深度搜索、办公任务和推理基准上达到前沿水平。


<details>
  <summary>Details</summary>
Motivation: 通过"小激活"（mini activations）来实现最大的实际智能，降低推理成本同时保持高性能，特别是面向智能体（agentic）场景的部署需求。

Method: 提出 MiniMax-M2 系列 MoE 模型（229.9B 总参数，9.8B 激活参数），核心包括：(i) 智能体驱动的数据管道，生成大规模可验证的智能体轨迹；(ii) Forge 系统，可扩展的智能体原生强化学习系统，包含窗口式 FIFO 调度、前缀树合并、推理优化以及训练-推理-智能体解耦；(iii) M2.7 检查点实现自我进化，可自动调试训练运行并修改自身 scaffold。

Result: M2 系列在智能体编程、深度搜索、办公任务和推理基准上达到前沿（frontier-tier）性能，尽管激活参数极少（仅 9.8B）。

Conclusion: 通过精心设计的 MoE 架构和智能体原生训练系统，可以在很低的计算激活成本下实现顶尖的智能体任务性能，验证了"小激活释放大智能"的设计理念。

Abstract: We introduce the MiniMax-M2 series, a family of Mixture-of-Experts language models built around the principle that mini activations can unleash maximum real-world intelligence. The flagship M2 contains 229.9B total parameters with only 9.8B activated per token. Designed end-to-end for agentic deployment, the M2 series rests on three components: (i) agent-driven data pipelines producing large-scale, verifiable trajectories across agentic coding and agentic cowork, each grounded in an executable workspace and an artifact-aligned reward; (ii) Forge, a scalable agent-native RL system that adapts to long-horizon agent trajectories, paired with windowed-FIFO scheduling, prefix-tree merging, inference optimization, and a clean training-inference-agent decoupling that supports both white-box and black-box agents; (iii) the latest M2.7 checkpoint takes an early step toward self-evolution -- autonomously debugging training runs and modifying its own scaffold. Across M2 through M2.7, this combination translates a mini-activation footprint into frontier-tier performance on agentic coding, deep search, office-task, and reasoning benchmarks.

</details>


### [30] [MobileExplorer: Accelerating On-Device Inference for Mobile GUI Agents via Online Exploration](https://arxiv.org/abs/2605.26546)
*Runxi Huang,Liyu Zhang,Shengzhong Liu,Xiaomin Ouyang*

Main category: cs.AI

TL;DR: MobileExplorer是一个通过在线探索加速设备端视觉移动GUI代理推理的框架，利用VLM长推理时间并行探索UI元素，采用两级回滚机制确保执行可靠性，将探索轨迹总结为上下文提示注入下一推理步骤，在AndroidWorld基准测试中将推理步骤和端到端延迟平均降低23%，任务成功率提升最高5%。


<details>
  <summary>Details</summary>
Motivation: 现有移动GUI代理大多依赖云端模型推理，存在隐私风险和网络延迟，完全设备端部署的移动GUI代理尚未被充分探索。

Method: 利用VLM每步推理的空闲时间，并行轻量级探索UI元素，记录探索轨迹为结构化记忆；设计两级回滚机制以在实时移动环境中可靠恢复初始UI状态；将探索轨迹总结为上下文提示注入下一推理步骤。

Result: 在AndroidWorld基准测试及更复杂任务中，推理步骤和端到端延迟平均降低23%，任务成功率维持或提升最高5%。

Conclusion: MobileExplorer通过在线探索有效加速了设备端移动GUI代理的推理，兼顾了效率与准确性，推动了完全设备端部署的可行性。

Abstract: Mobile graphical user interface (GUI) agents enable AI models to autonomously operate smartphones on behalf of users. However, most existing systems focus primarily on optimizing task accuracy and rely on cloud-hosted models for inference, which introduces privacy concerns and network-dependent latency. As a result, fully on-device deployment of mobile GUI agents remains underexplored. We propose MobileExplorer, a new framework that accelerates on-device inference for vision-based mobile GUI agents via online exploration. The key idea is to exploit the long per-step reasoning time of vision-language models (VLMs) by performing lightweight, parallel exploration of UI elements. During model inference, the agent proactively probes semantically relevant UI elements and records these exploration traces as structured memory. To ensure reliable execution in live mobile environments, we design a two-level rollback mechanism that robustly restores the initial UI state when a fast but naive backtracking strategy fails. The collected exploration traces are then summarized into concise contextual hints and injected into the prompt to enhance the subsequent reasoning step. We evaluate MobileExplorer on multiple off-the-shelf devices using the AndroidWorld benchmark, as well as newly designed, more complex tasks and dynamic on-device environments. MobileExplorer reduces the average number of reasoning steps and end-to-end latency by 23\%, while maintaining or improving task success rates by up to 5\%. A video demonstration of MobileExplorer performance in the real world is available at https://youtu.be/thK7MJmdlvM .

</details>


### [31] [AGORA: Adapter-Grounded Observation-Action Retention for Inference-Free Prompt Compression in LLM Agents](https://arxiv.org/abs/2605.26596)
*Haoran Zhang,Zhaohua Sun*

Main category: cs.AI

TL;DR: 本文揭示通用token级压缩器在LLM Agent场景下会导致"动作语法破坏"（action-grammar destruction）——压缩器会优先丢弃携带动作语义的token（标识符、括号、动作动词），导致环境拒绝执行。提出AGORA，一种推理无关的step级压缩方法，结合结构提示解析器、格式关键内容保留机制和基于反事实动作标签训练的125M参数相关性评分器，在9个测试单元格中8个保留≥75%的未压缩性能。


<details>
  <summary>Details</summary>
Motivation: 现有token级压缩器在LLM Agent场景中完全失效（平均奖励≤0.05），需要设计适合Agent场景的上下文压缩方法。

Method: AGORA是一种推理无关的step级压缩器，包含三个组件：1）结构提示解析器用于识别对话结构；2）始终保留格式关键和最近关键内容的保留下限（structural floor）；3）125M参数的相关性评分器，基于反事实动作标签（next-action-change labels）训练，每个step仅需约2ms。

Result: 在9个测试单元格中，AGORA是唯一在8个单元格中保留≥75%未压缩性能的方法（唯一例外为73%）；四组件消融实验表明结构保留下限是质量主导因素，学习型评分器可从单一固定保留比实现1.0-11.5x自适应端到端压缩。

Conclusion: Agent场景需要step粒度的压缩策略，token级通用压缩器因破坏动作语义而完全不可用；AGORA证明了结合结构保留和学习型评分的step级压缩是有效解决方案。

Abstract: The token-level extractive compressors widely used for general LM context are structurally inappropriate for LLM agents: across 17 (env, backbone, method) cells spanning two independent token-level method families, every cell collapses to mean reward <= 0.05 despite 1.3-13.3x realized compression. We name and characterize this failure mode as action-grammar destruction -- the tokens carrying action semantics (identifiers, brackets, action verbs) are exactly those self-information ranks lowest, so a general-purpose compressor reliably removes them and the environment rejects the residual. The diagnosis points to step-granularity compression. We introduce AGORA, an inference-free step-level compressor combining a structural prompt parser, an always-keep floor for format- and recency-critical content, and a 125M-parameter relevance scorer trained on counterfactual next-action-change labels (~2ms/step, zero per-step LLM toll). Across the compared inference-free and LLM-based methods, AGORA is the only one retaining >= 75% uncompressed performance in 8 of 9 cells (with the lone exception at 73%); a four-way component ablation isolates the structural floor as the dominant quality lever and the learned scorer as the source of 1.0-11.5x adaptive end-to-end compression from a single fixed keep ratio.

</details>


### [32] [Tail-Aware HiFloat4: W4A4 Post-Training Quantization for Wan2.2](https://arxiv.org/abs/2605.26628)
*Zhanfeng Feng,Shuai Guo,Xin Di,Long Peng,Yang Cao,Zhengjun Zha*

Main category: cs.AI

TL;DR: 本文针对低比特文生视频量化挑战，提出Tail-Aware HiFloat4方法，基于ViDiT-Q后训练量化框架适配Wan2.2，采用HiFloat4格式对主线性层进行W4A4伪量化，保留敏感边界模块高精度，并引入激活尾部感知的百分位校准模块构建通道掩码，配合PTQ状态恢复，减少稀有校准异常值影响，保持运行时HiFloat4算术和采样流程不变。


<details>
  <summary>Details</summary>
Motivation: 低比特量化（如W4A4）在文生视频模型部署中可显著降低内存和计算开销，但现有量化方案对异常激活值敏感，导致生成质量下降。本文旨在解决HiFloat4格式下稀有校准异常值对量化精度的影响。

Method: 将ViDiT-Q后训练量化框架适配到Wan2.2，使用HiFloat4数值格式；对Wan2.2 transformer的主线性层进行W4A4伪量化，数值敏感边界模块保持高精度；引入激活尾部感知的百分位校准模块用于通道掩码构建；结合紧凑的PTQ状态恢复技术。

Result: 在低比特文生视频量化挑战赛中提交的方案，通过尾部感知校准减少了异常值影响，保持HiFloat4运行时算术和采样流程不变。

Conclusion: Tail-Aware HiFloat4方案在保持HiFloat4运行时效率的同时，有效缓解了稀有校准异常值对量化的负面影响，适用于Wan2.2的低比特部署。

Abstract: This report describes Tail-Aware HiFloat4, our submission to the low-bit text-to-video generation quantization challenge. Our method adapts the public ViDiT-Q post-training quantization pipeline to Wan2.2 under the HiFloat4 numerical format. We quantize the main linear layers in both Wan2.2 transformer modules with W4A4 HiFloat4 fake quantization, keep numerically sensitive boundary modules in high precision, and introduce an activation-tail-aware percentile calibration module for channel-mask construction. Together with compact PTQ-state restoration, this design reduces the influence of rare calibration outliers while keeping the runtime HiFloat4 arithmetic and sampling pipeline unchanged.

</details>


### [33] [Neuro-Symbolic Verification of LLM Outputs for Data-Sensitive Domains (extended preprint)](https://arxiv.org/abs/2605.26942)
*Paul Sigloch,Christoph Benzmüller*

Main category: cs.AI

TL;DR: 提出了一种混合验证架构，结合形式化符号方法和神经语义分析，用于检测LLM生成内容中的幻觉、不一致和隐私漏洞，在医疗设备损害评估报告中验证了有效性。


<details>
  <summary>Details</summary>
Motivation: LLM在高风险领域存在可靠性挑战（幻觉、不一致、隐私漏洞），而基于提示的自验证方法继承了导致幻觉的分布偏差，无法提供可靠保证。

Method: 构建混合验证架构：输入验证使用形式化符号逻辑推理提供可判定的结构化需求保证；输出验证使用嵌入的语义相似性检测上下文幻觉；采用并行的基于actor的流水线实现两者分离。

Result: 在HAIMEDA医疗设备损害评估系统中，结构化实体幻觉检测率超过83%，语义虚构检测率72%，报告创建时间减少30%。

Conclusion: 神经符号架构可以为LLM在数据敏感领域的部署提供原则性的安全保障。

Abstract: LLMs deployed in high-stakes domains face fundamental reliability challenges: hallucinations, inconsistencies, and privacy vulnerabilities introduce unacceptable risks where errors carry legal, financial, or safety consequences. This paper presents a hybrid verification architecture combining formal symbolic methods with neural semantic analysis to provide complementary guarantees for LLM-generated content. This architecture employs logical reasoning for input verification, leveraging completeness properties to provide decidable guarantees on structured requirements. For output validation, embedding-based semantic similarity detects contextual hallucinations where formal methods lack expressiveness. This separation is realized in a parallel, actor-based pipeline, addressing limitations of prompt-based self-verification approaches, which inherit the distributional biases that produce hallucinations. The proposed architecture and type-aware verification method are validated with HAIMEDA, a real-world medical device damage assessment reporting system developed through Action Design Research. Evaluation shows hallucination detection rates of over 83% for structured entities and 72% for semantic fabrications, with a 30% reduction in report creation time, demonstrating that neuro-symbolic architectures can provide principled safeguards for LLM deployment in data-sensitive domains.

</details>


### [34] [UnityMAS-O: A General RL Optimization Framework for LLM-Based Multi-Agent Systems](https://arxiv.org/abs/2605.26646)
*Yiqun Chen,Wei Yang,Erhan Zhang,Shijie Wang,Qi Liu,Zechun Niu,Bin Zhang,Haitao Li,Rui Li,Lingyong Yan,Jinyuan Feng,Biqing Qi,Xiaochi Wei,Yan Gao,Yi Wu,Yao Hu,Jiaxin Mao*

Main category: cs.AI

TL;DR: UnityMAS-O是一个面向LLM多智能体系统的通用RL优化框架，将完整工作流作为优化单元，支持逻辑角色、图轨迹、用户定义奖励和智能体-模型映射四类核心对象，通过Ray星型拓扑运行时扩展verl，实现角色级/轮次级/轨迹级的多层级奖励分配和灵活的参数共享，在检索增强QA、迭代搜索和代码生成任务上验证了效果。


<details>
  <summary>Details</summary>
Motivation: 现有LLM多智能体系统依赖人工编排（提示、工具、控制规则），缺乏统一的RL优化接口；现有RL后训练框架主要面向单策略优化，缺少对多智能体工作流、结构化交互、角色级信用分配和可配置参数共享的抽象支持。

Method: 1) 将完整工作流而非单条响应/策略轨迹作为优化单元；2) 通过四类一等对象（逻辑智能体角色、图轨迹、用户定义奖励、智能体-模型映射）解耦逻辑智能体与物理模型参数；3) 支持完全共享/完全分离/部分共享三种参数共享模式；4) 在verl上扩展Ray星型拓扑运行时：中央控制器执行工作流/调用工具/记录轨迹/组装奖励，模型本地worker组负责rollout、缓冲、优势计算和分布式PPO更新。

Result: 在Natural Questions、HotPotQA和代码任务上，多智能体RL优化后的手动工作流性能提升明显，小模型和严格代码全部通过指标上增益尤为显著。

Conclusion: UnityMAS-O可作为可复用基础框架，将多样化的LLM多智能体工作流转化为可训练的多智能体RL系统。

Abstract: LLM-based multi-agent systems decompose complex tasks into interacting roles, but most remain manually orchestrated by prompts, tools, and control rules, while agents are rarely optimized through a unified reinforcement learning interface. Existing RL post-training frameworks mainly target single-policy optimization and lack abstractions for user-defined multi-agent workflows, structured interaction, role-specific credit assignment, and configurable parameter sharing.
  We present UnityMAS-O, a general RL optimization framework for LLM-based multi-agent systems. UnityMAS-O treats the complete workflow as the optimization unit, rather than a single response or policy trajectory. It represents workflows through four first-class objects: logical agent roles, graph trajectories, user-defined rewards, and agent--model mappings. This decouples logical agents from physical model parameters, supporting full sharing, full separation, and partial sharing, with rewards assigned at role, turn, and trajectory levels.
  UnityMAS-O extends verl with a Ray-based star-topology runtime. A central controller executes workflows, invokes tools, records structured trajectories, and assembles rewards; model-local worker groups handle rollout, buffering, advantage computation, and distributed PPO-style updates. Users can define agents, workflows, model mappings, and rewards without rewriting the optimization infrastructure.
  We instantiate UnityMAS-O on retrieval-augmented QA, iterative agentic search, and reflective code generation. Across Natural Questions, HotpotQA, and held-out code tasks, multi-agent RL improves manually specified workflows after optimization, with especially large gains for smaller models and strict code all-passed metrics. These results show that UnityMAS-O can serve as a reusable substrate for converting diverse LLM-based multi-agent workflows into trainable multi-agent RL systems.

</details>


### [35] [Towards Feedback-to-Plan Decisions for Self-Evolving LLM Agents in CUDA Kernel Generation](https://arxiv.org/abs/2605.26720)
*Yee Hin Chong,Jiaming Wu,Youhui Zhang,Peng Qu*

Main category: cs.AI

TL;DR: 本文提出 CUDAnalyst，一个用于分析 LLM 作为自进化智能体生成 CUDA 内核时，规划决策如何归因于各类反馈信号的分析框架。通过轨迹冻结和选择性反馈注入实现生成级归因，发现显式规划仅在反馈对齐时有效，有效规划源于结构化多反馈交互。


<details>
  <summary>Details</summary>
Motivation: 现有端到端消融实验无法区分规划决策中不同反馈信号的贡献，因为迭代规划会放大早期扰动并将反馈效果与轨迹依赖漂移混淆。

Method: 通过轨迹冻结（trajectory freezing）和选择性反馈注入（selective feedback injection）实现受控的生成级归因，采用联盟式归因方法评估反馈效果及其交互作用。

Result: 显式规划仅在反馈对齐时有益；有效规划来自结构化多反馈交互；强推理模型的高层规划可部分迁移到弱模型；这些趋势在多个骨干网络和工作负载上保持一致。

Conclusion: 识别出的反馈到规划的结构在受控轴上是鲁棒的，为理解 LLM 智能体在代码生成中的规划行为提供了分析基础。

Abstract: Large language models (LLMs) have shown strong empirical gains as self-evolving agents for CUDA kernel generation, driven by feedback-conditioned planning across generations. However, how planning decisions attribute and combine heterogeneous feedback signals remains opaque. Standard end-to-end ablations fail to resolve this question, as iterative planning amplifies early perturbations and conflates feedback effects with trajectory-dependent drift.
  We introduce \texttt{CUDAnalyst}, a unified analysis layer for controlled, generation-level attribution of planning decisions to feedback components via trajectory freezing and selective feedback injection. \texttt{CUDAnalyst} enables stable generation-level evaluation and principled coalitional-style attribution of feedback effects and interactions. Our results show that explicit planning is beneficial only when feedback is aligned, that effective planning emerges from structured multi-feedback interactions, and that high-level plans from stronger reasoning models can partially transfer to weaker ones. These trends hold across reference backbones, representative workloads, and reference induction regimes, indicating that the identified feedback-to-plan structure is robust within the controlled axes studied.

</details>


### [36] [It's Not the Capability: Harness Sensitivity Is Non-Monotone Across LLM Agent Tiers](https://arxiv.org/abs/2605.26731)
*Yong-eun Cho*

Main category: cs.AI

TL;DR: 通过432次实验，测试了6个模型在4个能力层级下配合3种约束条件（宽松、平衡、严格）的表现，推翻了"模型能力越强所需约束越少"的单调逆关系假设。发现严格约束对前沿对话模型反而降低性能（约束复杂度悖论），但对推理模型效果最佳。引入了六标签故障分类法，并提出了按模型层级选择约束的实用指南。


<details>
  <summary>Details</summary>
Motivation: 检验LLM智能体部署中的一个普遍假设：更强的结构化约束总能提升可靠性，且能力越强的模型需要的约束越少（即模型能力层级与最优约束复杂度之间存在单调逆关系）。

Method: 在HEAT-24（一个包含24个任务、基于git工作区验证的合成基准）上，进行了432次控制实验，交叉了6个模型（覆盖4个能力层级）和3种约束条件（宽松、平衡、严格）。

Result: 1) 前沿对话模型Gemini 2.5 Flash上，增加约束详细程度导致VTSR下降29-38个百分点（约束复杂度悖论）；2) 前沿推理模型Qwen3.5-122B上，严格约束达到最高VTSR（91.7%）和最低延迟；3) 2B小模型Gemma4:e2B在所有约束下均达到91.7%的稳定性，匹配强开源模型水平。

Conclusion: 模型能力层级与最优约束复杂度之间不存在单调逆关系；约束敏感性取决于模型类型（对话型vs推理型）而非单纯的能力层级；提出了六标签故障分类法（格式违规主导高能力模型故障，错误文件主导低能力模型故障）和按模型层级选择约束的实用指南。

Abstract: A prevalent assumption in LLM agent deployment holds that more structured harnesses universally improve
  reliability, and that higher-capability models need proportionally less structural guidance -- together
  implying a monotone inverse relationship between model capability tier and optimal harness complexity. We
  test this hypothesis through a controlled 432-run experiment crossing six models across four capability
  tiers with three harness conditions (light, balanced, strict) on HEAT-24, a 24-task synthetic benchmark
  with git-based workspace verification. Our results refute the monotone inverse relationship on two
  fronts. First, for the frontier chat model evaluated (Gemini 2.5 Flash), increased harness verbosity
  lowers VTSR by 29-38 percentage points -- a harness-complexity paradox. Second, for the frontier
  reasoning model evaluated (Qwen3.5-122B, extended thinking enabled), strict harness achieves the highest
  VTSR (91.7%) and the lowest latency, the opposite of the prediction. Within the constrained tier, a 2B
  model (Gemma4:e2B) matches strong-open-tier stability at 91.7% across all harnesses. Because each tier is
  represented by a single model in this study, these results should be interpreted as model-specific
  observations; harness sensitivity appears non-monotone across the models evaluated, and depends
  critically on model type (chat vs. reasoning). We introduce a six-label failure taxonomy showing that
  format_violation dominates capable-model failures while wrong_file dominates low-capability failures, and
  we derive practical tier-aware harness selection guidelines.

</details>


### [37] [Helicase: Uncertainty-Guided Supply Chain Knowledge Graph Construction with Autonomous Multi-Agent LLMs](https://arxiv.org/abs/2605.26835)
*Yunbo Long,Haolang Zhao,Ge Zheng,Alexandra Brintrup*

Main category: cs.AI

TL;DR: 提出Helicase，一个面向供应链知识图谱构建的自主多智能体LLM系统，能够将高层供应链查询分解为可执行调查计划，协调网络搜索、推理和代码智能体，增量构建带有不确定性标注的供应链知识图谱，并引入三层不确定性框架（动作层、轨迹层、记忆层）来追踪不确定性，同时提出了SCQA基准来评估推理能力。


<details>
  <summary>Details</summary>
Motivation: 供应链中的关键信息任务不是简单的一次性查询，而是需要跨复杂、碎片化网络资源进行多跳推理的结构化推断问题，且决策过程需要感知不确定性（信心的校准、可追溯性）。现有LLM多智能体系统主要用于知识检索和报告生成，缺乏支持供应链这种结构化推断和不确定性感知的能力。

Method: Helicase采用自主多智能体框架，将高层查询分解为可执行调查计划，协调专门的网络搜索、推理和编码智能体，通过迭代验证循环运作，增量构建查询特定的供应链知识图谱，并为每个事实标注不确定性。其三层不确定性框架在动作层、轨迹层和记忆层追踪不确定性。同时提出SCQA基准，包含80个供应链查询，覆盖单跳/多跳推理和高/低数据可见性四个象限。

Result: 提出了Helicase系统及其三层不确定性框架，以及SCQA基准来评估自主推理能力。该论文主要聚焦于方法设计和基准构建，未在摘要中提供具体实验数值结果。

Conclusion: Helicase填补了LLM多智能体系统在结构化、不确定性感知的供应链知识图谱构建方面的能力空白，实现了从简单查询到复杂多跳推理的自主推断，并通过SCQA基准支持全复杂度谱系的评估。

Abstract: LLM-based multi-agent systems have been widely adopted for knowledge retrieval and report generation, synthesizing known information through web search and textual reasoning. However, many critical information tasks in supply chains are not simple one-shot queries: they are structural inference problems requiring multi-hop reasoning across complex, fragmented web resources. Questions such as \textit{``Which Tesla components use lithium from Australian mines?''} have no answer in any single document; answers must be computationally synthesized through the autonomous construction and analysis of dynamic knowledge graphs assembled from fragmented, heterogeneous sources. Moreover, such discovery processes must be uncertainty-aware: decisions depend not only on answers but on calibrated confidence in their reliability, traceable to source quality and reasoning consistency. To address this capability gap, we propose \textit{Helicase}, an autonomous multi-agent LLM system for uncertainty-guided supply chain knowledge graph construction. \textit{Helicase} decomposes high-level supply-chain queries into executable investigation plans, coordinates specialized web-search, reasoning, and coding agents through iterative verification loops, and incrementally constructs query-specific supply chain knowledge graphs with per-fact uncertainty annotations. Its three-layer uncertainty framework tracks uncertainty at the action, trajectory, and memory layers, enabling both structural inference and calibrated confidence assessment. To evaluate autonomous reasoning across the full complexity spectrum, we introduce SCQA (Supply Chain Query Assessment), a benchmark of 80 supply chain queries organized into four quadrants spanning single-hop to multi-hop inference under both high and low data visibility.

</details>


### [38] [Generating Robust Portfolios of Optimization Models using Large Language Models](https://arxiv.org/abs/2605.27013)
*Eleni Straitouri,Cheol Woo Kim,Milind Tambe*

Main category: cs.AI

TL;DR: 提出一种利用LLM同时作为随机生成器和推理评估器，生成优化模型组合的算法，以保证在人类参与下至少有一个高质量候选模型可用，从而克服单次LLM生成的不可靠性问题。


<details>
  <summary>Details</summary>
Motivation: 构建忠实于现实的优化模型通常需要稀缺的领域知识和优化专业知识；LLM虽可从自然语言生成候选模型，但单次输出不可靠，需要更鲁棒的方法。

Method: 将同一个LLM赋予两种角色——随机生成器（产生多个候选模型）与推理评估器（评估候选质量），形成统一框架；理论保证只要生成器或评估器中至少一个与人类偏好对齐，组合中必包含高质量候选。

Result: 在多个优化建模任务上实证表现出强性能，组合策略有效提升了模型生成的可靠性。

Conclusion: 通过生成-评估互补框架，LLM生成的优化模型组合可实现可靠决策，支持人在回路中审阅多个候选后选择最优。

Abstract: Mathematical optimization is a powerful tool for structured decision-making across domains such as resource allocation and planning. Formulating optimization models faithful to reality, though, remains a significant bottleneck as it typically demands both domain expertise and optimization knowledge that are often scarce. Recent advances in large language models (LLMs) promise to bridge this gap, enabling the generation of candidate optimization models from natural language descriptions. However, there is no guarantee that any single LLM-generated model is reliable, and existing approaches that output only one model are therefore risky. In this work, we propose a novel algorithm that generates a portfolio of optimization models, designed to be robust to the limitations of LLMs. Our method exploits the observation that a single LLM can play two distinct roles $\unicode{x2014}$ as a stochastic generator and as a reasoning evaluator $\unicode{x2014}$ and proposes a unified framework that leverages both capabilities in a complementary manner. We provide theoretical guarantees showing that, as long as either the generator or the evaluator is well-aligned with human preferences, the portfolio is guaranteed to contain high-quality candidates, enabling a principled human-in-the-loop process in which a decision-maker can review multiple candidates before committing to one. We further validate our approach empirically, demonstrating strong performance across a range of optimization modeling tasks.

</details>


### [39] [Query Symbolically or Retrieve Semantically? A Dataset and Method for Semi-Structured Question Answering](https://arxiv.org/abs/2605.27164)
*Mateusz Czyżnikiewicz,Ryszard Tuora,Adam Kozakiewicz,Tomasz Ziętkiewicz,Mateusz Galiński,Michał Godziszewski,Michał Karpowicz,Timothy Hospedales,Cristina Cornelio*

Main category: cs.AI

TL;DR: DualGraph 是一个 RAG 框架，通过文本知识图谱（语义检索）和符号知识图谱（符号查询）两种互补视图来表示半结构化文档，在购物网站产品文档基准上优于现有方法。


<details>
  <summary>Details</summary>
Motivation: 现有 RAG 系统在处理半结构化语料时，纯语义检索无法支持精确过滤、聚合等操作，而符号方法在处理含噪自然语言时又过于脆弱，亟需一种能结合两者优势的框架。

Method: 提出 DualGraph 框架，构建 Textual Knowledge Graph（文本知识图谱）用于语义检索，Symbolic Knowledge Graph（符号知识图谱）用于对类型化的 subject-predicate-object 三元组进行符号查询，并提供多种策略来选择和组合语义与符号证据。

Result: 在 SpecsQA 基准（来自商业购物网站的半结构化产品文档）上，DualGraph 在开放性和规格导向性问题类型上均优于最先进的密集检索、GraphRAG、符号方法和表格导向基线。

Conclusion: 结合语义检索与符号查询的混合框架能有效提升半结构化文档上的 RAG 问答性能，代码和数据已开源。

Abstract: Retrieval-Augmented Generation (RAG) systems for question answering typically retrieve evidence by semantic similarity between the query and document chunks. While effective for unstructured text, this approach is less reliable on semi-structured corpora where answering may require exact filtering, aggregation, or exhaustive retrieval over structured attributes across multiple documents. Symbolic approaches support such operations, but they are often brittle on noisy natural-language corpora. We address this gap with DualGraph, a RAG framework that represents documents through two complementary views: a Textual Knowledge Graph for semantic retrieval and a Symbolic Knowledge Graph for symbolic querying over typed subject--predicate--object triples. Building on these two components, we provide multiple strategies for selecting or combining semantic and symbolic evidence.We also introduce SpecsQA, a benchmark from a commercial shopping website with semi-structured product documents and manually curated questions spanning open-ended and specification-oriented retrieval. Experiments show that DualGraph consistently outperforms state-of-the-art dense-retrieval, GraphRAG, symbolic, and table-oriented baselines across question types.Code and data are available at https://github.com/corneliocristina/DualGraphRAG.

</details>


### [40] [SIA: Self Improving AI with Harness & Weight Updates](https://arxiv.org/abs/2605.27276)
*Prannay Hebbar,Yogendra Manawat,Samuel Verboomen,Alesia Ivanova,Selvam Palanimalai,Kunal Bhatia,Vignesh Baskaran*

Main category: cs.AI

TL;DR: 提出SIA（自改进循环框架），让一个语言模型智能体（Feedback-Agent）同时更新任务专用智能体的脚手架（工具、提示、重试逻辑等）和模型权重，实现自我改进。在中文法律罪名分类、GPU内核优化、单细胞RNA去噪三个领域验证，效果显著优于单一维度改进。


<details>
  <summary>Details</summary>
Motivation: 当前AI改进中人类是瓶颈，而"脚手架更新"和"测试时训练"两条研究路线相互孤立。作者希望打破这种隔离，提出一种能同时更新脚手架和权重的自改进循环，让人工智能真正实现自我提升。

Method: SIA框架包含一个Feedback-Agent（语言模型智能体），在每次任务执行后根据反馈同时更新：(1) 任务智能体的脚手架（工具、提示、重试逻辑、搜索过程）；(2) 任务智能体的模型权重（通过类似测试时训练的RL pipeline）。两者协同迭代。

Result: 在三个领域均优于仅迭代脚手架：中文法律罪名分类提升56.6%，GPU内核优化运行时间减少91.9%，单细胞RNA去噪提升502%。

Conclusion: 同时更新脚手架和权重能互补增效：脚手架更新让模型更"智能体化"（塑造搜索和行动方式），权重更新建立提示或脚手架无法灌输的领域直觉。两个维度结合优于任一单一维度。

Abstract: Humans are the bottleneck in building and improving AI. Both the models and the agents that wrap them are written, tuned, and corrected by people. The long-horizon goal of an AI that can figure out how to improve itself remains open. Two largely disjoint research lines attack this bottleneck. The harness-update school has a meta-agent rewrite the scaffold of a task-specific agent (its tools, prompts, retry logic, and search procedure) while the model weights are held fixed. The test-time training school uses hand-written RL pipelines to update the model's own weights on task feedback while the harness is held fixed. These two silos operate in isolation. We propose SIA, a self-improving loop in which a language-model agent (the Feedback-Agent) updates both the harness and the weights of a task-specific agent. We evaluate across three contrasting domains: Chinese legal charge classification, low-level GPU kernel optimisation, and single-cell RNA denoising. Combining both levers outperforms scaffold iteration alone on all three benchmarks. The gains are 56.6% on LawBench, 91.9% runtime reduction on GPU kernels, and 502% on denoising over the initial baseline. Harness updates make the model agentic, shaping how it searches and acts, while weight updates build the domain intuition that no prompt or scaffold can instil.

</details>


### [41] [Natural Language Query to Configuration for Retrieval Agents](https://arxiv.org/abs/2605.27361)
*Melissa Z. Pan,Negar Arabzadeh,Mathew Jacob,Fiodar Kazhamiaka,Esha Choukse,Matei Zaharia*

Main category: cs.AI

TL;DR: 提出BRANE框架，利用LLM将查询转换为工作负载特征，训练轻量级预测器来为每个查询动态选择检索管道配置（LLM、检索器、文档数、跳数、合成策略），在保证准确率的同时降低最高89%的成本，推动成本-质量帕累托前沿。


<details>
  <summary>Details</summary>
Motivation: 现有检索代理的配置通常为整个工作负载手动调优一次，忽略了每个查询在成本和准确性上的优化潜力。需要一种能在推理时针对每个查询动态选择最优配置的方法。

Method: BRANE使用LLM将查询转换为工作负载特定特征，然后为每个管道配置训练一个轻量级预测器，估计该配置能否正确回答查询。推理时选择预测正确性（经成本惩罚后）最高的配置，实现可调的成本-质量权衡。

Result: 在MuSiQue、BrowseComp-Plus和FinanceBench上，BRANE持续推动成本-质量帕累托前沿，在匹配最佳固定配置准确率的同时降低最高89%的成本，优于LLM路由、基于规则和微调Qwen3-4B基线。

Conclusion: 每个查询级别的检索管道全配置是一种实用且优于静态工作负载调优的替代方案，能够有效实现成本与准确性之间的动态权衡。

Abstract: Modern retrieval agents expose many configuration choices -- LLM, retriever, number of documents, number of hops, and synthesis strategy -- each shaping both answer quality and serving cost. Today, these pipelines are typically hand-tuned once per workload, leaving substantial per-query optimization untapped. We formulate the problem: given a natural-language query and either an accuracy or a budget target, select from a predefined pipeline catalog the configuration that minimizes cost or maximizes accuracy at inference time. We propose **BRANE**, which uses an LLM to convert each query into workload-specific characteristics, then trains a lightweight per-configuration predictor that estimates whether the pipeline will answer the query correctly. At inference time, **BRANE** selects the configuration that maximizes predicted correctness penalized by cost, exposing a tunable cost-quality tradeoff without retraining. Across MuSiQue, BrowseComp-Plus, and FinanceBench, **BRANE** consistently pushes the cost-quality Pareto frontier, matches the best fixed configuration's accuracy at up to 89% lower cost, and outperforms LLM-routing, rule-based, and fine-tuned Qwen3-4B baselines. These results show that per-query configuration of the full retrieval pipeline is a practical alternative to static workload-level tuning.

</details>


<div id='cs.LG'></div>

# cs.LG [[Back]](#toc)

### [42] [GEM: Geometric Entropy Mixing for Optimal LLM Data Curation](https://arxiv.org/abs/2605.26121)
*Yue Min,Ziyun Qiao,Ruining Chen,Yujun Li*

Main category: cs.LG

TL;DR: GEM是一个将数据配比建模为超球面变分问题的框架，通过几何熵正则化和MM算法优化，结合教师-学生蒸馏实现Web规模扩展，在1.1B参数模型上提升了1.2%的下游准确率。


<details>
  <summary>Details</summary>
Motivation: LLM预训练效果越来越依赖数据配比而非数据量，但现有数据分类方法存在缺陷：人工分类存在本体论错位，欧氏聚类无法处理嵌入向量的各向异性，导致最优配比难以实现。

Method: 提出GEM框架，将数据配比重新定义为超球面上的变分问题，加入混合平衡正则化器；通过解耦生成先验并使用MM算法优化目标；利用教师-学生蒸馏将几何保真度扩展到Web规模语料；提出几何影响分数(GIS)用于可解释的类别生成。

Result: 在1.1B参数模型上的实验表明，GEM集成到DoReMi和RegMix等混合策略中建立了新的SOTA，平均下游准确率提升1.2%，为可预测的数据混合提供了鲁棒坐标系统。

Conclusion: GEM通过几何视角有效解决了数据分类中的语义结构发现难题，为LLM预训练数据配比优化提供了一种可扩展且可解释的解决方案。

Abstract: LLM pre-training efficacy increasingly depends on data composition rather than sheer volume. Yet, optimal mixing is hindered by categorization flaws: human taxonomies suffer from ontological misalignment, and Euclidean clustering fails to address embedding anisotropy. We introduce GEM (Geometric Entropy Mixing), a framework reformulating data curation as a variational problem on the hypersphere augmented with a mixing-balance regularizer. By decoupling the generative prior and optimizing the objective via a provable MM (Minorize-Maximize) algorithm, GEM effectively counteracts the cluster collapse to discover balanced semantic structures invisible to Euclidean heuristics. We employ teacher-student distillation to scale this geometric fidelity to web-scale corpora and introduce the Geometric Influence Score (GIS) for interpretable taxonomy generation. Experiments with 1.1B-parameter models demonstrate that GEM establishes a new state-of-the-art when integrated into mixing strategies like DoReMi and RegMix, improving average downstream accuracy by up to 1.2% and offering a robust coordinate system for predictable data mixing.

</details>


### [43] [On the Push-Based Asynchronous Federated Learning: A Bias-Correction Aggregation Approach](https://arxiv.org/abs/2605.26162)
*Jiahui Bai,Hai Dong,A. K. Qin*

Main category: cs.LG

TL;DR: 提出PushCen-ADFL框架，通过中心表示空间中的闭环设计（通信、聚合、局部稳定），解决异步去中心化联邦学习中的通信开销、聚合偏差和模型漂移问题，在异构数据下提升准确率并大幅降低通信成本。


<details>
  <summary>Details</summary>
Motivation: 异步去中心化联邦学习缺乏中心协调和全局同步，但存在频繁点对点通信、有向拓扑上的异步更新和非IID数据联合导致的通信开销过大、聚合有偏和模型漂移严重等问题。

Method: PushCen-ADFL框架将通信、聚合和局部稳定耦合在共享的中心表示空间中：客户端交换质心形式的消息，应用保平均的push-sum混合纠正聚合偏差，使用轻量级质心正则化缓解异构性和陈旧性带来的漂移，并采用有界、发送端去重的缓冲区增强对不规则异步到达的鲁棒性。

Result: 在视觉数据集上的实验表明，PushCen-ADFL在数据异构下准确率提升最高6%，每次推送通信成本降低80%以上，实现了良好的准确率-通信权衡。

Conclusion: 所提框架有效改善了异步去中心化联邦学习中通信效率和模型稳定性之间的平衡。

Abstract: Asynchronous decentralized federated learning (ADFL) eliminates central coordination and global synchronization, making it attractive for large-scale and heterogeneous systems. However, frequent peer-to-peer communication, asynchronous updates on directed topologies, and non-IID data jointly lead to excessive communication overhead, biased aggregation and severe model drift. We propose PushCen-ADFL, a communication-efficient ADFL framework that enables stable training under asymmetric communication and delayed client participation. PushCen-ADFL couples communication, aggregation, and local stabilization in a shared centroid representation space, forming a closed loop between compression and optimization. Clients exchange centroid-form messages, apply average-preserving push-sum mixing to correct aggregation bias, and use a lightweight centroid regularization anchored in the same centroid space to mitigate drift under heterogeneity and staleness. A bounded, sender-deduplicated buffer further improves robustness under irregular asynchronous arrivals. Experiments on vision datasets demonstrate that PushCen-ADFL improves accuracy under data heterogeneity by up to 6\% while reducing per-push communication cost by more than 80\%, achieving a favorable accuracy-communication trade-off.

</details>


### [44] [InfoQuant: Shaping Activation Distributions for Low-Bit LLM Quantization](https://arxiv.org/abs/2605.26175)
*Ke Li,Dong An,Xiaoling Zang,Can Ye,Liang Xie,Qibo Qiu,Chen Shen,Xiaofei He,Wenxiao Wang*

Main category: cs.LG

TL;DR: InfoQuant是一种免训练的后训练量化方法，通过信息论分析指导激活分布设计，采用峰值抑制正交变换(PSOT)和自适应异常token选择，在W4A4KV4下平均保留97%的浮点精度。


<details>
  <summary>Details</summary>
Motivation: 低比特激活量化是LLM高效部署的主要瓶颈，现有方法虽然抑制峰值、平衡通道或最小化重构误差，但未明确什么样的激活分布才易于量化，导致数值上看似平滑但量化误差仍然很大。

Method: 提出InfoQuant，包含峰值抑制正交变换(PSOT)将激活塑造成更利于量化的分布，以及自适应异常token选择提升优化鲁棒性，从信息论角度分析量化误差指导激活变换。

Result: 在多个LLM家族上，InfoQuant一致优于先前的PTQ和端到端训练基线。W4A4KV4下平均保留97%浮点精度，LLaMA-2 13B性能差距较之前最优方法缩小42%。

Conclusion: 通过将激活变换重新定义为面向量化器的分布设计，从信息论角度分析量化友好分布的特征，可以有效提升低比特量化效果。

Abstract: Low-bit activation quantization remains a major bottleneck in efficient large language model (LLM) deployment. The difficulty is not only that activations contain outliers, but that their distributions are often poorly matched to a low-bit uniform quantizer. Existing post-training quantization (PTQ) methods suppress peaks, balance channels, or minimize reconstruction error, yet they rarely specify what activation distribution is actually easy to discretize. As a result, activations may appear numerically smoother while still incurring large quantization error because the quantization range remains wide or most values collapse into a few levels near the mean. We recast activation transformation as quantizer-facing distribution design and analyze quantization error from an information-theoretic perspective. Our analysis shows that quantization-friendly activations should jointly have a smaller numerical range and sufficient dispersion within that range. Guided by this analysis, we propose InfoQuant, a train-free method that employs Peak Suppression Orthogonal Transformation (PSOT) to shape activations into more quantization-friendly distributions. We further introduce adaptive outlier-token selection to improve the robustness of PSOT during optimization. Across multiple LLM families, InfoQuant consistently outperforms prior PTQ and end-to-end training baselines. Under W4A4KV4, it preserves 97% of floating-point accuracy on average and reduces the LLaMA-2 13B performance gap by 42% over the previous state of the art. Code is available at [https://github.com/LLIKKE/InfoQuant](https://github.com/LLIKKE/InfoQuant)

</details>


### [45] [Max-Window Scale Estimation for Near-Lossless HiF8 W8A8 Quantization-Aware Training](https://arxiv.org/abs/2605.26189)
*Yingying Cheng,Jinquan Shi,Li Zhou,Zhiyang He,Zhaoyi Sun,Fan Zhang,Jie Sun*

Main category: cs.LG

TL;DR: 本文系统研究了HiF8 W8A8量化感知训练(QAT)在OpenPangu-Embedded-1B模型上的应用，识别并解耦了两种正交失效模式：amax饱和（延迟缩放估计导致前向裁剪破坏知识敏感表征）和灾难性遗忘（激进学习率覆盖预训练常识知识）。通过保守的DTS策略和BF16预热+低学习率QAT，最终在MMLU等基准上仅损失0.22%-0.58%精度，训练损失APE仅0.11%。


<details>
  <summary>Details</summary>
Motivation: 低比特浮点格式的QAT在LLM部署中引入标准训练指标无法检测的细微失效模式，需要系统研究这些失效模式并提出修复方法。

Method: 通过8个控制实验，利用延迟张量缩放(DTS)视角分析HiF8 W8A8 QAT，识别出amax饱和和灾难性遗忘两种失效模式，并提出64步历史窗口的保守max算法DTS策略和500步BF16预热+lr=10^-5的QAT训练方案。

Result: 最终配置相比BF16基线：MMLU下降0.43%，HellaSwag下降0.58%，ARC-Challenge下降0.22%，训练损失APE仅0.11%。

Conclusion: 两种修复方法（保守DTS策略和BF16预热+低学习率QAT）必要且充分，可有效恢复QAT中丢失的模型精度。

Abstract: Quantization-aware training (QAT) with low-bit floating-point formats enables efficient LLM deployment, yet introduces subtle failure modes invisible to standard training metrics. We present a systematic study of HiF8 W8A8 QAT for OpenPangu-Embedded-1B through the lens of Delayed Tensor Scaling (DTS). Across eight controlled experiments, we identify and disentangle two orthogonal failure modes: (i)amax saturation, where delayed scale estimates silently corrupt knowledge-sensitive representations via forward-pass clipping, and (ii)catastrophic forgetting, where an aggressive learning rate overwrites pretrained commonsense knowledge independently of quantization. Neither is detectable from training loss alone. We address amax saturation with a conservative max-algorithm DTS strategy over a 64-step history window, and mitigate forgetting via a 500-step BF16 warmup followed by QAT at lr=10^{-5}. Both fixes are necessary and sufficient: our final configuration achieves 0.43% MMLU drop, 0.58% HellaSwag drop, and 0.22% ARC-Challenge drop versus a matched BF16 baseline, with a training loss APE of only 0.11% over 10,000 steps.

</details>


### [46] [When Does Deep RL Beat Calibrated Baselines? A Benchmark Study on Adaptive Resource Control](https://arxiv.org/abs/2605.26418)
*Guilin Zhang,Chuanyi Sun,Kai Zhao,Shahryar Sarkani,John Fossaceca*

Main category: cs.LG

TL;DR: 本文通过RLScale-Bench基准测试发现，经过适当校准的基于规则的自动缩放器在成本控制上优于六种主流深度强化学习算法，RL在资源控制中的瓶颈不在于算法选择，而在于基线校准、奖励工程和评估协议。


<details>
  <summary>Details</summary>
Motivation: 质疑深度强化学习在自适应资源控制中的实际价值，探究DRL何时能真正优于传统规则方法。

Method: 构建RLScale-Bench可重复基准测试，在Kubernetes HPA上对比PPO、DQN、A2C、SAC、TD3、DDPG与校准的规则基线，在六种工作负载模式和五个随机种子下进行240次实验。

Result: (1) 校准的规则控制器在所有六种工作负载上成本最低，但在突发和闪流流量上不如最佳RL智能体；(2) 离散动作算法在约束违规上比连续动作算法好一到两个数量级；(3) 没有单一算法在所有工作负载上占优，排名变化可达四位。

Conclusion: 基于RL的资源控制瓶颈不是算法选择，而是基线校准、奖励工程和现实的评估协议。

Abstract: A properly calibrated rule-based autoscaler can beat every one of six mainstream deep reinforcement learning (DRL) algorithms on cost across every workload we test - so when, if ever, does DRL actually help? We study this in RLScale-Bench, a reproducible benchmark and evaluation protocol for DRL on adaptive resource control, where an agent allocates compute to a dynamic workload under cost and service-level constraints. We evaluate PPO, DQN, A2C, SAC, TD3, and DDPG under matched architectures, training budgets, and reward functions against a calibrated rule-based baseline across six workload patterns and five seeds (240 runs), instantiate the benchmark on Kubernetes Horizontal Pod Autoscaling, and probe distribution-shift generalization. Three findings challenge common assumptions: (i) the calibrated controller achieves the lowest cost on all six workloads, though it trails the best RL agents on bursty and flash traffic; (ii) discrete-action algorithms outperform continuous-action ones by one to two orders of magnitude in constraint violations due to action-space mismatch; and (iii) no single algorithm dominates across workloads, with rankings shifting by up to four positions. The bottleneck in RL-based resource control is not algorithm selection but baseline calibration, reward engineering, and realistic evaluation protocols.

</details>


### [47] [ReMoE: Boosting Expert Reuse through Router Fine-Tuning in Memory-Constrained MoE LLM Inference](https://arxiv.org/abs/2605.27081)
*Xiongwei Zhu,Xiaojian Liao,Tianyang Jiang,Yusen Zhang,Liang Wang,Limin Xiao*

Main category: cs.LG

TL;DR: ReMoE是一种路由器微调框架，通过偏向近期选中的专家来产生时间稳定的路由，从而提高MoE模型的专家重用率，减少推理时从外部存储加载专家带来的I/O开销。


<details>
  <summary>Details</summary>
Motivation: 在内存受限的推理场景下，MoE模型只能缓存少量专家，未被缓存的专家需要从慢速外部存储（如UFS）获取，导致频繁的缓存驱逐和大量I/O开销，影响推理效率。

Method: 提出ReMoE路由器微调框架，通过训练使路由器偏向于近期被选中的专家，产生时间上稳定的路由策略，从而增加短时间窗口内的专家重用，减少专家从存储中读取的次数，且不增加推理时的额外计算。

Result: 在DeepSeek和Qwen模型上，专家重用率提升26%，同时保持下游任务性能。在vLLM GPU-CPU专家卸载场景下吞吐量提升8.4%，在Jetson Orin NX上使用llama.cpp时TPOT降低43.6-49.8%，对应1.77-1.99倍解码加速。

Conclusion: ReMoE通过路由器微调有效提升了MoE模型在内存受限场景下的专家重用，显著减少了I/O开销并提升了推理吞吐和响应速度，且不牺牲模型质量。

Abstract: Fine-grained Mixture-of-Experts (MoE) models sparsely activate only a subset of experts per token, reducing activated computation while maintaining high model capacity. However, in memory-constrained inference scenarios, only a small set of experts can be cached. Experts not in the cache must be fetched from slow external storage (e.g., UFS), leading to frequent evictions and substantial I/O overhead.
  We propose ReMoE, a router fine-tuning framework designed to boost token-wise expert reuse. ReMoE biases the router toward recently selected experts, producing temporally stable routing that better matches cache locality constraints. By increasing short-horizon expert reuse, ReMoE reduces expert fetches from storage without adding inference-time computation.
  Experiments on DeepSeek and Qwen models show that ReMoE improves expert reuse by 26% while maintaining downstream task performance. Real-system evaluations further confirm these benefits, improving output throughput by 8.4% under vLLM GPU-CPU expert offloading and reducing TPOT by 43.6-49.8% under llama.cpp on Jetson Orin NX, corresponding to a 1.77-1.99$\times$ decode speedup across diverse workloads. Checkpoints and usage instructions are available at https://github.com/BUAA-OSCAR/ReMoE.

</details>


### [48] [Provably Communication-Efficient and Privacy-Preserving Federated Graph Neural Networks](https://arxiv.org/abs/2605.26243)
*Zhishuai Guo,Wenhan Wu,Chen Chen,Lei Zhang,Olivera Kotevska,Ravi K Madduri*

Main category: cs.LG

TL;DR: CE-FedGNN 提出了一种通信高效且保护隐私的联邦图神经网络框架，用于处理跨组织分布的耦合图数据。该方法通过低频交换聚合的节点表示、引入移动平均估计器处理跨客户端依赖和过时问题，并采用度量差分隐私提供隐私保障。在合成反洗钱基准和引文网络上的实验表明，该方法在显著降低通信开销的同时保持强性能和鲁棒性。


<details>
  <summary>Details</summary>
Motivation: 现实世界的图数据通常分布在多个组织之间，由于隐私和政策约束无法共享原始数据。现有的联邦GNN方法要么忽略跨客户端链接导致精度下降，要么需要频繁交换嵌入导致高昂的通信和隐私成本。

Method: 1) 低频交换聚合的节点表示而非每轮嵌入；2) 移动平均估计器持续跟踪节点表示，实现跨轮稳定复用；3) 采用度量差分隐私框架，基于嵌入空间距离而非最坏情况输入扰动度量隐私，在标准差分隐私过于保守的噪声水平下提供有意义的保障；4) 建立了$O(1/\sqrt{T})$收敛率和$O(T^{3/4})$通信复杂度的理论保证。

Result: 在合成银行间反洗钱基准和引文网络上的实验表明，CE-FedGNN在显著降低通信开销的同时保持了强性能，并在隐私保护噪声下维持了鲁棒性。

Conclusion: CE-FedGNN在耦合图上实现了通信高效且隐私保护的联邦图学习，在理论和实验上都验证了其有效性。

Abstract: Graph neural networks (GNNs) achieve strong performance on relational data, but real-world graphs are often distributed across organizations that cannot share raw data due to privacy and policy constraints. Existing federated GNN methods either ignore cross-client links, leading to degraded accuracy, or require frequent embedding exchanges, incurring substantial communication and privacy costs. We propose CE-FedGNN, a communication-efficient and privacy-preserving federated GNN framework for learning over such coupled graphs. Our approach avoids sharing raw data or per-round embeddings by infrequently exchanging aggregated node representations. To handle cross-client dependency and staleness, we introduce a moving-average estimator that continuously tracks node representations and enables their stable reuse across rounds. To provide formal privacy guarantees for the released representations, we adopt the metric differential privacy (metric-DP) framework, which measures privacy with respect to distances in the learned embedding space rather than worst-case input perturbations. This yields meaningful guarantees at noise levels where standard differential privacy becomes overly conservative. We establish convergence to a stationary point at a rate of $O(1/\sqrt{T})$ with $O(T^{3/4})$ communication complexity. In addition, we derive $(\varepsilon,δ)$-metric-DP guarantees via Rényi differential privacy composition under a public-cohort threat model. Experiments on synthetic interbank anti-money laundering benchmarks and citation networks demonstrate that CE-FedGNN achieves strong performance while significantly reducing communication and maintaining robustness under privacy-preserving noise.

</details>


### [49] [Unified Neural Scaling Laws](https://arxiv.org/abs/2605.26248)
*Ethan Caballero,Priyank Jaini,David Krueger,Irina Rish*

Main category: cs.LG

TL;DR: 提出了一种统一神经缩放定律（UNSL），能够准确建模和预测深度神经网络在多个维度（模型参数、数据集大小、训练步数、推理步数、计算量、超参数）同时变化时的缩放行为，涵盖视觉、语言、数学和强化学习等多种任务。


<details>
  <summary>Details</summary>
Motivation: 现有神经缩放定律通常只考虑单一维度（如仅模型参数或仅数据量），缺乏能同时捕捉多个维度变化对模型性能影响的统一函数形式，导致跨维度联合缩放的外推精度不足。

Method: 提出UNSL函数形式，同时建模多个缩放维度（参数、数据、步数、推理、计算、超参数），并在大规模视觉、语言、数学和强化学习任务上进行拟合与验证。

Result: UNSL在多个上游和下游任务上，相比其他神经缩放函数形式，能显著更准确地外推缩放行为。

Conclusion: UNSL提供了更精确的多维度联合缩放建模能力，有助于指导大规模模型训练的资源分配和性能预测。

Abstract: We present a functional form (that we refer to as a Unified Neural Scaling Law (UNSL)) that accurately models and extrapolates the scaling behaviors of deep neural networks as multiple dimensions all vary simultaneously (i.e. how the evaluation metric of interest varies as one simultaneously varies the number of model parameters, training dataset size, number of training steps, number of inference steps, amount of compute, and various hyperparameters) for various architectures and for each of various tasks within a varied set of upstream and downstream tasks. This set includes large-scale vision, language, math, and reinforcement learning. When compared to other functional forms for neural scaling, this functional form yields extrapolations of scaling behavior that are considerably more accurate on this set.

</details>


### [50] [Stateful Inference for Low-Latency Multi-Agent Tool Calling](https://arxiv.org/abs/2605.26289)
*Victor Norgren*

Main category: cs.LG

TL;DR: 提出了一种有状态推理架构，通过持久化KV缓存和增量更新机制，将传统推理框架中每个工具调用独立处理全对话的开销从O(n_t)降低到O(Δ_t)，在代理式多轮工具调用场景下实现2.1倍到4.2倍的每轮加速。


<details>
  <summary>Details</summary>
Motivation: 现有LLM推理框架（如vLLM、SGLang）将每个工具调用视为独立请求，每次从头重新处理整个对话，但实际85-95%的提示内容与前一轮相同，造成了大量冗余计算开销。

Method: 提出有状态推理架构：(1) 持久化KV缓存跨轮次存活，仅摄取新token实现增量更新；(2) radix前缀缓存扩展支持多代理交错流量；(3) prompt-lookup投机解码加速结构化输出。

Result: 在完全生成的新工作负载上，相比vLLM和SGLang，参考实现在6轮代理工作流中每轮快2.1倍，在35轮工作流的中位轮次快4.2倍，端到端耗时减半。

Conclusion: 状态复用和投机解码（而非传统缓存）是加速的核心来源，该架构能显著降低多轮工具调用场景下的推理延迟。

Abstract: Multi-agent tool calling is becoming the dominant interaction pattern for LLM-based systems, yet existing inference frameworks treat each tool call as an independent request, re-processing the entire conversation from scratch even though 85-95% of the prompt is unchanged from the previous turn.
  We present a stateful inference architecture that converts the $O(n_t)$ per-turn cost of conventional serving into an $O(Δ_t)$ delta-only cost: a persistent KV cache lives across turns and advances by ingesting only the new tokens, while a radix prefix cache extends this across interleaved multi-agent traffic and a prompt-lookup speculative decoder accelerates structured output. Against vLLM and SGLang on novel, fully-generated workloads, the reference implementation is $2.1\times$ faster per turn on a 6-turn agentic workflow and $4.2\times$ on the median turn of a 35-turn one, halving end-to-end wall time. The advantage comes from stateful reuse and speculation, not caching.

</details>


### [51] [Reparametrizing Shampoo and SOAP for Subspace Basis Updates and BFloat16 Storage](https://arxiv.org/abs/2605.26327)
*Alan Milligan,Zikun Xu,Simon Lacoste-Julien,Felix Dangel,Wu Lin*

Main category: cs.LG

TL;DR: 提出了一种对Shampoo类优化器预条件子的重参数化方法，支持BFP16存储并通过子空间QR分解仅更新部分基向量，从而降低计算开销和内存占用，同时缓解低精度带来的性能下降。


<details>
  <summary>Details</summary>
Motivation: 现有Shampoo类方法依赖FP32精度的QR分解，计算和内存开销大；使用BFP16存储虽能节省内存但会损害性能，亟需一种兼顾效率与精度的方法。

Method: 对预条件子进行重参数化，将更新后的基向量与不变的基向量组合形成完整基；仅通过子空间中的QR分解更新部分基向量，减少计算量并支持BFP16存储。

Result: 在BFP16存储下提升了SOAP和KL-SOAP的性能，使KL-SOAP能达到或超过KL-Shampoo的水平；整体上使得Shampoo类方法更节省内存和时间。

Conclusion: 该方法广泛适用于使用QR分解的Shampoo类优化器（KL-Shampoo, SOAP, KL-SOAP），有效提升了内存和时间效率。

Abstract: Shampoo-based methods, such as KL-Shampoo and SOAP, have demonstrated strong performance in training neural networks and rely on QR decomposition. Because existing QR implementations require single-precision (FP32) arithmetic and remain computationally expensive, these methods become time- and memory-intensive when their preconditioning matrices are large. Moreover, using BFloat16 (BFP16) storage to reduce memory usage can degrade the performance of Shampoo-based methods. We propose a reparametrization of the preconditioner that supports BFP16 storage and forms a complete basis by combining updated basis vectors with unchanged ones. By updating only part of the basis through QR decomposition in a subspace, our approach reduces computational overhead while mitigating the performance degradation caused by BFP16 storage. Our approach applies broadly to Shampoo-based methods that employ QR decomposition, including KL-Shampoo, SOAP, and KL-SOAP. In particular, it improves the performance of SOAP and KL-SOAP under BFP16 storage, enabling KL-SOAP to match or exceed KL-Shampoo. Overall, our approach makes Shampoo-based methods more memory- and time-efficient.

</details>


### [52] [QAM-W: Joint 2D Codebook Quantization for LLM Weights via Hadamard Rotation and Activation-Aware Scaling](https://arxiv.org/abs/2605.26339)
*Preetam Sharma,Kacper Dobek*

Main category: cs.LG

TL;DR: 提出QAM-W，一种将权重行配对为2D坐标并使用Lloyd-Max码本量化的后训练权重量化方法，在约5.5 bpw下匹配SmoothQuant W8A8质量但节省32%比特。


<details>
  <summary>Details</summary>
Motivation: 标量后训练量化器丢弃了权重行内的成对坐标结构，本文旨在恢复这种结构以实现更好的权重量化。

Method: L2归一化每行→块Hadamard旋转→配对为2D坐标→用训练于单位圆高斯分布的Lloyd-Max码本量化，并加入激活感知的逐通道缩放。

Result: 在5个LLM（1.1B-13B）上约5.5 bpw下perplexity偏差±0.4%，匹配SmoothQuant W8A8但比特数少32%；联合2D编码优于极坐标编码2-15pp ΔPPL。

Conclusion: QAM-W在5-6 bpw区间能保持质量，但严格4 bpw下不如QTIP。

Abstract: Scalar post-training quantizers discard pairwise coordinate structure within weight rows. We introduce QAM-W (Quadrature Amplitude Modulation for Weights), a codec that recovers this structure: each row is L2-normalized, block-Hadamard rotated, paired into 2D coordinates, and quantized against a single Lloyd-Max codebook trained on the unit circular Gaussian, with activation-aware per-channel scaling. In a cross-model study spanning five LLMs from four families (1.1B--13B parameters) and eight quantized configurations, the activation-aware variant at $\approx 5.5$ bpw stays within $\pm 0.4\%$ of BF16 WikiText-2 perplexity on every model, matching the SmoothQuant W8A8 quality envelope at $32\%$ fewer weight bits. Joint 2D coding outperforms polar (amplitude $\times$ phase) coding by 2--15~pp $Δ$PPL at equal bitrate, and paired KL against BF16 tracks $Δ$PPL\% at Spearman $ρ= 0.99$ across 37 (method, model) rows, consistent with a monotone composite bound from codec distortion to KL divergence. A 3.5~bpw variant is competitive on quantization-tolerant architectures. At strict 4~bpw, the rotated-codebook frontier method QTIP outperforms QAM-W; the contribution is the quality-preserving 5--6~bpw band.

</details>


### [53] [MuCon: Clipped Muon Updates for LLM Training](https://arxiv.org/abs/2605.26459)
*Albert Yi*

Main category: cs.LG

TL;DR: MuCon是一种对Muon优化器的改进，对预处理矩阵B进行奇异值裁剪（而非映射为纯极因子），并探讨了无需完整SVD即可近似该裁剪步骤的数值方法，指出了靠近裁剪阈值时的病态问题。


<details>
  <summary>Details</summary>
Motivation: Muon风格的优化器将预处理矩阵B替换为其极因子，将所有奇异值映射为1。本文希望通过奇异值裁剪（MuCon）保留较大奇异值的信息，同时控制更新步长，并探究是否能在避免完全SVD的情况下高效实现该裁剪。

Method: 定义了MuCon裁剪算子MClip_τ，将其解释为Frobenius范数下到谱范数球的投影；推导了两个精确恒等式（极/绝对值公式和标量根公式），并引入有理Newton滤波器来处理裁剪后的正半定因子；分析了在阈值附近病态条件的根本原因。

Result: MuCon裁剪等价于到谱范数球的投影；两个恒等式都面临阈值附近奇异值导致的符号判断困难和有理求解病态问题；矩阵函数方法只有在搭配稳定的极/平方根原语或显式正则化时才有效。

Conclusion: 若无稳定的数值原语，直接用矩阵函数近似MuCon裁剪在阈值附近不可靠；实际中需结合正则化或可靠的SVD分解。

Abstract: Muon-style optimizers take a matrix-valued momentum or preconditioned update $B = U \operatorname{diag}(σ_1,\ldots,σ_r) V^\top$ and replace it with its canonical partial polar factor $\operatorname{Pol}(B) = U V^\top$. This maps every nonzero singular value to one. MuCon is the clipped-Muon variant studied here: it applies singular-value clipping to the same Muon matrix, $D^{\mathrm{MuCon}}\_τ(B) = \operatorname{MClip}\_τ(B) = U \operatorname{diag}\bigl(\min\{σ\_i,τ\}\bigr) V^\top, \qquad τ> 0$. Thus, $\operatorname{MClip}\_τ$ denotes the mathematical clipping operator, while MuCon denotes the optimizer primitive that substitutes this clipped direction for Muon's polar direction. The Muon/MuCon scaling parameterization used in this work is called $\text{SpectralP}$: it is the hidden-matrix scaling recipe under which polar Muon or clipped MuCon directions are applied. The map $\operatorname{MClip}\_τ$ is the Frobenius projection onto the spectral-norm ball $\{X : \|X\|_2 \le τ\}$: it leaves singular values at or below $τ$ unchanged and modifies only the violating singular directions. This paper asks when the MuCon clipping step can be approximated without a full dense SVD. We record two exact identities, a polar/absolute-value formula and a scalar-root formulation leading to a rational Newton filter for the clipped positive-semidefinite factor, and identify the numerical obstruction common to both: singular values near the threshold make sign decisions and rational solves ill-conditioned. Matrix-function methods are therefore useful only when paired with stable polar/square-root primitives or explicit regularization near the clipping boundary.

</details>


### [54] [Dense2MoE: Pushing the Pareto Frontier of On-Device LLMs via Unified Pruning and Upcycling](https://arxiv.org/abs/2605.26496)
*Fengfa Li,Hongjin Ji,Yifeng Ding,Lei Ren,Chen Wei*

Main category: cs.LG

TL;DR: 提出Dense2MoE框架，通过Layer Fusion UpCycling (LF-UC)技术，将稠密大模型高效转换为适合端侧部署的MoE模型，在保持模型能力的同时减少参数冗余和推理延迟。


<details>
  <summary>Details</summary>
Motivation: MoE架构非常适合资源受限的端侧设备，但从头训练成本过高。现有方法（如将稠密模型升级为MoE）存在参数冗余导致推理效率下降，而标准层剪枝又会损害模型精度。需要一种既能减少冗余又不损失精度的方法。

Method: 提出Dense2MoE框架，核心是Layer Fusion UpCycling (LF-UC)：基于硬件Roofline理论，剪枝冗余层中带宽密集的注意力模块，同时将其多层感知机（MLP）重新利用为MoE专家。通过选择性token路由严格限制活跃参数数量。

Result: 在适度持续预训练预算下，Dense2MoE能高效将公开稠密LLM转换为端侧可部署的MoE模型。实验证明该方法显著推进了端侧推理延迟与模型精度的帕累托前沿，优于稠密基线、SOTA压缩方法和标准升级策略。

Conclusion: Dense2MoE统一了剪枝和升级策略，有效解决了稠密模型转MoE过程中的参数冗余与精度损失之间的矛盾，为端侧设备部署大模型提供了可行方案。

Abstract: The Mixture of Experts MoE architecture is highly promising for resource constrained on device deployments yet training these models from scratch incurs prohibitive costs Current methods attempt to alleviate this by upcycling dense models into MoEs however they often introduce parameter redundancy that degrades inference efficiency Alternatively standard layer pruning mitigates redundancy but inevitably compromises model accuracy To resolve this dilemma we propose Dense2MoE a novel framework that unifies pruning and upcycling through Layer Fusion UpCycling LF UC Guided by hardware Roofline theory Dense2MoE systematically overcomes the inference memory wall by pruning bandwidth heavy attention modules from redundant layers while repurposing their Multi Layer Perceptrons MLPs into MoE experts This structural innovation preserves the models core capabilities and strictly limits active parameters via selective token routing With a modest continual pre training budget Dense2MoE efficiently converts publicly available dense LLMs into on device ready MoE models Extensive experiments demonstrate that Dense2MoE significantly advances the Pareto frontier for on device inference latency versus model accuracy outperforming dense baselines state of the art compression and standard upcycling methods

</details>


### [55] [Spend Your Rollouts Where It Counts: Rollout Allocation for Group-Based RL Post-Training](https://arxiv.org/abs/2605.26606)
*Woojeong Kim,Ziyi Yang,Jing Nathan Yan,Jialu Liu*

Main category: cs.LG

TL;DR: 提出Pilot-Commit框架，通过两阶段（pilot评估+提交分配）实现基于组的RL后训练中rollout预算的自适应分配，在保持精度的同时显著降低采样成本。


<details>
  <summary>Details</summary>
Motivation: 现有基于组的策略优化方法（如GRPO、DAPO）在训练过程中对每个prompt分配相同数量的rollout，但一些prompt的奖励分布已经坍缩，继续分配rollout带来的学习信号微乎其微，浪费了大量计算资源。

Method: 提出Pilot-Commit框架，将rollout分配解耦为两个阶段：(1) Pilot阶段，使用少量预算评估每个prompt的信息量（基于奖励方差）；(2) Commit阶段，将剩余rollout预算分配给高信息量的prompt，跳过低信号prompt。

Result: 在多个数学推理基准和1.5B至14B参数规模的模型上，Pilot-Commit在达到目标精度时累积rollout数量比GRPO快1.9倍，比DAPO快4.0倍。

Conclusion: 基于组的策略更新在奖励方差高的regime下最有效，因此训练过程中需要在线估计prompt信息量并动态分配rollout预算。

Abstract: Reinforcement learning (RL) is the dominant paradigm for post-training large language models. However, in the online, on-policy setting, rollout generation dominates the computational cost of training. Group-based policy optimization methods compute advantages from multiple rollouts per prompt, yet they indiscriminately allocate budget to prompts with collapsed reward distributions, wasting expensive rollouts on negligible learning signals. We demonstrate that group-based updates are most effective in regimes of high reward variance. Since the policy evolves throughout training, prompt informativeness must be estimated online rather than precomputed, but exhaustively evaluating every prompt is computationally prohibitive. We introduce Pilot-Commit, a budget-aware rollout allocation framework for group-based RL post-training. Pilot-Commit decouples prompt evaluation from exploitation: a pilot stage estimates per-prompt informativeness using a fraction of the budget, and the remaining rollouts are allocated to high-leverage prompts while low-signal prompts are skipped. Across multiple math reasoning benchmarks and model scales from 1.5B to 14B parameters, Pilot-Commit matches baseline accuracy with significantly lower sampling costs, reaching target accuracy up to $1.9\times$ faster than GRPO and $4.0\times$ faster than DAPO in cumulative rollouts.

</details>


### [56] [RT-Lynx: Putting the GEMM Sparsity In a Right Way for Diffusion Models](https://arxiv.org/abs/2605.26632)
*Xing Cong,Hanlin Tang,Kan Liu,Lan Tao,Lin Qu,Chenhao Xie*

Main category: cs.LG

TL;DR: 提出RT-Lynx，通过对扩散Transformer激活值进行N:M半结构化稀疏化，结合误差补偿技术和优化CUDA内核，在保持生成质量的同时实现线性层平均1.55倍加速。


<details>
  <summary>Details</summary>
Motivation: 扩散Transformer推理成本高，现有方法多用权重量化或蒸馏，但半结构化稀疏化（可减少近一半FLOPs）未被充分探索，因为权重稀疏化会损害模型容量和生成质量。

Method: 发现DiT激活值天然稀疏且对N:M稀疏化更鲁棒，因此从权重稀疏转向激活稀疏；提出RT-Lynx，对激活值做N:M稀疏化并引入误差补偿技术，同时实现高度优化的CUDA内核。

Result: 在线性层上平均实现1.55倍加速，在多种扩散模型上保持原始模型的生成质量。

Conclusion: 激活稀疏化比权重稀疏化更适合扩散Transformer，可以在显著加速推理的同时不损失生成质量。

Abstract: Diffusion Transformers (DiT) achieve strong performance in image generation but incur substantial inference costs. While prior work has reduced this cost via quantization and distillation, semi-structured sparsity, which can nearly halve FLOPs, remains underexplored. A key reason is that most existing approaches focus on weight sparsification, and pruning 50% of the weights can remove critical model capacity and degrade generation quality. Our study, however, shows that DiT activations are intrinsically sparse and significantly more robust to N:M semi-structured sparsification than weights. Motivated by this observation, we advocate a paradigm shift from weight sparsification to activation sparsification. We propose RT-Lynx, which applies N:M sparsification to activations and incorporates error-compensation techniques to mitigate accuracy loss. We further implement highly optimized CUDA kernels tailored to this setting, achieving up to a 1.55x speedup on average in linear layers. Extensive experiments across multiple diffusion models demonstrate that our method preserves the generation quality of the original models while substantially accelerating inference.

</details>


### [57] [WINDQuant: Weight-Informed Neural Decision-Making for Global Mixed-Precision LLM Quantization](https://arxiv.org/abs/2605.26660)
*Phong Nam Huu Nguyen,Khoi M. Le,Cong-Duy T Nguyen,Anh Tuan Luu,Thong Thanh Nguyen,Tho Quan*

Main category: cs.LG

TL;DR: WINDQuant 提出了一种基于强化学习（PPO）的混合精度量化控制器，在超低位宽下为 LLM 的列块级权重分配位宽和量化策略，兼顾模型压缩与性能保持。


<details>
  <summary>Details</summary>
Motivation: 现有后训练量化在超低位宽下精度下降严重，而量化感知训练成本高；混合精度策略多依赖粗粒度或启发式敏感性分析，忽略了权重矩阵内的细粒度变化。

Method: 采用强化学习（PPO）作为分配控制器，在全局存储预算下为列块级别（column-chunk）的权重学习位宽分配和量化处理方式，结合激活感知校准、轻量级每单元量化器拟合和显式有效比特核算。

Result: 在 LLaMA 模型上，WINDQuant 在超低位宽设置下取得了有竞争力的性能，同时相比基于重训练的方法降低了优化开销。

Conclusion: 强化学习可作为自适应混合精度量化的实用控制器，在极低位宽下平衡模型压缩与精度。

Abstract: Quantization is an effective approach to reduce the memory footprint and inference cost of large language models (LLMs), yet maintaining performance in the ultra-low-bit regime remains challenging. Existing post-training methods often suffer from severe accuracy degradation, while quantization-aware training requires costly retraining and additional resources. Moreover, most mixed-precision strategies rely on coarse-grained or heuristic sensitivity analysis that overlooks fine-grained variations within weight matrices. We propose WINDQuant, a reinforcement-learning-based allocation controller for ultra-low-bit LLM quantization. Rather than introducing another low-level quantization operator, WINDQuant learns how to assign bit-widths and quantization treatments to fine-grained column chunks under a global storage budget. By operating at the column-chunk level, WINDQuant enables flexible and fine-grained precision assignment within layers under a global target bit-width. The implementation combines PPO with activation-aware calibration, lightweight per-unit quantizer fitting, and explicit effective-bit accounting of the learned mixed-precision plan. Experiments on LLaMA models demonstrate that WINDQuant achieves competitive performance in ultra-low-bit settings while reducing optimization overhead relative to retraining-based approaches, highlighting reinforcement learning as a practical controller for adaptive mixed-precision quantization.

</details>


### [58] [Latent Recurrent Transformer: Architecture Exploration, Training Strategies, and Scaling Behavior](https://arxiv.org/abs/2605.26797)
*Zeyi Huang,Xuehai He,LiLiang Ren,Yiping Wang,Baolin Peng,Hao Cheng,Shuohang Wang,Pengcheng He,Jianfeng Gao,Yong Jae Lee,Yelong Shen*

Main category: cs.LG

TL;DR: LRT是一种轻量级增强方案，通过跨层的循环潜在路径复用前一token的隐藏状态作为当前token的循环记忆，不增加额外计算深度，同时提出交错并行训练方法，在约2倍基线计算量下实现循环感知监督预训练。


<details>
  <summary>Details</summary>
Motivation: 在保持标准Transformer架构和KV-cache接口不变的前提下，为自回归Transformer引入高效的循环记忆机制，以改进语言建模损失和上下文学习能力，同时避免传统循环展开带来的训练开销。

Method: 提出Latent Recurrent Transformer (LRT)，跨层复用前一token的高层隐藏状态作为当前token的循环记忆；并设计交错并行训练方法，通过一次完整序列前向构建共享缓冲区，再并行精调不相交位置子集并写回实现循环感知训练。

Result: 在nanochat风格主干网络上，跨多种tokens-per-parameter预算下，LRT在匹配有效计算时改进了语言建模损失和上下文学习能力，仅增加0.3%参数量。

Conclusion: LRT以极小参数量代价为Transformer带来循环记忆能力，且训练效率高（约2倍基线计算量），是改进自回归Transformer的有效轻量方案。

Abstract: We study Latent Recurrent Transformer (LRT), a lightweight augmentation of autoregressive transformers that reuses a high-level source-layer hidden state from the previous token as recurrent memory for the next token. Because this source state is already computed during ordinary decoding, LRT adds a cross-layer recurrent latent pathway across positions without inserting pause tokens or extra depth loops, and the standard attention mechanism and KV-cache interface are preserved. To pretrain this recurrence at scale without sequentially unrolling the transformer, we introduce interleaved parallel training: a single full-sequence initialization forward pass builds a shared buffer; then disjoint position subsets are refined in parallel and written back, so that all tokens receive recurrent-memory-aware supervision at roughly 2 times baseline compute. Across nanochat style backbones and a wide range of tokens-per-parameter budgets, LRT improves both language-modeling loss and in-context learning under matched effective compute while adding as little as 0.3% parameters.

</details>


### [59] [Negligible in Size, Significant in Effect: On Scale Vectors in Large Language Models](https://arxiv.org/abs/2605.26895)
*Mingze Wang,Shuchen Zhu,Yuxin Fang,Binghui Li,Kai Shen,Shu Zhong*

Main category: cs.LG

TL;DR: 本文系统研究了LLM中的scale向量，发现其虽参数占比极小但对预训练至关重要，通过自放大预条件效应优化优化过程而非增加表达能力；提出分支特异性异质性、线性映射周围放置改进和幅度-方向重参数化三种轻量改进，统一策略在0.12B-2B参数模型上持续优于基线。


<details>
  <summary>Details</summary>
Motivation: 尽管scale向量在LLM中广泛使用，但其作用机制未被充分理解，本文旨在从表达能力、优化和架构角度系统阐明scale向量的作用原理并提出改进方案。

Method: 首先通过移除实验和理论分析证明scale向量通过自放大预条件效应改善优化；区分Input-Norm和Output-Norm层分析权重衰减的影响；提出三种改进：分支特异性异质性、改进线性映射周围放置、幅度-方向重参数化。

Result: 在0.12B到2B参数的稠密和MoE模型上，统一策略在各种优化器和学习率调度下持续达到更低的终端损失，且具有更优的缩放行为，参数和计算开销可忽略。

Conclusion: Scale向量通过优化而非表达能力提升起作用；权重衰减对Input-Norm有益但对Output-Norm有害；提出的轻量改进能持续提升LLM预训练效果。

Abstract: Normalization layers in modern large language models (LLMs) consist of a deterministic normalization operation and a learnable scale vector. While the normalization operation has been extensively studied, the scale vector remains poorly understood despite its ubiquitous use. In this work, we present a systematic study of scale vectors in LLMs from the perspectives of expressivity, optimization, and architectural structure. First, we show empirically that although scale vectors constitute only a negligible fraction of model parameters, removing them substantially degrades LLM pre-training. Our theory further shows that, in Pre-Norm architectures, scale vectors do not increase expressivity; instead, they improve optimization through a self-amplifying preconditioning effect on subsequent linear mappings. Second, we investigate the role of weight decay for scale vectors. By distinguishing Input-Norm and Output-Norm layers, we theoretically show that weight decay is beneficial for the former but harmful for the latter, due to their distinct roles in optimization and expressivity. Third, motivated by this understanding, we propose three lightweight and complementary improvements to scale vectors: branch-specific heterogeneity, improved placement around linear mappings, and magnitude-direction reparameterization. Both theory and experiments show that each improvement yields consistent gains. Finally, we combine these improvements into a unified scale-vector strategy and evaluate it through extensive LLM pre-training experiments on dense and mixture-of-experts models ranging from 0.12B to 2B parameters, across multiple optimizers and learning rate schedules, under industrial-scale token budgets. The unified strategy consistently achieves lower terminal loss than well-tuned baselines and exhibits more favorable scaling behavior, while adding negligible parameter and computational overhead.

</details>


### [60] [Learning to Orchestrate Agents under Uncertainty](https://arxiv.org/abs/2605.27073)
*Mary Chriselda Antony Oliver,Lan Jiang,Aaron Bundi Anampiu,Elaf Almahmoud,Francesco Quinzan,Umang Bhatt*

Main category: cs.LG

TL;DR: 提出BOT-Orch框架，将异构智能体自适应编排问题建模为带最优传输(OT)距离正则化的多臂赌博机问题，在不确定性下进行智能体委托决策，理论上达到O(√T)遗憾界，并能在合成对抗任务中优于标准基线。


<details>
  <summary>Details</summary>
Motivation: 现有智能体编排工作主要关注性能或成本，但未在编排层显式建模智能体可靠性和输出分布的不确定性，而实际场景中智能体行为是动态变化的。

Method: 将编排问题转化为带正则化的多臂赌博机问题，使用OT距离约束智能体输出分布与任务参考分布之间的对齐，在标准假设下证明O(√T)遗憾界，并证明能对均值相同但分布对齐不同的智能体产生偏好排序。

Result: 在合成对抗性任务分配场景中，BOT-Orch在异构非独立同分布智能体行为下优于标准赌博机算法和启发式基线。

Conclusion: 引入OT正则化能有效处理智能体编排中的不确定性，在理论上和实证上都显示出优势。

Abstract: Adaptive orchestration of heterogeneous agents requires making sequential delegation decisions under uncertain and evolving agent behaviour, e.g., coordinating specialised AI models with varying reliability, cost, and response quality. While prior work on agent orchestration focuses on performance or cost, uncertainty in agent reliability and output distributions is typically not modelled explicitly at the orchestration level. In this work, we study the problem of adaptive orchestration of heterogeneous agents under uncertainty, where a meta-controller must decide when to delegate to an agent, accounting for reliability, cost, and uncertainty. We propose BOT-Orch, a lightweight framework that recasts orchestration as a bandit problem over agents, regularized by OT distances between agent output distributions and task-specific reference distributions. We show that the regularised orchestration enjoys $\mathcal{O}(\sqrt{T})$ regret under standard assumptions, and provably induces preference ordering among agents with identical mean rewards but differing distributional alignment. Empirically, we demonstrate that BOT-Orch outperforms standard bandit and heuristic baselines in synthetic but adversarial task allocation settings with heterogeneous, non-i.i.d. agent behaviour.

</details>


### [61] [Guiding LLM Post-training Data Engineering with Model Internals from Sparse Autoencoders](https://arxiv.org/abs/2605.27354)
*Yi Jing,Zao Dai,Jinwu Hu,Zijun Yao,Lei Hou,Juanzi Li,Xiaozhi Wang*

Main category: cs.LG

TL;DR: 本文提出SAERL框架，利用稀疏自编码器（SAE）提取模型内部表征，对LLM后训练RL数据的三类内在属性（多样性、难度、质量）进行建模和工程化操作，显著提升训练效率和准确性。


<details>
  <summary>Details</summary>
Motivation: 当前后训练数据工程主要依赖外部信号（如奖励模型），忽略了LLM模型内部蕴含的丰富信号，作者希望利用模型内部表征来更有效地指导数据工程。

Method: 使用SAE（稀疏自编码器）提取模型内部表征，设计三个数据工程操作：SAE空间聚类+适度批混合控制多样性、基于难度代理的易到难课程排序、质量探针用于数据过滤。在GRPO等RL算法上验证。

Result: 相比vanilla GRPO平均准确率提升3.00%，达到目标准确率所需训练步数减少20%，在多种模型规模和RL算法下均有一致增益，且SAE可跨模型家族和规模迁移。

Conclusion: 模型内部表征是后训练数据工程中强大且实用的信号源，SAE可作为轻量级、可复用的数据工程工具。

Abstract: Model internals encode rich information about how a large language model (LLM) processes its training data; however, post-training data engineering largely relies on external signals and ignores rich intrinsic signals lying in model internals. We propose SAERL, a data engineering framework for LLM reinforcement learning (RL). It models three intrinsic data properties: diversity, difficulty, and quality, using model internals extracted with Sparse Autoencoder (SAE), an advanced mechanistic interpretability tool. Each property grounds a concrete data engineering operation: SAE-space clustering with moderate batch mixing for batch diversity control, a difficulty proxy for easy-to-hard curriculum ordering, and a quality probe for data filtering. SAERL improves average accuracy by 3.00% over vanilla GRPO and reaches target accuracy with 20% fewer training steps on Qwen2.5-Math-1.5B, with consistent gains across model scales and RL algorithms. Experiments show that SAE transfers effectively across model families and scales, serving as a lightweight and reusable data engineering tool. These results demonstrate that model internals are a powerful and practical source of signals for post-training data engineering.

</details>


### [62] [MobileMoE: Scaling On-Device Mixture of Experts](https://arxiv.org/abs/2605.27358)
*Yanbei Chen,Hanxian Huang,Ernie Chang,Jacob Szwejbka,Digant Desai,Zechun Liu,Vikas Chandra,Raghuraman Krishnamoorthi*

Main category: cs.LG

TL;DR: MobileMoE提出了一系列面向移动端部署的子十亿参数MoE语言模型（0.3-0.9B活跃参数，1.3-5.3B总参数），通过制定移动端MoE缩放定律、采用细粒度稀疏与共享专家架构、以及四阶段训练方案，在14项基准测试中匹配或超越领先的移动端稠密LLM和MoE模型，并在商用智能手机上实现了高效的MoE推理。


<details>
  <summary>Details</summary>
Motivation: Mixture-of-Experts (MoE)已成为数百亿参数语言模型的主流架构，但其在十亿参数以下规模、面向移动端部署的优势尚未被充分探索。本文旨在填补这一空白，研究如何在移动内存和计算约束下设计和优化小规模MoE模型。

Method: (1) 制定了一个移动端MoE缩放定律，在移动内存和计算约束下联合优化MoE架构，确定适度稀疏性与细粒度共享专家为最优配置；(2) 采用四阶段训练方案：预训练、中期训练、指令微调、量化感知训练，全部使用开源数据集；(3) 在商用智能手机上进行首次高效的MoE推理部署与性能剖析。

Result: 在14个基准测试中，MobileMoE匹配或超越领先的移动端稠密LLM，推理FLOPs减少2-4倍；匹配或超越SOTA MoE模型OLMoE-1B-7B，参数减少高达60%。在INT4权重重量的条件下，MobileMoE-S相较稠密基线MobileLLM-Pro实现1.8-3.8倍prefill加速和2.2-3.4倍decode加速。

Conclusion: MobileMoE在子十亿参数规模下验证了MoE架构对移动端部署的有效性，建立了新的移动端LLM帕累托前沿，并通过实际的智能手机部署证明了其效率优势。

Abstract: Mixture-of-Experts (MoE) has become the de facto architecture for hundred-billion-parameter language models, yet its advantages at sub-billion scales for on-device deployment remain largely unexplored. To close this gap, we present MobileMoE, a family of on-device MoE language models with sub-billion active parameters (0.3-0.9B active and 1.3-5.3B total) that establish a new Pareto frontier for on-device LLMs. We first formulate an on-device MoE scaling law that jointly optimizes MoE architecture under mobile memory and compute constraints, identifying an on-device sweet spot - moderate sparsity with fine-grained and shared experts - that is simultaneously memory and compute-optimal. Building on the derived architectures, we train MobileMoE with a four-stage recipe covering pre-training, mid-training, instruction fine-tuning, and quantization-aware training, all on open-source datasets. Across 14 benchmarks, MobileMoE matches or exceeds leading on-device dense LLMs with 2-4$\times$ fewer inference FLOPs, and matches or surpasses the state-of-the-art MoE OLMoE-1B-7B with up to 60% fewer parameters. To bridge the last mile to mobile deployment, we provide the first efficient MoE inference on commodity smartphones with comprehensive on-device profiling. At comparable INT4 weight memory, MobileMoE-S delivers $1.8$-$3.8\times$ faster prefill and $2.2$-$3.4\times$ faster decode than the dense baseline MobileLLM-Pro.

</details>


### [63] [BASIS: Batchwise Advantage Estimation from Single-Rollout Information Sharing for LLM Reasoning](https://arxiv.org/abs/2605.27293)
*Shijin Gong,Erhan Xu,Kai Ye,Francesco Quinzan,Giulia Livieri,Chengchun Shi*

Main category: cs.LG

TL;DR: 提出BASIS，一种无需评论家的强化学习后训练算法，通过跨批次利用信息改进价值函数估计，在仅采样单次rollout的情况下达到接近多rollout GRPO方法的性能。


<details>
  <summary>Details</summary>
Motivation: 现有基于可验证奖励的强化学习算法在计算效率与样本效率之间存在权衡，作者希望设计一种既高效又不需要多rollout采样的算法。

Method: BASIS在每步在线训练中每个提示只采样一个rollout，但利用整个批次中所有提示的丰富信息来改进价值函数估计，是一种无评论家（critic-free）的后训练算法。

Result: 相比REINFORCE++，价值函数估计的MSE降低69%；使用单rollout比使用8 rollouts的组均值估计器的MSE更低；用更少训练时间达到接近多rollout GRPO方法的性能。

Conclusion: BASIS有效解决了计算效率与样本效率的权衡，为提升大模型推理能力的强化学习后训练提供了一种更高效的方案。

Abstract: Reinforcement learning with verifiable rewards has become a standard recipe for improving the reasoning abilities of large language models. Existing algorithms face a tradeoff between computational efficiency and sample efficiency in value estimation and policy learning. We introduce BASIS, a critic-free post-training algorithm designed to address this tradeoff. At each online training step, BASIS samples only one rollout per prompt, but leverages rich information across prompts in the entire batch to improve value function estimation. Our experiments demonstrate that BASIS reduces MSE in value function estimation by 69% compared to REINFORCE++, a representative single-rollout baseline, and achieves lower MSE with one rollout than group mean estimators with 8 rollouts. This improvement in value estimation translates to better policy optimization: using substantially less training time, BASIS achieves performance close to multi-rollout GRPO-type baselines and often outperforms single-rollout REINFORCE-type baselines.

</details>


<div id='cs.DC'></div>

# cs.DC [[Back]](#toc)

### [64] [Xe-Forge: Multi-Stage LLM-Powered Kernel Optimization for Intel GPU](https://arxiv.org/abs/2605.26118)
*Marcin Spoczynski,Daniel Fleischer,Moshe Berchansky,Gabriela Ben-Melech Stan,Shira Guskin,Weilin Xu,Adam Siemieniuk,Alexander Heinecke*

Main category: cs.DC

TL;DR: Xe-Forge是一个多阶段LLM驱动的自动化流水线，用于将Triton内核移植到Intel GPU，通过九个优化阶段（包括CoVeR验证机制和硬件知识库）实现自动优化，在97个KernelBench内核和Flash Attention上取得1.17x几何平均加速比，最高达82x。


<details>
  <summary>Details</summary>
Motivation: 将深度学习算法移植到新硬件加速器时，开发者需要反复手动对每个Triton内核应用底层优化（量化、内存合并、tile调优等），这种重复性劳动是主要瓶颈。

Method: Xe-Forge采用多阶段LLM流水线，包含九个优化阶段（算法重构、算子融合、块指针现代化、GPU特定调优、开放式发现等），每个阶段由CoVeR（链式验证与精炼）智能体驱动，生成候选方案并在真实硬件上验证迭代。同时构建了编码Intel GPU约束的知识库（如warp计数、GRF模式、SLM大小），确保LLM输出在架构有效范围内。

Result: 在Intel Arc Pro B70上，97个KernelBench内核实现1.17x几何平均加速比，67%内核有提升，9个内核超过5x（最高82x）；Flash Attention在所有配置下实现2-13.3x加速，无回归。

Conclusion: 结构化领域知识与硬件在环验证相结合，可以系统性地消除新加速器上算法部署中重复的内核移植工作。

Abstract: Porting deep learning algorithms to new hardware accelerators requires developers to repeatedly apply the same low-level optimizations -- quantization, memory access coalescing, tile size tuning, and architecture-specific workarounds -- to every Triton kernel in their code-base. This manual, repetitive effort is a major bottleneck: each kernel demands the same cycle of trial-and-error profiling against hardware constraints that vary across devices, yet the underlying optimization patterns remain largely consistent. We present Xe-Forge, a multi-stage LLM-powered pipeline that automates this process for Intel GPU. Given a functionally correct Triton kernel, the system applies up to nine optimization stages -- from algorithmic restructuring and operator fusion through block pointer modernization, GPU-specific tuning, and open-ended discovery -- each driven by a Chain-of-Verification-and-Refinement (CoVeR) agent that generates candidates, validates them on real hardware, and iterates on failures. A curated knowledge base encodes Intel GPU constraints (power-of-two warp counts, GRF modes, SLM sizing) that are absent from LLM training data, keeping the model within architecturally valid bounds. We evaluate Xe-Forge on 97 Level-2 KernelBench kernels and Flash Attention on the Intel Arc Pro B70, achieving a 1.17x geometric mean speedup over PyTorch eager with 67% of kernels improving, nine kernels exceeding 5x (up to 82x), and 2--13.3x speedups on Flash Attention across all tested configurations without regression -- demonstrating that structured domain knowledge with hardware-in-the-loop verification can systematically eliminate the repetitive porting effort that currently gates algorithm deployment on new accelerators.

</details>


### [65] [Edge AI Deployment Beyond Models: A BSP-Aware Systems Framework for Industrial Embedded Platforms](https://arxiv.org/abs/2605.26119)
*Pitchai Muthu M*

Main category: cs.DC

TL;DR: 本文论证工业边缘AI部署应作为系统问题而非后期应用打包问题，提出了面向嵌入式平台的BSP感知框架，涵盖硬件、BSP/OS适配、运行时与加速、应用/推理、运维/验证五个层级。


<details>
  <summary>Details</summary>
Motivation: 工业边缘AI项目常先做模型后考虑平台，但这种顺序在嵌入式系统（长生命周期、供应商特定内核、异构加速器、安全约束、复杂I/O路径）中行不通，模型只是从传感器到生产服务环路的执行链中的一个组件。

Method: 基于Android、NXP i.MX、NVIDIA Jetson、ONNX Runtime、TensorRT等厂商架构文档以及嵌入式AI基准测试、设备不稳定性和异构边缘集群相关系统文献，提出五层框架。

Result: 提出了一个将底层平台工作与可测量的部署成果（可复现性、可诊断性、持续吞吐量、现场可靠性）联系起来的实用框架。

Conclusion: 工业边缘AI部署必须被当作系统问题处理，框架有助于实现鲁棒的部署成果。

Abstract: Industrial Edge AI programs often begin with the model and only later confront the platform. That sequencing is attractive because it allows early demonstrations, but it breaks down when the deployment target is an embedded system with long product lifecycles, vendor-specific kernels, heterogeneous accelerators, safety constraints, and nontrivial I/O paths. In that environment, a model is only one component of a larger execution chain that begins at the sensor, traverses the board support package (BSP), and ends in a production service loop. This paper argues that robust Edge AI deployment must be treated as a systems problem rather than a late-stage application packaging exercise. The paper presents a BSP-aware framework for industrial embedded platforms organized around five layers: hardware, BSP/operating-system adaptation, runtime and acceleration, application/inference, and operations/validation. The discussion is grounded in vendor architecture documentation for Android, NXP i.MX, NVIDIA Jetson, ONNX Runtime, and TensorRT, and in systems literature on embedded AI benchmarking, device instability, and heterogeneous edge fleets. The result is a practical framework that connects low-level platform work to measurable deployment outcomes such as reproducibility, diagnosability, sustained throughput, and field reliability.

</details>


### [66] [Semantic-aware Token Selection and Resource Optimization for Communication-efficient Split Federated Fine-tuning in Edge Intelligence](https://arxiv.org/abs/2605.26120)
*Xianke Qiang,Zheng Chang,Geyong Min*

Main category: cs.DC

TL;DR: 提出ST-SFLora，一个基于语义令牌的拆分联邦LoRA微调框架，通过引入语义传输效率（STE）指标和联合资源优化算法，解决边缘设备上部署大型Transformer视觉模型时的计算和通信瓶颈问题。


<details>
  <summary>Details</summary>
Motivation: 在资源受限的移动边缘设备上部署大型Transformer视觉模型面临硬件限制和动态无线环境的严重约束；联邦学习的本地微调对边缘设备计算负担过重；拆分联邦学习（SFL）虽可卸载深层到边缘服务器，但传输高维激活令牌导致巨大通信开销。

Method: 提出ST-SFLora框架，引入语义传输效率（STE）指标平衡语义保留与传输成本；基于STE构建联合资源优化问题（混合整数非凸），通过交替优化算法动态决定令牌选择、上行带宽分配和传输功率。

Result: 在多个基准上，ST-SFLora在所有基线中实现了最低的客户端资源消耗，并在通信效率和模型性能之间取得了有利的平衡。

Conclusion: ST-SFLora有效解决了边缘设备上大型视觉模型联邦微调的通信瓶颈问题，为资源受限环境下的分布式AI推理与训练提供了可行方案。

Abstract: Deploying large Transformer-based vision models on resource-limited mobile devices at network edge is severely constrained by hardware limitations and dynamic wireless environments. While federated learning (FL) enables collaborative training without sharing raw data, strictly local fine-tuning of such massive models remains computationally prohibitive for edge devices. Split federated learning (SFL) alleviates this burden by offloading deep layers to the edge server, yet it suffers from heavy communication overhead when transmitting high-dimensional activation tokens. To address this bottleneck, we propose ST-SFLora, a semantic token-based split federated LoRA fine-tuning framework. We introduce a new metric, \emph{Semantic Transmission Efficiency} (STE), to balance semantic retention and transmission cost. Based on STE, we formulate a joint resource optimization problem that dynamically determines token selection, uplink bandwidth allocation, and transmit power under latency and energy constraints. The resulting mixed-integer nonconvex problem is efficiently solved via an alternating algorithm. Experiments on multiple benchmarks demonstrate that ST-SFLora achieves the lowest client-side resource consumption among baselines while delivering a favorable trade-off between communication efficiency and model performance.

</details>


### [67] [Agentic AI Workload Characteristics](https://arxiv.org/abs/2605.26297)
*Yichao Yuan,Ankita Nayak,Souvik Kundu,Nishil Talati*

Main category: cs.DC

TL;DR: 该论文通过端到端追踪基础设施，系统刻画了ReAct-style智能体在LLM推理和工具执行两个维度的工作负载特征，发现智能体工作负载并非简单的长提示词负载：在有效上下文缓存下，多数输入token跨轮次复用，计算转为解码主导，同时工具使用呈现清晰的阶段性时间结构（前期读/探索，后期执行/写）。


<details>
  <summary>Details</summary>
Motivation: 随着Agentic AI的兴起，LLM服务从独立的提示-生成请求转变为有状态的多轮执行，现有研究缺乏对这一新型工作负载在LLM服务和工具执行两个层面的系统理解与刻画。

Method: 使用端到端追踪基础设施，在推理型和非推理型的Gemma和Qwen配置上，针对五个智能体基准测试，从LLM服务和工具执行两个视角对ReAct-style智能体进行特征分析。

Result: 智能体工作负载并非长提示词负载；有效上下文缓存使多数输入token跨轮复用，执行转为解码主导且依赖长期KV-cache状态；工具使用呈现从读/探索到执行/写的清晰时间结构。

Conclusion: 高效的智能体服务需要联合管理重复模型重入、持久上下文状态和依赖工作负载的工具行为。

Abstract: Agentic AI shifts LLM serving from isolated prompt-generation requests to stateful, multi-turn executions that repeatedly invoke the model, call tools, and grow context over time. This paper characterizes ReAct-style agents from both the LLM-serving and tool-execution perspectives using an end-to-end tracing infrastructure across reasoning and non-reasoning Gemma and Qwen configurations on five agentic benchmarks. Our study shows that agentic workloads are not simply long-prompt workloads: with effective context caching, most input tokens are reused across turns, making execution decode-dominated while increasing dependence on long-lived KV-cache state. We also find that tool use has a clear temporal structure, with agents shifting from read/explore behavior early in execution to execute/write behavior later. These results show that efficient agentic serving must jointly manage repeated model re-entry, persistent context state, and workload-dependent tool behavior.

</details>


### [68] [Totoro$^+$: An Adaptive and Scalable Edge Federated Learning System](https://arxiv.org/abs/2605.26323)
*Cheng-Wei Ching,Xin Chen,Taehwan Kim,Jian-Jhih Kuo,Dilma Da Silva,Liting Hu*

Main category: cs.DC

TL;DR: Totoro$^+$ 是一个基于分布式哈希表（DHT）和P2P架构的去中心化联邦学习系统，支持海量FL应用同时在边缘网络上运行，通过为每个应用分配专用参数服务器、环形结构和发布/订阅抽象实现线性可扩展性。


<details>
  <summary>Details</summary>
Motivation: 传统联邦学习系统采用中心化参数服务器架构，难以支持大量FL应用同时在边缘网络上高效运行，存在单点瓶颈和可扩展性问题。

Method: 基于DHT的P2P模型重新设计FL系统；每个应用分配专用参数服务器；引入三个创新：局部感知P2P多环结构、发布/订阅森林抽象、博弈论路径规划模型（保证ε-近似纳什均衡）。

Result: 在500个AWS EC2服务器上实验，总训练时间加速1.2倍到14.0倍，模型分发和梯度聚合仅需O(log N)跳，可扩展到数百万节点。

Conclusion: Totoro$^+$ 实现了完全去中心化的FL系统，显著提升了可扩展性、自适应性和对边缘网络动态变化的鲁棒性。

Abstract: Federated Learning (FL) is an emerging distributed machine learning (ML) technique that enables in-situ model training and inference on decentralized edge devices. We propose Totoro$^+$, a novel scalable FL system that enables massive FL applications to run simultaneously on edge networks. The key insight is to explore a distributed hash table (DHT)-based peer-to-peer (P2P) model to re-architect the centralized FL system design into a fully decentralized one. In contrast to previous studies where many FL applications shared one centralized parameter server, Totoro$^+$ assigns a dedicated parameter server to each application. Any edge node can act as any application's coordinator, aggregator, client selector, worker (participant device), or any combination of the above, thereby radically improving scalability and adaptivity. Totoro$^+$ introduces three innovations to realize its design: a locality-aware P2P multi-ring structure, a publish/subscribe-based forest abstraction, and a game-theoretic path planning model with a guarantee of an $ε$-approximate Nash equilibrium. Real-world experiments on 500 Amazon EC2 servers show that Totoro$^+$ scales gracefully with the number of FL applications and $N$ edge nodes speeds up the total training time by $1.2\times-14.0\times$, achieves $\mathcal{O}(\log N)$ hops for model dissemination and gradient aggregation with millions of nodes, and efficiently adapts to the practical edge networks and churns.

</details>


### [69] [GridPilot: Real-Time Grid-Responsive Control for AI Supercomputers](https://arxiv.org/abs/2605.26384)
*Denisa-Andreea Constantinescu,David Atienza*

Main category: cs.DC

TL;DR: GridPilot 是一个三层次预测控制器，能够在真实硬件上将电网请求转化为GPU功率变化，端到端响应时间97.2毫秒，比北欧快速频率储备的700毫秒要求快6.9倍，同时集成了PUE校正以确保电表级别的可靠性。


<details>
  <summary>Details</summary>
Motivation: AI/HPC设施的大规模电力需求快速增长，电网需要能够快速响应（秒级）的灵活负载以吸收波动的风能和太阳能发电，但现有方案缺乏实际可测量的软件栈端到端响应能力。

Method: 提出GridPilot三层次预测控制器，在毫秒、秒和小时三个时间尺度上运行，并增加了确定性安全岛旁路实现快速响应。在NVIDIA V100三GPU测试平台上验证，同时集成瞬时PUE校正。

Result: 端到端触发到目标响应时间97.2毫秒，比700毫秒要求快6.9倍。在六个欧洲电网的回放实验中，PUE感知控制器缩小了2.5-5.8个百分点的冷却开销拖累。

Conclusion: 证明了兆瓦级AI/HPC设施可以通过设计实现可控的、电网响应的灵活性，GridPilot已开源。

Abstract: At global scale, data-center electricity demand is growing faster than the grids that supply it, while system operators increasingly require large flexible loads that can adjust power within seconds to absorb variable wind and solar generation. For multi-megawatt AI/HPC facilities, the key unresolved question is practical and measurable: how quickly can the software stack translate a grid request into a real change in GPU power at the facility meter, where commitments are settled? We answer this on real hardware with GridPilot, a three-tier predictive controller operating across milliseconds, seconds, and hours, augmented by a deterministic safety-island bypass for fast response. On a three-GPU NVIDIA V100 testbed, GridPilot achieves a measured end-to-end trigger-to-target response of 97.2 ms, which is 6.9x faster than the 700 ms requirement of Nordic Fast Frequency Reserve. We further incorporate an instantaneous Power Usage Effectiveness (PUE) correction so dispatched commitments remain robust at meter level rather than only at IT load level. In replay experiments across six representative European grids (from Sweden to Poland), the PUE-aware controller closes 2.5-5.8 percentage points of cooling-overhead drag. GridPilot is released as open source and serves as a proof of concept that MW-scale AI/HPC demand can be engineered as controllable, grid-responsive flexibility by design.

</details>


### [70] [Configuration-Driven Dynamic API Routing for Resilient Service Integrations](https://arxiv.org/abs/2605.26404)
*Nataraj Agaram Sundar,Tejas Morabia*

Main category: cs.DC

TL;DR: 提出了一种配置驱动的动态API路由架构，通过可插拔因子列表、实时遥测、熔断器、舱壁隔离和闭环决策引擎，实现第三方服务的弹性集成，支持运行时动态切换供应商而无需重新部署应用。


<details>
  <summary>Details</summary>
Motivation: 现代在线服务高度依赖第三方API（如认证、支付、通信等），但这些依赖不受应用所有者直接控制，可能出现区域性故障、限流、延迟飙升等问题，导致用户可见的失败。现有方案缺乏灵活且可动态调整的路由策略。

Method: 提出配置驱动的动态API路由架构，核心包括：(1) 可插拔因子列表，定义操作级别的硬门控和加权评分函数，综合实时指标、区域策略、配额、延迟、成本和故障信号评估候选供应商；(2) 实时遥测与滑动窗口健康指标计算；(3) 熔断器和舱壁隔离；(4) 闭环决策引擎，支持运行时调整路由策略而无需重新部署。

Result: 形式化了因子列表模型，描述了请求时路由算法和事件流水线；通过匿名化短信验证案例展示了用完成率遥测驱动的自动路由替代手动供应商切换的实际效果。

Conclusion: 该架构能有效解耦路由策略与应用代码，支持运行时动态适应第三方服务状态变化，提升系统弹性，减少人工干预。

Abstract: Modern online services rely on third-party APIs for authentication, payments, communication, identity verification, fraud detection, observability, and fulfillment. These dependencies are outside the direct operational control of the application owner and may experience regional outages, throttling, latency spikes, quota exhaustion, or behavior changes that surface as user-visible failures. This paper presents configuration-driven dynamic API routing, an architecture for resilient third-party service integration based on pluggable factor lists, real-time telemetry, circuit breakers, bulkhead isolation, and a closed-loop decision engine. A factor list defines operation-specific hard gates and weighted scoring functions that evaluate candidate providers using live metrics, regional policy constraints, quota state, latency, cost, and incident signals. The router separates routing policy from application code, allowing operators to adapt vendor selection at runtime without redeploying applications. We formalize the factor-list model, describe a request-time routing algorithm, present the event pipeline that computes sliding-window provider health metrics, and analyze failover behavior under degraded-provider scenarios. We also describe an anonymized SMS verification case study in which manual vendor switching was replaced by automated routing driven by completion-rate telemetry.

</details>


### [71] [Characterization-Guided GPU Fault Resilience in NVIDIA MPS](https://arxiv.org/abs/2605.26461)
*Rixin Liu,Xingqi Cui,Kaijian Wang,Xinheng Ding,Zirui Liu,Yuke Wang,Jiarong Xing*

Main category: cs.DC

TL;DR: 本文针对NVIDIA MPS在多进程GPU共享中存在的故障脆弱性问题，提出了一种故障弹性MPS设计方案，通过故障隔离（针对内存相关故障）和快速恢复（基于虚拟内存的GPU状态共享）两种互补机制，提升GPU集群的容错能力。


<details>
  <summary>Details</summary>
Motivation: NVIDIA MPS允许GPU细粒度共享，但一个进程的故障会导致所有协同进程终止，限制了其在多租户GPU集群等对容错性要求高的场景中的应用。

Method: 通过对GPU故障的系统性表征和端到端处理流水线分析，设计了两种互补机制：(1) 故障隔离机制，针对占主导的内存相关故障，在开源GPU驱动内核模块中通过软件干预实现完全隔离；(2) 快速恢复机制，针对其他故障（涉及闭源软件），利用基于虚拟内存的GPU驻留状态共享实现快速恢复。

Result: 在不同GPU和工作负载上的评估表明，这些机制能够有效处理相应故障，且开销极小。

Conclusion: 提出的故障弹性MPS设计能够在不引入显著性能开销的前提下，显著提升MPS在多进程GPU共享场景中的容错能力，有利于其在多租户GPU集群中的实际部署。

Abstract: NVIDIA Multi-Process Service (MPS) enables fine-grained GPU sharing by allowing multiple processes to execute concurrently on the same GPU, making it an important mechanism for improving GPU utilization. However, MPS has weak fault resilience: a fault in one process can terminate all co-running processes, limiting its adoption in resilience-critical settings such as multi-tenant GPU clusters. In this work, we design fault-resilient MPS to solve this problem. Our design is guided by insights from a systematic characterization of GPU faults and a deep analysis of their end-to-end processing pipeline. Based on these insights, we design two complementary mechanisms. A fault isolation mechanism for the dominant memory-related faults that can be fully isolated by software intervention in the open GPU driver kernel module. For other faults whose process is within proprietary software, we design a practical mechanism -- fast recovery using virtual memory based GPU-resident state sharing. Our evaluation on different GPUs and workloads shows that these mechanisms can handle corresponding faults effectively with minimal overhead.

</details>


### [72] [StreamSplit: Continuous Audio Representation Learning via Uncertainty-Guided Adaptive Splitting](https://arxiv.org/abs/2605.26523)
*Minh K. Quan,Pubudu N. Pathirana*

Main category: cs.DC

TL;DR: StreamSplit 是一个让大规模对比学习能够在边缘设备上高效运行的框架，通过分布式的混合损失函数和基于强化学习的自适应分割策略，在保持精度的同时大幅降低延迟、带宽和能耗。


<details>
  <summary>Details</summary>
Motivation: 边缘设备资源受限，大规模对比学习（如CLAP、COLA）需要大批量数据，但边缘端小批量会降低模型质量，云端卸载又存在延迟和带宽问题。现有静态模型压缩无法适应边缘环境的动态变化。

Method: 提出StreamSplit框架：1) 基于分布的流式框架，用Hybrid Loss将表征质量与本地批量大小解耦；2) 不确定性引导的自适应分割器，通过轻量级RL策略结合实时资源监控和嵌入模糊度动态优化计算分配。

Result: 在Raspberry Pi 4到Apple M2等硬件上评估，相比纯服务端方案，单样本延迟降低4.7倍，带宽降低77.1%，能耗降低52.3%，精度仅下降2.2%。

Conclusion: 证明了自适应的分布式学习是边缘生态系统的可行路径，使流式对比学习在异构ARM平台上变得实用。

Abstract: Large-batch Contrastive Learning (CL), the foundation of modern representation learning, is fundamentally incompatible with the volatile resource constraints of edge devices. This conflict creates a dilemma: small on-device batches degrade model fidelity, while offloading to the cloud incurs unacceptable latency and bandwidth costs. Existing solutions often resort to static model compression, which fails to adapt to the runtime volatility of edge environments. To bridge this gap, we present StreamSplit, a novel framework that makes streaming CL practical across heterogeneous ARM client platforms. StreamSplit resolves the conflict between the continuous nature of ambient audio and the discrete batch requirements of models like CLAP and COLA. We introduce: (1) A distribution-based streaming framework that decouples representation quality from local batch size, using a tractable Hybrid Loss to maintain fidelity despite sparse updates; and (2) An Uncertainty-Guided Adaptive Splitter that uses a lightweight Reinforcement Learning (RL) policy to dynamically partition computation. Uniquely, this policy integrates real-time resource monitoring with embedding ambiguity to optimize the accuracy-latency trade-off on the fly. We evaluate StreamSplit on diverse hardware, from the resource-constrained Raspberry Pi 4 to the high-performance Apple M2. Results demonstrate that StreamSplit reduces per-sample latency by up to 4.7x and cuts bandwidth by 77.1% and energy by 52.3% compared to server-centric baselines. Crucially, it maintains accuracy within 2.2% of server-centric models, proving that adaptive, distributed learning is a viable path for the modern edge ecosystem.

</details>


### [73] [Reducing Internal State in Eigenvalue-Only Divide-and-Conquer Tridiagonal Eigensolvers](https://arxiv.org/abs/2605.26599)
*Ruiyi Zhan,Shaoshuai Zhang*

Main category: cs.DC

TL;DR: 提出一种边界行分治算法用于仅需特征值的对称特征分解，通过仅传播边界行/列而非完整特征向量矩阵，将内存需求从二次降为线性，并消除了传统延迟重放方案中不必要的矩阵-向量计算。


<details>
  <summary>Details</summary>
Motivation: 传统分治算法在仅需特征值的场景下仍然会物化或重放大规模变换矩阵，导致内存和计算开销过大，使得实际数值库通常默认使用QR方法而非分治方法。

Method: 边界行分治算法：在分治递归过程中仅传播和操作选定的边界行/列，而非完整的累积特征向量矩阵，从而在线性空间内完成特征值计算。

Result: 提供了算法的时间/空间复杂度分析、正确性与稳定性论证，以及在CPU和GPU上的优化实现，并与标准数值库中的QR和D&C例程进行了对比评估。

Conclusion: 所提算法显著降低了分治策略在纯特征值计算中的内存和计算开销，使其在现代多核CPU和GPU上比QR方法更具竞争力。

Abstract: Divide and Conquer (D&C) is a widely used algorithmic strategy for symmetric eigenvalue decomposition. Its natural parallelism makes D&C attractive on modern multicore CPUs and GPUs, but existing eigenvalue-only routines often default to QR-based methods because conventional D&C still materializes or replays large transformation matrices during the conquer phase. This paper proposes a boundary-row D&C algorithm for eigenvalue-only computation. The key observation is that the conquer phase only needs selected boundary rows/columns rather than the full accumulated eigenvector matrix. By propagating these boundary rows directly through the recursion, the proposed algorithm reduces the memory requirement from quadratic to linear space while also eliminating unnecessary matrix-vector work in the conventional lazy-replay formulation. We provide the algorithm, its time and space complexity analysis, correctness and stability arguments, optimized CPU and GPU implementations, and an evaluation against QR and D&C routines in standard numerical libraries.

</details>


### [74] [Revisiting Bruck: Phase-Efficient All-to-All Communication in Reconfigurable Networks](https://arxiv.org/abs/2605.26930)
*Anton Juerss,Stefan Schmid*

Main category: cs.DC

TL;DR: 本文提出ReTri，一种面向光可重构网络的双向All-to-All通信调度方案，利用平衡三进制块传播在O(log₃ n)阶段内完成通信，并通过配对双向交换将重配置延迟分摊到多个阶段，相比静态方案最高提升10倍性能。


<details>
  <summary>Details</summary>
Motivation: All-to-All通信是分布式ML和HPC工作负载的关键性能瓶颈，光可重构网络(ORN)通过动态调整物理拓扑有望改善通信效率，但其收益严重依赖于是否存在可被稀疏可重用拓扑状态服务的结构化通信阶段，亟需通信模式与重配置策略的协同设计。

Method: 提出ReTri，一种双向All-to-All调度方案：采用平衡三进制块传播机制，将通信组织为⌈log₃ n⌉个阶段；利用配对双向交换使得重配置延迟可以在多个阶段间分摊，实现通信模式与重配置策略的联合优化。

Result: 初步仿真结果显示，即使在毫秒级重配置延迟下，ReTri相比静态All-to-All提升最高10倍完成时间，相比可重构Bruck方案提升最高2.1倍。

Conclusion: 光可重构网络中All-to-All的性能可以通过通信调度与拓扑重配置的协同设计获得显著提升，ReTri为面向AI/HPC的ORN网络架构提供了有效的优化方向。

Abstract: All-to-All communication is a key performance bottleneck for distributed machine learning (ML) and high-performance computing (HPC) workloads, where dense traffic increasingly stresses scale-up interconnects. While these ML and HPC workloads have driven unprecedented infrastructure demand, optical reconfigurable networks (ORNs) offer a promising path forward. By adapting the physical topology to the active workload, they improve communication cost and bandwidth utilization. However, their benefit is critically contingent on whether the collective consists of structured phases that can be served by sparse and reusable topology states.
  In this paper, we revisit Bruck's All-to-All implementation and demonstrate the benefits of topology optimization in which both communication pattern and reconfiguration strategy are co-designed. We present ReTri, a bidirectional All-to-All schedule for ORNs. ReTri uses balanced ternary block propagation to complete All-to-All in $\lceil \log_3 n\rceil$ phases. The induced reconfiguration strategy from ReTri's pairwise bidirectional exchanges allow reconfiguration delays to be amortized across multiple phases. Preliminary simulations show that ReTri improves completion time by up to $10\times$ over static All-to-All, even for millisecond-scale reconfiguration delays, and improving reconfigurable Bruck by up to $2.1\times$.

</details>


### [75] [Nonlinear spectral clustering with C++ GraphBLAS](https://arxiv.org/abs/2605.26975)
*Dimosthenis Pasadakis,Olaf Schenk,Verner Vlacic,Albert-Jan Yzelman*

Main category: cs.DC

TL;DR: 本文提出了一种基于C++ GraphBLAS API的p-范数（p∈(1,2]）非线性谱聚类算法的共享内存实现，在多达800万节点和4800万边的数据集上展示了强扩展能力。


<details>
  <summary>Details</summary>
Motivation: 非线性谱聚类方法虽然在数值精度和数学理论上具有优势，但估计多个非线性特征向量的计算成本较高，需要更高效的实现方案。

Method: 使用C++ GraphBLAS API实现p-范数下的直接多路谱聚类算法，将关键操作表达为线性代数形式，在稀疏矩阵和稠密向量上执行，并参数化相关代数运算。

Result: 在多个人工测试集上验证了算法的有效性和准确性，与竞争方法相比在平衡图割度量上取得了高质量聚类结果，并在多达800万节点和4800万边的数据集上展示了强扩展能力。

Conclusion: 提出的基于GraphBLAS的p-范数谱聚类实现能够高效处理大规模图数据，具有良好的并行扩展性能。

Abstract: Nonlinear reformulations of the spectral clustering method have gained a lot of recent attention due to their increased numerical benefits and their solid mathematical background. However, the estimation of the multiple nonlinear eigenvectors is associated with an increased computational cost. We present an implementation of a direct multiway spectral clustering algorithm in the $p$-norm, for $p\in(1,2]$, using a novel C++ GraphBLAS API. The key operations are expressed in linear algebraic terms and are executed over the resulting sparse matrices and dense vectors, parameterized in the algebra pertinent to the computation. We demonstrate the effectiveness and accuracy of our shared-memory algorithm on several artificial test cases. Our numerical examples and comparative results against competitive methods indicate that the proposed implementation attains high quality clusters in terms of the balanced graph cut metric. The strong scaling capabilities of our algorithm are showcased on a range of datasets with up to $8$ million nodes and $48$ million edges.

</details>


### [76] [Autonomic Federated-Market Orchestration for the Edge-Cloud Continuum](https://arxiv.org/abs/2605.27106)
*Lauri Lovén,Roberto Morabito,Abhishek Kumar,Susanna Pirttikangas,Jukka Riekki,Sasu Tarkoma*

Main category: cs.DC

TL;DR: 本文提出 Neural Pub/Sub，一个基于联邦代理的自主计算 substrate，通过市场驱动的价格信号在边缘-云连续体中实现跨自治域的自组织管理，同时保障数据主权。其在4-VM、4域、48 worker的测试平台上验证了相比集中式编排器2-4%的性能优势，并展现了强大的抗故障能力。


<details>
  <summary>Details</summary>
Motivation: 边缘-云连续体需要跨自治管理域的自管理机制，同时要满足租户和运营商指定的数据主权要求，现有集中式控制方法难以同时满足可扩展性和主权约束。

Method: 提出联邦代理架构的 Neural Pub/Sub，采用MAPE-K控制循环，基于Walrasian市场均衡理论，通过边际成本清算价格分析、多拟阵可行域上的放置规划、联邦跨域调度和带边界陈旧性价格信号的共享对等订阅摘要来实现去中心化资源分配。

Result: 在4-VM、4域、48 worker的联邦边缘-云测试平台上进行了1005次运行实验。联邦市场相比单进程oracle提升2-4%（45/45种子胜出，sign-test p~2.8e-14）；与等进程数的四分片集中式编排器差距在±1.5%以内；轮询调度完成率从98.8%崩溃至3.3%，而市场机制保持完成率；联邦能承受代理死亡和网络分区（完成率≥98.7%）；主权 enforcement 无额外运行时开销。

Conclusion: 基于市场价格信号的联邦自主计算 substrate 能有效替代集中式控制，在保障数据主权的同时实现接近甚至优于集中式编排器的性能，并具有更强的鲁棒性。异构域压力测试和跨站点WAN部署留待未来工作。

Abstract: The edge-cloud computing continuum demands self-management mechanisms that scale across autonomous administrative domains while honouring tenant- and operator-specified data sovereignty. We present Neural Pub/Sub, a federated-broker autonomic substrate whose self-organising behaviour emerges from market-based price signals rather than centralised control. Its MAPE-K control loop closes over per-broker health and load monitoring, marginal-cost clearing-price analysis, placement planning over a polymatroidal feasibility region, federated cross-domain dispatch, and shared peer subscription summaries with bounded-staleness price signals. The Plan step is anchored in a Walrasian convergence proposition: under gross-substitutes valuations on tree and series-parallel service-dependency DAGs, decentralised price-based allocation matches the welfare of a centralised oracle. We evaluate the substrate on a 4-VM, 4-domain, 48-worker federated edge-cloud testbed (single data centre, 50 ms emulated WAN) in a 1005-run campaign augmented by a fair-process-count sharded-oracle comparator. The federated market dominates a single-process oracle by 2-4% with 45 of 45 per-seed wins (sign-test p ~ 2.8e-14, Hodges-Lehmann median -39.6 ms); against a four-shard centralised orchestrator at equal process count the gap stays within +/-1.5% across all nine (pipeline, load) cells. Round-robin completion rate collapses 98.8% -> 22.4% -> 3.3% across arrival rates 5/10/15 pps while the market preserves completion; the advantage decomposes into three Walrasian properties (information completeness, admission control, price discovery). Federation withstands broker death and network partition (completion rate >= 98.7% across 75 cells), and sovereignty enforcement adds no measurable runtime overhead across 60 governance-grid runs. Heterogeneous-domain stressors and cross-site WAN deployment remain future work.

</details>
